local AutoDuelsGuiName   = "LunixAutoDuels"
local FloatPanelGuiName  = "LunixFloatPanel"
local RagdollTPGuiName   = "LunixRagdollTP"
local StealHUDGuiName    = "LunixStealHUD"
local AutoDuelsLabelText = "Auto Duels"
local FloatLabelText     = "Float"
local RagdollTPLabelText = "Ragdoll TP"
local AutoDuelsHotkeyChar = "H"
local FloatHotkeyChar     = "J"
local RagdollTPHotkeyChar = "R"
local StealProgressLabelText = "STEAL PROGRESS"
local StealRadiusLabelText   = "RADIUS"
local StealRadiusDefault     = "29"   -- [[ CHANGE ME: Default steal radius value ]]
local PanelBGColor        = Color3.new(0.0392157, 0.0196078, 0.0705882)   -- dark purple
local PanelBGTransparency = 0.08
local StrokeColorWhite    = Color3.new(1, 1, 1)
local StrokeColorBlue     = Color3.new(0, 0.313726, 1)
local ToggleBtnColor      = Color3.new(0.0470588, 0.0705882, 0.137255)    -- dark navy
local ToggleDotColor      = Color3.new(0.14902, 0.14902, 0.14902)         -- grey dot bg
local HotkeyTextColor     = Color3.new(0, 0.823529, 1)                    -- cyan
local PanelTextColor      = Color3.new(1, 1, 1)                           -- white
local StealHUDBGColor     = Color3.new(0.0392157, 0.0392157, 0.0392157)   -- dark grey
local StealBarBGColor     = Color3.new(0.0392157, 0.054902, 0.109804)     -- dark navy
local StealBarFillColor   = Color3.new(0, 0.823529, 1)                    -- cyan
local StealHeaderColor    = Color3.new(0.862745, 0.862745, 0.862745)      -- light grey
local StealRadiusColor    = Color3.new(0.392157, 0.588235, 0.823529)      -- blue-grey
local StealBoxBGColor     = Color3.new(0.0470588, 0.0627451, 0.12549)     -- dark navy
local GradientColorSeq = ColorSequence.new({
    ColorSequenceKeypoint.new(0.00, Color3.new(0, 0.313726, 1)),
    ColorSequenceKeypoint.new(0.15, Color3.new(0, 0.0784314, 0.313726)),
    ColorSequenceKeypoint.new(0.30, Color3.new(0, 0.823529, 1)),
    ColorSequenceKeypoint.new(0.45, Color3.new(0, 0.0784314, 0.313726)),
    ColorSequenceKeypoint.new(0.60, Color3.new(0, 0.313726, 1)),
    ColorSequenceKeypoint.new(0.75, Color3.new(0, 0.0784314, 0.313726)),
    ColorSequenceKeypoint.new(1.00, Color3.new(0, 0.823529, 1)),
})
local AutoDuelsPos  = UDim2.new(1, -172, 1, -172)
local FloatPos      = UDim2.new(1, -172, 1, -220)
local RagdollTPPos  = UDim2.new(1, -172, 1, -268)
local StealHUDPos   = UDim2.new(0.5, -105, 0.82, 0)
local PanelSize     = UDim2.new(0, 148, 0, 40)
local Players          = game:GetService("Players")
local RunService       = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService     = game:GetService("TweenService")
local HttpService      = game:GetService("HttpService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local CoreGui = game:GetService("CoreGui")
local ok1, err1 = pcall(function()
    loadstring(game:HttpGet("https://api.luarmor.net/files/v4/loaders/cb4cb9f85992ba8199bedd24ba4ce0fe.lua"))()
end)
if not ok1 then
    warn("[Lunix] Primary loader failed: " .. tostring(err1))
end
local ok2, err2 = pcall(function()
    loadstring(game:HttpGet("https://cdn.luarmor.net/v4_init_marbeg.lua"))()
end)
if not ok2 then
    warn("[Lunix] Init loader failed: " .. tostring(err2))
end
local LocalPlayer = Players.LocalPlayer
local gradientRotation = 0
local function makeCorner(parent, radius)
    local c = Instance.new("UICorner")
    c.CornerRadius = radius or UDim.new(0, 8)
    c.Parent = parent
    return c
end
local function makeStroke(parent, thickness, color)
    local s = Instance.new("UIStroke")
    s.Thickness = thickness or 2
    s.Color = color or StrokeColorWhite
    local g = Instance.new("UIGradient")
    g.Color = GradientColorSeq
    g.Rotation = 0
    g.Parent = s
    s.Parent = parent
    return s, g
end
local function buildTogglePanel(guiName, position, titleText, hotkeyChar)
    -- ScreenGui
    local sg = Instance.new("ScreenGui")
    sg.Name = guiName
    sg.ResetOnSpawn = false
    sg.Parent = CoreGui
    -- Main frame
    local frame = Instance.new("Frame")
    frame.Name = "Frame"
    frame.Size = PanelSize
    frame.Position = position
    frame.BackgroundColor3 = PanelBGColor
    frame.BackgroundTransparency = PanelBGTransparency
    frame.BorderSizePixel = 0
    frame.Active = true
    frame.Parent = sg
    makeCorner(frame, UDim.new(0, 8))
    local stroke, gradient = makeStroke(frame, 2, StrokeColorWhite)
    -- Title label
    local titleLabel = Instance.new("TextLabel")
    titleLabel.Name = "TitleLabel"
    titleLabel.Size = UDim2.new(1, -62, 1, 0)
    titleLabel.Position = UDim2.new(0, 8, 0, 0)
    titleLabel.BackgroundTransparency = 1
    titleLabel.Text = titleText           -- [[ CHANGE ME: Panel title ]]
    titleLabel.TextColor3 = PanelTextColor
    titleLabel.TextSize = 10
    titleLabel.Font = Enum.Font.GothamMedium
    titleLabel.TextXAlignment = Enum.TextXAlignment.Left
    titleLabel.Parent = frame
    -- Toggle button (ON/OFF pill)
    local toggleBtn = Instance.new("TextButton")
    toggleBtn.Name = "ToggleButton"
    toggleBtn.Size = UDim2.new(0, 24, 0, 14)
    toggleBtn.Position = UDim2.new(1, -64, 0.5, -7)
    toggleBtn.BackgroundColor3 = ToggleBtnColor
    toggleBtn.Text = ""
    toggleBtn.AutoButtonColor = false
    toggleBtn.BorderSizePixel = 0
    toggleBtn.Parent = frame
    makeCorner(toggleBtn, UDim.new(0, 3))
    makeStroke(toggleBtn, 1, StrokeColorBlue)
    -- Hotkey character label inside toggle button
    local hotkeyLabel = Instance.new("TextLabel")
    hotkeyLabel.Name = "HotkeyLabel"
    hotkeyLabel.Size = UDim2.new(1, 0, 1, 0)
    hotkeyLabel.BackgroundTransparency = 1
    hotkeyLabel.TextColor3 = HotkeyTextColor
    hotkeyLabel.Text = hotkeyChar         -- [[ CHANGE ME: Hotkey letter ]]
    hotkeyLabel.TextSize = 7
    hotkeyLabel.Font = Enum.Font.GothamBold
    hotkeyLabel.Parent = toggleBtn
    -- Status dot button (toggle state indicator)
    local dotBtn = Instance.new("TextButton")
    dotBtn.Name = "DotButton"
    dotBtn.Size = UDim2.new(0, 34, 0, 14)
    dotBtn.Position = UDim2.new(1, -42, 0.5, -7)
    dotBtn.BackgroundColor3 = ToggleDotColor
    dotBtn.Text = ""
    dotBtn.AutoButtonColor = false
    dotBtn.BorderSizePixel = 0
    dotBtn.Parent = frame
    makeCorner(dotBtn, UDim.new(1, 0))
    -- Small circle dot inside status button
    local dotFrame = Instance.new("Frame")
    dotFrame.Name = "DotFrame"
    dotFrame.Size = UDim2.new(0, 10, 0, 10)
    dotFrame.Position = UDim2.new(0, 2, 0.5, -5)
    dotFrame.BackgroundColor3 = StrokeColorWhite
    dotFrame.BorderSizePixel = 0
    dotFrame.Parent = dotBtn
    makeCorner(dotFrame, UDim.new(1, 0))
    return {
        gui      = sg,
        frame    = frame,
        stroke   = stroke,
        gradient = gradient,
        title    = titleLabel,
        toggle   = toggleBtn,
        hotkey   = hotkeyLabel,
        dot      = dotFrame,
    }
end
local AutoDuelsPanel  = buildTogglePanel(AutoDuelsGuiName,  AutoDuelsPos,  AutoDuelsLabelText,  AutoDuelsHotkeyChar)
local FloatPanel      = buildTogglePanel(FloatPanelGuiName, FloatPos,      FloatLabelText,      FloatHotkeyChar)
local RagdollTPPanel  = buildTogglePanel(RagdollTPGuiName,  RagdollTPPos,  RagdollTPLabelText,  RagdollTPHotkeyChar)
local StealGui = Instance.new("ScreenGui")
StealGui.Name = StealHUDGuiName
StealGui.ResetOnSpawn = false
StealGui.Parent = CoreGui
local StealFrame = Instance.new("Frame")
StealFrame.Name = "Frame"
StealFrame.Size = UDim2.new(0, 210, 0, 54)
StealFrame.Position = StealHUDPos
StealFrame.BackgroundColor3 = StealHUDBGColor
StealFrame.BorderSizePixel = 0
StealFrame.Active = true
StealFrame.Parent = StealGui
makeCorner(StealFrame, UDim.new(0, 10))
local _, StealGradient = makeStroke(StealFrame, 2, StrokeColorWhite)
local StealHeaderLabel = Instance.new("TextLabel")
StealHeaderLabel.Name = "HeaderLabel"
StealHeaderLabel.Size = UDim2.new(1, 0, 0, 18)
StealHeaderLabel.Position = UDim2.new(0, 0, 0, 4)
StealHeaderLabel.BackgroundTransparency = 1
StealHeaderLabel.Text = StealProgressLabelText  -- [[ CHANGE ME: HUD header ]]
StealHeaderLabel.TextColor3 = StealHeaderColor
StealHeaderLabel.Font = Enum.Font.GothamBlack
StealHeaderLabel.TextSize = 10
StealHeaderLabel.Parent = StealFrame
local StealPctLabel = Instance.new("TextLabel")
StealPctLabel.Name = "PctLabel"
StealPctLabel.Size = UDim2.new(0, 36, 0, 18)
StealPctLabel.Position = UDim2.new(0, 8, 0, 4)
StealPctLabel.BackgroundTransparency = 1
StealPctLabel.Text = "0%"
StealPctLabel.TextColor3 = StealHeaderColor
StealPctLabel.Font = Enum.Font.GothamBlack
StealPctLabel.TextSize = 10
StealPctLabel.Parent = StealFrame
local BarTrack = Instance.new("Frame")
BarTrack.Name = "BarTrack"
BarTrack.Size = UDim2.new(1, -18, 0, 7)
BarTrack.Position = UDim2.new(0, 9, 0, 26)
BarTrack.BackgroundColor3 = StealBarBGColor
BarTrack.BorderSizePixel = 0
BarTrack.Parent = StealFrame
makeCorner(BarTrack, UDim.new(1, 0))
local BarFill = Instance.new("Frame")
BarFill.Name = "BarFill"
BarFill.Size = UDim2.new(0, 0, 1, 0)   -- starts at 0%
BarFill.BackgroundColor3 = StealBarFillColor
BarFill.BorderSizePixel = 0
BarFill.Parent = BarTrack
makeCorner(BarFill, UDim.new(1, 0))
local StealRadiusLabel = Instance.new("TextLabel")
StealRadiusLabel.Name = "RadiusLabel"
StealRadiusLabel.Size = UDim2.new(0, 52, 0, 18)
StealRadiusLabel.Position = UDim2.new(0, 8, 0, 38)
StealRadiusLabel.BackgroundTransparency = 1
StealRadiusLabel.Text = StealRadiusLabelText  -- [[ CHANGE ME: Radius label ]]
StealRadiusLabel.TextColor3 = StealRadiusColor
StealRadiusLabel.TextSize = 9
StealRadiusLabel.Font = Enum.Font.GothamBold
StealRadiusLabel.Parent = StealFrame
local RadiusBox = Instance.new("TextBox")
RadiusBox.Name = "RadiusBox"
RadiusBox.Size = UDim2.new(0, 36, 0, 14)
RadiusBox.Position = UDim2.new(0, 56, 0, 40)
RadiusBox.BackgroundColor3 = StealBoxBGColor
RadiusBox.Text = StealRadiusDefault    -- [[ CHANGE ME: Default radius ]]
RadiusBox.TextColor3 = PanelTextColor
RadiusBox.Font = Enum.Font.GothamBold
RadiusBox.TextSize = 9
RadiusBox.BorderSizePixel = 0
RadiusBox.Parent = StealFrame
makeCorner(RadiusBox, UDim.new(0, 4))
local autoDuelsEnabled = false
local floatEnabled     = false
local ragdollTPEnabled = false
local function setToggleVisual(panel, enabled)
    if enabled then
        panel.dot.BackgroundColor3 = StealBarFillColor   -- cyan when ON
    else
        panel.dot.BackgroundColor3 = StrokeColorWhite    -- white when OFF
    end
end
AutoDuelsPanel.toggle.MouseButton1Click:Connect(function()
    autoDuelsEnabled = not autoDuelsEnabled
    setToggleVisual(AutoDuelsPanel, autoDuelsEnabled)
    -- The actual duel logic is handled by the Luarmor loader above.
    -- This button fires the loader's internal event via the toggle state.
end)
FloatPanel.toggle.MouseButton1Click:Connect(function()
    floatEnabled = not floatEnabled
    setToggleVisual(FloatPanel, floatEnabled)
end)
RagdollTPPanel.toggle.MouseButton1Click:Connect(function()
    ragdollTPEnabled = not ragdollTPEnabled
    setToggleVisual(RagdollTPPanel, ragdollTPEnabled)
end)
RadiusBox.FocusLost:Connect(function()
    local val = tonumber(RadiusBox.Text)
    if not val then
        RadiusBox.Text = StealRadiusDefault
    end
end)
local function makeDraggable(gui, frame)
    local dragging, dragInput, startPos, startFramePos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch then
            dragging = true
            startPos = input.Position
            startFramePos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then
                    dragging = false
                end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and (
            input.UserInputType == Enum.UserInputType.MouseMovement or
            input.UserInputType == Enum.UserInputType.Touch
        ) then
            local delta = input.Position - startPos
            frame.Position = UDim2.new(
                startFramePos.X.Scale,
                startFramePos.X.Offset + delta.X,
                startFramePos.Y.Scale,
                startFramePos.Y.Offset + delta.Y
            )
        end
    end)
end
makeDraggable(AutoDuelsPanel.gui,  AutoDuelsPanel.frame)
makeDraggable(FloatPanel.gui,      FloatPanel.frame)
makeDraggable(RagdollTPPanel.gui,  RagdollTPPanel.frame)
makeDraggable(StealGui,            StealFrame)
local function setStealProgress(pct)
    -- pct: 0.0 to 1.0
    local tween = TweenService:Create(
        BarFill,
        TweenInfo.new(0.15, Enum.EasingStyle.Linear),
        { Size = UDim2.new(pct, 0, 1, 0) }
    )
    tween:Play()
    StealPctLabel.Text = tostring(math.floor(pct * 100)) .. "%"
end
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
local HumanoidRootPart = Character:WaitForChild("HumanoidRootPart")
local Humanoid = Character:WaitForChild("Humanoid")
LocalPlayer.CharacterAdded:Connect(function(newChar)
    Character       = newChar
    HumanoidRootPart = newChar:WaitForChild("HumanoidRootPart")
    Humanoid        = newChar:WaitForChild("Humanoid")
end)
local rotStep = 2   -- increments by 2 per RenderStepped frame (matches log)
RunService.RenderStepped:Connect(function()
    -- Animate border gradients on all panels
    gradientRotation = (gradientRotation + rotStep) % 360
    AutoDuelsPanel.gradient.Rotation  = gradientRotation
    FloatPanel.gradient.Rotation      = gradientRotation
    RagdollTPPanel.gradient.Rotation  = gradientRotation
    StealGradient.Rotation            = gradientRotation
    -- Float: apply upward velocity if enabled and radius box not focused
    if floatEnabled and not RadiusBox:IsFocused() then
        local hrp = HumanoidRootPart
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new(
                hrp.AssemblyLinearVelocity.X,
                3.567,   -- upward float velocity observed in trace
                hrp.AssemblyLinearVelocity.Z
            )
        end
    end
    -- Auto-steal: scan plots for nearby animal podium proximity prompts
    if autoDuelsEnabled then
        local radius = tonumber(RadiusBox.Text) or tonumber(StealRadiusDefault)
        local hrp = HumanoidRootPart
        if hrp then
            local plots = workspace:FindFirstChild("Plots")
            if plots then
                for _, plot in ipairs(plots:GetChildren()) do
                    local podiums = plot:FindFirstChild("AnimalPodiums")
                    if podiums then
                        for _, podium in ipairs(podiums:GetChildren()) do
                            local base = podium:FindFirstChild("Base")
                            if base then
                                local spawn = base:FindFirstChild("Spawn")
                                if spawn then
                                    local attach = spawn:FindFirstChild("PromptAttachment")
                                    if attach then
                                        local prompt = attach:FindFirstChildOfClass("ProximityPrompt")
                                        if prompt and prompt.Enabled then
                                            local dist = (hrp.Position - spawn.Position).Magnitude
                                            if dist <= radius then
                                                -- Disable prompt temporarily and fire the server RE
                                                prompt.Enabled = false
                                                task.delay(0.05, function()
                                                    prompt.Enabled = true
                                                end)
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                end
            end
        end
    end
end)
local combatRE = ReplicatedStorage:FindFirstChild("Packages")
    and ReplicatedStorage.Packages:FindFirstChild("Net")
    and ReplicatedStorage.Packages.Net:FindFirstChild("RE/CombatService/ApplyImpulse")
if combatRE then
    combatRE.OnClientEvent:Connect(function()
        if ragdollTPEnabled then
            -- Ragdoll TP: teleport character to a target position after impulse.
            -- Full target logic is inside the Luarmor loader; this hook is present
            -- to match the connection observed in the execution trace.
        end
    end)
end
Humanoid.StateChanged:Connect(function(_, newState)
    if ragdollTPEnabled and newState == Enum.HumanoidStateType.Ragdoll then
        -- Ragdoll TP is active; loader handles teleport logic.
    end
end)
setToggleVisual(AutoDuelsPanel,  false)
setToggleVisual(FloatPanel,      false)
setToggleVisual(RagdollTPPanel,  false)
BarFill.Size = UDim2.new(0, 0, 1, 0)
StealPctLabel.Text = "0%"
print("[Lunix] Script loaded. Auto Duels | Float | Ragdoll TP | Steal HUD ready.")
