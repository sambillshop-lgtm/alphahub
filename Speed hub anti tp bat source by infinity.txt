local Players = game:GetService("Players") local RunService = game:GetService("RunService") local TweenService = game:GetService("TweenService") local UserInputService = game:GetService("UserInputService") local HttpService = game:GetService("HttpService") local LocalPlayer = Players.LocalPlayer local UIS = UserInputService local State = { antiBatActive = false, infJumpActive = false, antiBatConn = nil, infJumpConn = nil, minimized = false, } local antiBatKey = { value = Enum.KeyCode.ButtonX } local infJumpKey = { value = Enum.KeyCode.ButtonA } local CONFIG_FILE = "SpeedHub_config.json" local function getKeyName(keyCode) local name = tostring(keyCode):gsub("Enum.KeyCode.", "") local map = { LeftControl="LCTRL", RightControl="RCTRL", LeftShift="LSHIFT", RightShift="RSHIFT", LeftAlt="LALT", RightAlt="RALT", ButtonA="A", ButtonB="B", ButtonX="X", ButtonY="Y", ButtonL1="LB", ButtonR1="RB", ButtonL2="LT", ButtonR2="RT", ButtonStart="START", ButtonSelect="SEL", LeftThumbstick="L3", RightThumbstick="R3", DirectionalPadUp="DUP", DirectionalPadDown="DDN", DirectionalPadLeft="DLT", DirectionalPadRight="DRT", } return map[name] or name:sub(1,6):upper() end local function corner(p, r) local c = Instance.new("UICorner", p) c.CornerRadius = UDim.new(0, r or 8) return c end local function saveConfig() pcall(function() if writefile then writefile(CONFIG_FILE, HttpService:JSONEncode({ antiBatKey = tostring(antiBatKey.value):gsub("Enum.KeyCode.", ""), infJumpKey = tostring(infJumpKey.value):gsub("Enum.KeyCode.", ""), })) end end) end local function loadConfig() local ok, raw = pcall(function() return readfile and readfile(CONFIG_FILE) end) if not ok or not raw or raw == "" then return end local success, cfg = pcall(HttpService.JSONDecode, HttpService, raw) if success and cfg then if Enum.KeyCode[cfg.antiBatKey] then antiBatKey.value = Enum.KeyCode[cfg.antiBatKey] end if Enum.KeyCode[cfg.infJumpKey] then infJumpKey.value = Enum.KeyCode[cfg.infJumpKey] end end end pcall(loadConfig) local antiBatStatus, antiBatSwitchBall, antiBatRow, antiBatRowStroke local infJumpStatus, infJumpSwitchBall, infJumpRow, infJumpRowStroke local lastSafeCFrame = nil local lastCheck = 0 local function updateAntiBatVisuals() local on = State.antiBatActive if antiBatStatus then antiBatStatus.Text = on and "● ACTIVE" or "○ DISABLED" antiBatStatus.TextColor3 = Color3.fromRGB(255,255,255) end if antiBatRow and antiBatRowStroke then TweenService:Create(antiBatRow, TweenInfo.new(0.2), { BackgroundTransparency = on and 0.4 or 0.6 }):Play() TweenService:Create(antiBatRowStroke, TweenInfo.new(0.2), { Color = Color3.fromRGB(255,255,255) }):Play() end if antiBatSwitchBall then TweenService:Create(antiBatSwitchBall, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Position = on and UDim2.new(1,-13,0.5,-4.5) or UDim2.new(0,4,0.5,-4.5), BackgroundColor3 = on and Color3.fromRGB(255,255,255) or Color3.fromRGB(180,180,180), }):Play() end end local function startAntiBat() if State.antiBatConn then State.antiBatConn:Disconnect() State.antiBatConn = nil end lastSafeCFrame = nil lastCheck = 0 State.antiBatConn = RunService.Heartbeat:Connect(function() if not State.antiBatActive then return end local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum = char:FindFirstChildOfClass("Humanoid") if not root or not hum or hum.Health <= 0 then return end local now = tick() local vel = root.AssemblyLinearVelocity local speed = vel.Magnitude if speed < 70 then lastSafeCFrame = root.CFrame lastCheck = now return end if speed > 110 and lastSafeCFrame and (now - lastCheck) < 1.5 then root.CFrame = lastSafeCFrame * CFrame.new(0, 0.1, 0) end end) end local function stopAntiBat() if State.antiBatConn then State.antiBatConn:Disconnect() State.antiBatConn = nil end lastSafeCFrame = nil end local function toggleAntiBat() State.antiBatActive = not State.antiBatActive if State.antiBatActive then startAntiBat() else stopAntiBat() end updateAntiBatVisuals() end local holdingJump = false local function updateInfJumpVisuals() local on = State.infJumpActive if infJumpStatus then infJumpStatus.Text = on and "● ACTIVE" or "○ DISABLED" infJumpStatus.TextColor3 = Color3.fromRGB(255,255,255) end if infJumpRow and infJumpRowStroke then TweenService:Create(infJumpRow, TweenInfo.new(0.2), { BackgroundTransparency = on and 0.4 or 0.6 }):Play() TweenService:Create(infJumpRowStroke, TweenInfo.new(0.2), { Color = Color3.fromRGB(255,255,255) }):Play() end if infJumpSwitchBall then TweenService:Create(infJumpSwitchBall, TweenInfo.new(0.25, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Position = on and UDim2.new(1,-13,0.5,-4.5) or UDim2.new(0,4,0.5,-4.5), BackgroundColor3 = on and Color3.fromRGB(255,255,255) or Color3.fromRGB(180,180,180), }):Play() end end local function startInfJump() if State.infJumpConn then State.infJumpConn:Disconnect() State.infJumpConn = nil end State.infJumpConn = RunService.RenderStepped:Connect(function() if not State.infJumpActive or not holdingJump then return end local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root or hum.Health <= 0 then return end local state = hum:GetState() if state ~= Enum.HumanoidStateType.Jumping and state ~= Enum.HumanoidStateType.Freefall then hum:ChangeState(Enum.HumanoidStateType.Jumping) end local vel = root.AssemblyLinearVelocity if vel.Y < 35 then root.AssemblyLinearVelocity = Vector3.new(vel.X, 48, vel.Z) end end) end local function stopInfJump() if State.infJumpConn then State.infJumpConn:Disconnect() State.infJumpConn = nil end holdingJump = false end local function setInfJump(on) if State.infJumpActive == on then return end State.infJumpActive = on if on then startInfJump() else stopInfJump() end updateInfJumpVisuals() end local function toggleInfJump() setInfJump(not State.infJumpActive) end UIS.InputBegan:Connect(function(input, gpe) if gpe then return end if input.KeyCode == Enum.KeyCode.Space or input.KeyCode == Enum.KeyCode.ButtonA then if State.infJumpActive then holdingJump = true end end end) UIS.InputEnded:Connect(function(input) if input.KeyCode == Enum.KeyCode.Space or input.KeyCode == Enum.KeyCode.ButtonA then holdingJump = false end end) task.spawn(function() local pg = LocalPlayer:WaitForChild("PlayerGui", 15) if not pg then return end local function hook(btn) if btn:IsA("GuiButton") and btn.Name == "JumpButton" and not btn:GetAttribute("SHHooked") then btn:SetAttribute("SHHooked", true) btn.MouseButton1Down:Connect(function() if State.infJumpActive then holdingJump = true end end) btn.MouseButton1Up:Connect(function() holdingJump = false end) btn.MouseLeave:Connect(function() holdingJump = false end) end end for _, v in ipairs(pg:GetDescendants()) do hook(v) end pg.DescendantAdded:Connect(hook) end) UIS.JumpRequest:Connect(function() if State.infJumpActive then holdingJump = true end end) LocalPlayer.CharacterAdded:Connect(function() task.wait(0.6) if State.antiBatActive then stopAntiBat() task.wait(0.15) startAntiBat() end if State.infJumpActive then stopInfJump() startInfJump() end end) local FULL_HEIGHT = 204 local MINI_HEIGHT = 36 local gui = Instance.new("ScreenGui") gui.Name = "SpeedHub" gui.ResetOnSpawn = false pcall(function() gui.Parent = game:GetService("CoreGui") if syn and syn.protect_gui then syn.protect_gui(gui) end end) if not gui.Parent then gui.Parent = LocalPlayer:WaitForChild("PlayerGui") end local mainFrame = Instance.new("Frame", gui) mainFrame.Size = UDim2.new(0, 190, 0, FULL_HEIGHT) mainFrame.Position = UDim2.new(0.05, 0, 0.3, 0) mainFrame.BackgroundTransparency = 1 mainFrame.BorderSizePixel = 0 mainFrame.ClipsDescendants = true corner(mainFrame, 10) local bgImage = Instance.new("ImageLabel", mainFrame) bgImage.Size = UDim2.new(1, 0, 1, 0) bgImage.BackgroundColor3 = Color3.fromRGB(0, 0, 0) bgImage.Image = "rbxassetid://94187149763780" bgImage.ImageColor3 = Color3.fromRGB(255, 255, 255) bgImage.ScaleType = Enum.ScaleType.Crop bgImage.ZIndex = 1 corner(bgImage, 10) local bgOverlay = Instance.new("Frame", mainFrame) bgOverlay.Size = UDim2.new(1, 0, 1, 0) bgOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0) bgOverlay.BackgroundTransparency = 0.72 bgOverlay.BorderSizePixel = 0 bgOverlay.ZIndex = 2 corner(bgOverlay, 10) local border = Instance.new("UIStroke", mainFrame) border.Thickness = 1.5 border.Color = Color3.fromRGB(0, 0, 0) local title = Instance.new("TextLabel", mainFrame) title.Size = UDim2.new(1, -52, 0, 28) title.Position = UDim2.new(0, 8, 0, 4) title.BackgroundTransparency = 1 title.Text = "SPEED HUB" title.TextColor3 = Color3.fromRGB(255, 255, 255) title.Font = Enum.Font.GothamBlack title.TextSize = 11 title.TextXAlignment = Enum.TextXAlignment.Left title.ZIndex = 3 local minimizeBtn = Instance.new("TextButton", mainFrame) minimizeBtn.Size = UDim2.new(0, 18, 0, 18) minimizeBtn.Position = UDim2.new(1, -26, 0, 7) minimizeBtn.BackgroundColor3 = Color3.fromRGB(20, 20, 20) minimizeBtn.BackgroundTransparency = 0.3 minimizeBtn.BorderSizePixel = 0 minimizeBtn.Text = "–" minimizeBtn.TextColor3 = Color3.fromRGB(200, 200, 200) minimizeBtn.Font = Enum.Font.GothamBold minimizeBtn.TextSize = 12 minimizeBtn.ZIndex = 5 corner(minimizeBtn, 4) Instance.new("UIStroke", minimizeBtn).Color = Color3.fromRGB(60, 60, 60) local drag = {} mainFrame.InputBegan:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then drag.active = true drag.start = inp.Position drag.startPos = mainFrame.Position inp.Changed:Connect(function() if inp.UserInputState == Enum.UserInputState.End then drag.active = false end end) end end) mainFrame.InputChanged:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then drag.input = inp end end) UIS.InputChanged:Connect(function(inp) if inp == drag.input and drag.active then local d = inp.Position - drag.start mainFrame.Position = UDim2.new( drag.startPos.X.Scale, drag.startPos.X.Offset + d.X, drag.startPos.Y.Scale, drag.startPos.Y.Offset + d.Y ) end end) minimizeBtn.MouseButton1Click:Connect(function() State.minimized = not State.minimized minimizeBtn.Text = State.minimized and "+" or "–" TweenService:Create(mainFrame, TweenInfo.new(0.22, Enum.EasingStyle.Quad), { Size = UDim2.new(0, 190, 0, State.minimized and MINI_HEIGHT or FULL_HEIGHT) }):Play() end) local function makeKeyRow(parent, yPos, labelText, defaultKeyName) local row = Instance.new("Frame", parent) row.Size = UDim2.new(1, -16, 0, 76) row.Position = UDim2.new(0, 8, 0, yPos) row.BackgroundColor3 = Color3.fromRGB(0, 0, 0) row.BackgroundTransparency = 0.6 row.ZIndex = 3 corner(row, 6) local rowStroke = Instance.new("UIStroke", row) rowStroke.Color = Color3.fromRGB(60, 60, 60) rowStroke.Thickness = 1 local nameLabel = Instance.new("TextLabel", row) nameLabel.Size = UDim2.new(1, -50, 0, 14) nameLabel.Position = UDim2.new(0, 8, 0, 8) nameLabel.BackgroundTransparency = 1 nameLabel.Text = labelText nameLabel.TextColor3 = Color3.fromRGB(255, 255, 255) nameLabel.Font = Enum.Font.GothamBold nameLabel.TextSize = 11 nameLabel.TextXAlignment = Enum.TextXAlignment.Left nameLabel.ZIndex = 4 local statusLabel = Instance.new("TextLabel", row) statusLabel.Size = UDim2.new(1, -50, 0, 12) statusLabel.Position = UDim2.new(0, 8, 0, 24) statusLabel.BackgroundTransparency = 1 statusLabel.Text = "○ DISABLED" statusLabel.TextColor3 = Color3.fromRGB(255, 255, 255) statusLabel.Font = Enum.Font.GothamSemibold statusLabel.TextSize = 9 statusLabel.TextXAlignment = Enum.TextXAlignment.Left statusLabel.ZIndex = 4 local pill = Instance.new("Frame", row) pill.Size = UDim2.new(0, 32, 0, 18) pill.Position = UDim2.new(1, -38, 0, 10) pill.BackgroundColor3 = Color3.fromRGB(0, 0, 0) pill.BackgroundTransparency = 0.5 pill.ZIndex = 4 corner(pill, 9) Instance.new("UIStroke", pill).Color = Color3.fromRGB(60, 60, 60) local switchBall = Instance.new("Frame", pill) switchBall.Size = UDim2.new(0, 9, 0, 9) switchBall.Position = UDim2.new(0, 4, 0.5, -4.5) switchBall.BackgroundColor3 = Color3.fromRGB(180, 180, 180) switchBall.ZIndex = 5 corner(switchBall, 4.5) local chip = Instance.new("Frame", row) chip.Size = UDim2.new(0, 90, 0, 20) chip.Position = UDim2.new(0, 8, 0, 48) chip.BackgroundColor3 = Color3.fromRGB(0, 0, 0) chip.BackgroundTransparency = 0.5 chip.ZIndex = 4 corner(chip, 4) Instance.new("UIStroke", chip).Color = Color3.fromRGB(60, 60, 60) local fl = Instance.new("TextLabel", chip) fl.Size = UDim2.new(0, 26, 1, 0) fl.Position = UDim2.new(0, 5, 0, 0) fl.BackgroundTransparency = 1 fl.Text = "key" fl.TextColor3 = Color3.fromRGB(200, 200, 200) fl.Font = Enum.Font.GothamSemibold fl.TextSize = 7 fl.TextXAlignment = Enum.TextXAlignment.Left fl.ZIndex = 5 local keyBtn = Instance.new("TextButton", chip) keyBtn.Size = UDim2.new(0, 50, 1, -2) keyBtn.Position = UDim2.new(0, 32, 0, 1) keyBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 30) keyBtn.BackgroundTransparency = 0.3 keyBtn.BorderSizePixel = 0 keyBtn.Text = defaultKeyName keyBtn.TextColor3 = Color3.fromRGB(255, 255, 255) keyBtn.Font = Enum.Font.GothamBold keyBtn.TextSize = 8 keyBtn.ZIndex = 6 corner(keyBtn, 3) Instance.new("UIStroke", keyBtn).Color = Color3.fromRGB(60, 60, 60) local rowBtn = Instance.new("TextButton", row) rowBtn.Size = UDim2.new(1, 0, 1, 0) rowBtn.BackgroundTransparency = 1 rowBtn.Text = "" rowBtn.ZIndex = 3 return row, rowStroke, statusLabel, switchBall, keyBtn, rowBtn end local function listenKey(bindTbl, displayBtn) displayBtn.Text = "..." local conn conn = UIS.InputBegan:Connect(function(inp, gpe) if gpe then return end local isKB = inp.UserInputType == Enum.UserInputType.Keyboard local isGP = inp.UserInputType == Enum.UserInputType.Gamepad1 or inp.UserInputType == Enum.UserInputType.Gamepad2 or inp.UserInputType == Enum.UserInputType.Gamepad3 or inp.UserInputType == Enum.UserInputType.Gamepad4 if (isKB or isGP) and inp.KeyCode ~= Enum.KeyCode.Unknown then conn:Disconnect() bindTbl.value = inp.KeyCode displayBtn.Text = getKeyName(inp.KeyCode) saveConfig() end end) end local antiBatKeyBtn, abRowBtn antiBatRow, antiBatRowStroke, antiBatStatus, antiBatSwitchBall, antiBatKeyBtn, abRowBtn = makeKeyRow(mainFrame, 36, "Anti Bat", getKeyName(antiBatKey.value)) antiBatKeyBtn.MouseButton1Click:Connect(function() listenKey(antiBatKey, antiBatKeyBtn) end) abRowBtn.MouseButton1Click:Connect(toggleAntiBat) local infJumpKeyBtn, ijRowBtn infJumpRow, infJumpRowStroke, infJumpStatus, infJumpSwitchBall, infJumpKeyBtn, ijRowBtn = makeKeyRow(mainFrame, 120, "Inf Jump", getKeyName(infJumpKey.value)) infJumpKeyBtn.MouseButton1Click:Connect(function() listenKey(infJumpKey, infJumpKeyBtn) end) ijRowBtn.MouseButton1Click:Connect(toggleInfJump) UIS.InputBegan:Connect(function(inp, gpe) if gpe then return end if inp.KeyCode == antiBatKey.value then toggleAntiBat() elseif inp.KeyCode == infJumpKey.value then toggleInfJump() end end) print("Speed Hub v3 | CFrame Anti Bat + Inf Jump")