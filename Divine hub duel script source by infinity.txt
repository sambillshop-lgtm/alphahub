local _activeIntroSound = nil task.spawn(function() pcall(function() local _skipMusic = false pcall(function() local _s = readfile("SpeedHubConfig.json") or readfile("DIVINECCConfig.json") if _s and _s ~= "" then local _c = game:GetService("HttpService"):JSONDecode(_s) if _c and _c.introEnabled == false then _skipMusic = true end end end) if _skipMusic then return end local MUSIC_FILE = "DIVINECC_Intro_tktp6l" local MUSIC_URL = "https://files.catbox.moe/tktp6l.mp3" local MUSIC_START = 10 local MUSIC_END = 16 pcall(function() if isfile("SpeedHubIntro") then delfile("SpeedHubIntro") end if isfile("SpeedHubIntro_tktp6l") then delfile("SpeedHubIntro_tktp6l") end end) if not isfile(MUSIC_FILE) then local ok, data = pcall(game.HttpGet, game, MUSIC_URL) if not (ok and data and #data > 1000) then return end writefile(MUSIC_FILE, data) end local parent = (gethui and gethui()) or game:GetService("CoreGui") _activeIntroSound = Instance.new("Sound", parent) _activeIntroSound.Name = "DIVINECC_IntroMusic" _activeIntroSound.SoundId = getcustomasset(MUSIC_FILE) _activeIntroSound.PlaybackSpeed = 1 _activeIntroSound.Volume = 0.55 _activeIntroSound.Looped = false _activeIntroSound.TimePosition = MUSIC_START _activeIntroSound:Play() task.spawn(function() pcall(function() for _ = 1, 8 do if not _activeIntroSound then return end if _activeIntroSound.TimePosition < MUSIC_START - 0.15 then _activeIntroSound.TimePosition = MUSIC_START if not _activeIntroSound.IsPlaying then _activeIntroSound:Play() end end task.wait(0.05) end if not _activeIntroSound then return end task.wait(MUSIC_END - MUSIC_START) if _activeIntroSound then _activeIntroSound:Stop() _activeIntroSound:Destroy() _activeIntroSound = nil end end) end) end) end) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UIS = game:GetService("UserInputService") local TweenService = game:GetService("TweenService") local HttpService = game:GetService("HttpService") local ContentProvider = game:GetService("ContentProvider") local Lighting = game:GetService("Lighting") local StarterGui = game:GetService("StarterGui") local LP = Players.LocalPlayer local State = { normalSpeed = 60, carrySpeed = 30, laggerSpeed = 13, laggerCarrySpeed = 13, speedType = "normal", laggerActive = false, autoBatToggled = false, hittingCooldown = false, infJumpEnabled = false, infJumpMode = "hold", antiRagdollEnabled = false, antiRagdollMode = "splatter", fpsBoostEnabled = false, stretchRezEnabled = false, guiVisible = true, vonEnabled = true, vonRadius = 64, vonDelay = 1.30, vonStatus = "IDLE", stealMode = "Normal", stealRadii = { Normal = 62, Semi = 40 }, medusaLastUsed = 0, medusaDebounce = false, medusaCounterEnabled = false, autoMedusaResetEnabled = false, autoMedusaResetDebounce = false, dropBrainrotActive = false, autoLeftEnabled = false, autoRightEnabled = false, autoLeftPhase = 1, autoRightPhase = 1, _tpInProgress = false, lastMoveDir = Vector3.new(0, 0, 0), animEnabled = false, unwalkEnabled = false, savedToggles = {}, mobileVisible = true, mobileLocked = false, mobileDragEnabled = false, mbBtnPositions = {}, mbPanelPos = nil, originalC0 = nil, currentFOV = 70, darkModeEnabled = false, introEnabled = true, purpleSkyEnabled = false, autoTpDownEnabled = false, autoTpDownY = 20, batCounterEnabled = false, batCounterDebounce = false, aimbotSpeed = 58, antiBatActive = false, antiBatInfJumpEnabled = false, batV2Toggled = false, batV2HitCD = false, tpBatActive = false, bypassBatMode = "bypass", safeModeEnabled = false, safeModeActive = false, safeModeLastIsStealing = false, savedStealFeatures = nil, isGrabbing = false, _speedLastPosition = nil, _speedLastTime = nil, _speedOwnershipTimer = 0, _speedLagbackCooldown = 0, espSpeedEnabled = true, } local SWING_COOLDOWN = 0 local Keys = { autoBat = Enum.KeyCode.E, speed = Enum.KeyCode.Q, lagger = Enum.KeyCode.C, guiHide = Enum.KeyCode.LeftControl, autoLeft = Enum.KeyCode.L, autoRight = Enum.KeyCode.R, dropBrainrot = Enum.KeyCode.H, tpDown = Enum.KeyCode.T, instaReset = Enum.KeyCode.F, antiBat = Enum.KeyCode.G, batV2 = Enum.KeyCode.V, autoMedusaReset = Enum.KeyCode.M, } local C = { BG = Color3.fromRGB(15, 15, 20), PANEL = Color3.fromRGB(12, 12, 12), ROW = Color3.fromRGB(14, 14, 14), ROW_HOV = Color3.fromRGB(28, 28, 28), BORDER = Color3.fromRGB(255, 255, 255), BORDER2 = Color3.fromRGB(200, 200, 200), HEADER = Color3.fromRGB(15, 15, 18), ACCENT = Color3.fromRGB(255, 255, 255), ACCENT2 = Color3.fromRGB(200, 200, 200), DIM = Color3.fromRGB(180, 180, 185), TEXT = Color3.fromRGB(255, 255, 255), ON_BG = Color3.fromRGB(255, 255, 255), OFF_BG = Color3.fromRGB(25, 25, 25), KEY_BG = Color3.fromRGB(0, 0, 0), TAB_ACTIVE = Color3.fromRGB(18, 18, 22), TAB_INACTIVE = Color3.fromRGB(30, 30, 35), SIDENAV = Color3.fromRGB(12, 12, 16), MB_BG = Color3.fromRGB(8, 8, 8), MB_BTN = Color3.fromRGB(12, 12, 12), MB_STROKE = Color3.fromRGB(255, 255, 255), MB_TEXT = Color3.fromRGB(255, 255, 255), } local executeVonSteal, getNearestStealable, getRootPart, VonData local barProgress, barReset, NormalSteal, SemiSteal, autoGrabSync, autoGrabStop do VonData = { IsStealing = false, CurrentTarget = nil, PromptCache = {}, } getRootPart = function() local char = LP.Character return char and (char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso")) end local function isMyPlot(plot) if not plot then return false end local sign = plot:FindFirstChild("PlotSign") if sign then local yourBase = sign:FindFirstChild("YourBase") if yourBase and yourBase:IsA("BillboardGui") and yourBase.Enabled then return true end end return false end NormalSteal = { enabled = false, radius = 62, duration = 1.3, animals = {}, promptCache = {}, internalCache = {}, scannerStarted = false, isStealing = false, stealConn = nil, lastSteal = 0, cooldown = 0.08, } local function scanPlotsNormal() NormalSteal.animals = {} local plots = workspace:FindFirstChild("Plots") if not plots then return end for _, plot in ipairs(plots:GetChildren()) do if plot:IsA("Model") and not isMyPlot(plot) then local podiums = plot:FindFirstChild("AnimalPodiums") if podiums then for _, podium in ipairs(podiums:GetChildren()) do if podium:IsA("Model") then local base = podium:FindFirstChild("Base") local spawn = base and base:FindFirstChild("Spawn") if spawn then table.insert(NormalSteal.animals, { plot = plot.Name, slot = podium.Name, worldPosition = spawn.Position, uid = plot.Name .. "_" .. podium.Name, }) end end end end end end end local function ensureScannerNormal() if NormalSteal.scannerStarted then return end NormalSteal.scannerStarted = true task.spawn(function() task.wait(1) while true do if NormalSteal.enabled then pcall(scanPlotsNormal) end task.wait(3) end end) end local function findPromptNormal(data) if not data then return nil end local cached = NormalSteal.promptCache[data.uid] if cached and cached.Parent then return cached end local plots = workspace:FindFirstChild("Plots") local plot = plots and plots:FindFirstChild(data.plot) local podiums = plot and plot:FindFirstChild("AnimalPodiums") local podium = podiums and podiums:FindFirstChild(data.slot) local base = podium and podium:FindFirstChild("Base") local spawn = base and base:FindFirstChild("Spawn") local attach = spawn and spawn:FindFirstChild("PromptAttachment") if not attach then return nil end for _, prompt in ipairs(attach:GetChildren()) do if prompt:IsA("ProximityPrompt") then NormalSteal.promptCache[data.uid] = prompt return prompt end end return nil end local function cacheCallbacksNormal(prompt) if NormalSteal.internalCache[prompt] then return end local data = { hold = {}, trigger = {}, ready = true } pcall(function() if getconnections then for _, conn in ipairs(getconnections(prompt.PromptButtonHoldBegan)) do if type(conn.Function) == "function" then table.insert(data.hold, conn.Function) end end for _, conn in ipairs(getconnections(prompt.Triggered)) do if type(conn.Function) == "function" then table.insert(data.trigger, conn.Function) end end end end) if #data.hold > 0 or #data.trigger > 0 then NormalSteal.internalCache[prompt] = data end end local function doNormalSteal(prompt) if not prompt or not prompt.Parent or NormalSteal.isStealing then return end if tick() - (NormalSteal.lastSteal or 0) < (NormalSteal.cooldown or 0.08) then return end cacheCallbacksNormal(prompt) local data = NormalSteal.internalCache[prompt] if not data or not data.ready then return end data.ready = false NormalSteal.isStealing = true NormalSteal.lastSteal = tick() VonData.IsStealing = true task.spawn(function() if #data.hold > 0 then for _, fn in ipairs(data.hold) do task.spawn(function() pcall(fn) end) end end local startTime = tick() local dur = 1.3 while NormalSteal.enabled and State.stealMode == "Normal" and tick() - startTime < dur do if barProgress then barProgress((tick() - startTime) / dur) end task.wait(0.02) end if not NormalSteal.enabled or State.stealMode ~= "Normal" then data.ready = true NormalSteal.isStealing = false VonData.IsStealing = false if barReset then barReset() end return end if barProgress then barProgress(1) end if #data.trigger > 0 then for _, fn in ipairs(data.trigger) do task.spawn(function() pcall(fn) end) end end task.wait(0.12) data.ready = true NormalSteal.isStealing = false VonData.IsStealing = false if barReset then barReset() end end) end local function nearestAnimalNormal() local root = getRootPart() if not root then return nil end local best, bestDist = nil, math.huge local plots = workspace:FindFirstChild("Plots") for _, data in ipairs(NormalSteal.animals) do if data.worldPosition then local plotObj = plots and plots:FindFirstChild(data.plot) if not isMyPlot(plotObj) then local dist = (root.Position - data.worldPosition).Magnitude if dist < bestDist then best = data bestDist = dist end end end end if best and bestDist <= (tonumber(NormalSteal.radius) or 62) then return best end return nil end local function startNormalSteal() NormalSteal.radius = (State.stealRadii and State.stealRadii.Normal) or 62 NormalSteal.enabled = true ensureScannerNormal() pcall(scanPlotsNormal) if NormalSteal.stealConn then NormalSteal.stealConn:Disconnect() NormalSteal.stealConn = nil end NormalSteal.stealConn = RunService.Heartbeat:Connect(function() if not NormalSteal.enabled then return end if State.stealMode ~= "Normal" then NormalSteal.enabled = false if NormalSteal.stealConn then NormalSteal.stealConn:Disconnect() NormalSteal.stealConn = nil end return end if NormalSteal.isStealing then return end local target = nearestAnimalNormal() if not target then return end local prompt = findPromptNormal(target) if prompt then doNormalSteal(prompt) end end) end local function stopNormalSteal() NormalSteal.enabled = false NormalSteal.isStealing = false VonData.IsStealing = false if NormalSteal.stealConn then NormalSteal.stealConn:Disconnect() NormalSteal.stealConn = nil end if barReset then barReset() end end SemiSteal = { enabled = false, radius = 9, primeRange = 80, entryDelay = 0.3, cooldown = 0.05, conn = nil, scanThread = nil, plotSync = { caches = {}, connections = {} }, animals = {}, promptCache = {}, internalCache = {}, state = { active = false, startTime = 0, phase = "idle" }, } local function ensureSyncSemi() if SemiSteal.syncReady then return true end local ok = pcall(function() local rs = game:GetService("ReplicatedStorage") SemiSteal.packages = rs:WaitForChild("Packages", 10) SemiSteal.datas = rs:WaitForChild("Datas", 10) SemiSteal.plots = workspace:WaitForChild("Plots", 10) if not (SemiSteal.packages and SemiSteal.datas and SemiSteal.plots) then return end SemiSteal.animalsData = require(SemiSteal.datas:WaitForChild("Animals", 10)) local sync = SemiSteal.packages:WaitForChild("Synchronizer", 10) SemiSteal.channelFolder = sync:WaitForChild("Channel", 10) SemiSteal.routeRemote = sync:WaitForChild("CommunicationRoute", 10) SemiSteal.requestData = sync:FindFirstChild("RequestData") local function splitPath(path) if typeof(path) == "table" then return path end local out = {} for part in string.gmatch(tostring(path), "[^%.]+") do table.insert(out, tonumber(part) or part) end return out end local function resolvePath(path, root2) local cur, par, k = root2, nil, nil for _, part in ipairs(splitPath(path)) do par = cur k = part cur = cur and cur[part] or nil end return cur, par, k end local function applySyncDiff(channelName, packet) local cache = SemiSteal.plotSync.caches[channelName] if typeof(cache) ~= "table" then return end local path, action, a, b = packet[1], packet[2], packet[3], packet[4] local cur, par, k = resolvePath(path, cache) if action == "Changed" then if par ~= nil then par[k] = a end elseif action == "ArrayInsert" then if cur ~= nil then table.insert(cur, b, a) end elseif action == "ArrayRemoved" then if cur ~= nil then table.remove(cur, b) end elseif action == "DictionaryInsert" then if cur ~= nil then cur[b] = a end elseif action == "DictionaryRemoved" then if cur ~= nil then cur[b] = nil end end end local function attachPlotChannelSemi(remote, pl, rd) if SemiSteal.plotSync.connections[remote] then return end local ch = tostring(remote.Name) if not pl:FindFirstChild(ch) then return end if rd and SemiSteal.plotSync.caches[ch] == nil then local ok2, d2 = pcall(function() return rd:InvokeServer(ch) end) SemiSteal.plotSync.caches[ch] = (ok2 and typeof(d2) == "table") and d2 or {} elseif SemiSteal.plotSync.caches[ch] == nil then SemiSteal.plotSync.caches[ch] = {} end SemiSteal.plotSync.connections[remote] = remote.OnClientEvent:Connect(function(queue) for _, pkt in ipairs(queue) do applySyncDiff(ch, pkt) end end) end for _, child in ipairs(SemiSteal.channelFolder:GetChildren()) do if child:IsA("RemoteEvent") then attachPlotChannelSemi(child, SemiSteal.plots, SemiSteal.requestData) end end SemiSteal.channelFolder.ChildAdded:Connect(function(child) if child:IsA("RemoteEvent") then attachPlotChannelSemi(child, SemiSteal.plots, SemiSteal.requestData) end end) SemiSteal.routeRemote.OnClientEvent:Connect(function(actions) for _, act in ipairs(actions) do local kind, ch = act[1], tostring(act[2]) if SemiSteal.plots and SemiSteal.plots:FindFirstChild(ch) then if kind == "ListenerAdded" then local rem = SemiSteal.channelFolder and SemiSteal.channelFolder:FindFirstChild(ch) if rem and rem:IsA("RemoteEvent") then attachPlotChannelSemi(rem, SemiSteal.plots, SemiSteal.requestData) end elseif kind == "ListenerRemoved" then for rem, conn in pairs(SemiSteal.plotSync.connections) do if tostring(rem.Name) == ch then pcall(function() conn:Disconnect() end) SemiSteal.plotSync.connections[rem] = nil SemiSteal.plotSync.caches[ch] = nil break end end end end end end) SemiSteal.syncReady = true end) return ok and SemiSteal.syncReady == true end local function scanAllPlotsSemi() if not ensureSyncSemi() then return end local newCache = {} for _, plot in ipairs(SemiSteal.plots:GetChildren()) do local cache = SemiSteal.plotSync.caches[plot.Name] local animalList = cache and cache.AnimalList if typeof(animalList) == "table" then for slot, animalData in pairs(animalList) do if type(animalData) == "table" then local animalName = animalData.Index local info = SemiSteal.animalsData and SemiSteal.animalsData[animalName] if info then table.insert(newCache, { name = info.DisplayName or animalName, plot = plot.Name, slot = tostring(slot), uid = plot.Name .. "_" .. tostring(slot), }) end end end end end SemiSteal.animals = newCache end local function getPlotOwnerSemi(plot) local sign = plot and plot:FindFirstChild("PlotSign") local frame = sign and sign:FindFirstChild("SurfaceGui") and sign.SurfaceGui:FindFirstChild("Frame") local label = frame and frame:FindFirstChild("TextLabel") if not label or label.Text == "Empty Base" then return nil end return label.Text:gsub("'s [Bb]ase$", ""):gsub("%s+$", "") end local function isMyBaseAnimalSemi(animalData) if not animalData or not animalData.plot or not SemiSteal.plots then return false end local plot = SemiSteal.plots:FindFirstChild(animalData.plot) if not plot then return false end local owner = getPlotOwnerSemi(plot) return owner == LP.DisplayName or owner == LP.Name end local function podiumForSemi(animalData) local plot = SemiSteal.plots and SemiSteal.plots:FindFirstChild(animalData.plot) local podiums = plot and plot:FindFirstChild("AnimalPodiums") return podiums and podiums:FindFirstChild(animalData.slot) or nil end local function animalPosSemi(animalData) local podium = podiumForSemi(animalData) return podium and podium:GetPivot().Position or nil end local function distToAnimalSemi(animalData) local root = getRootPart() local pos = animalPosSemi(animalData) return root and pos and (root.Position - pos).Magnitude or math.huge end local function findPromptForAnimalSemi(animalData) if not animalData then return nil end local cached = SemiSteal.promptCache[animalData.uid] if cached and cached.Parent then return cached end local podium = podiumForSemi(animalData) local base = podium and podium:FindFirstChild("Base") local spawn = base and base:FindFirstChild("Spawn") local attach = spawn and spawn:FindFirstChild("PromptAttachment") if not attach then return nil end for _, prompt in ipairs(attach:GetChildren()) do if prompt:IsA("ProximityPrompt") then SemiSteal.promptCache[animalData.uid] = prompt return prompt end end return nil end local function pickClosestSemi() local root = getRootPart() if not root then return nil end local best, bestDist = nil, math.huge for _, animalData in ipairs(SemiSteal.animals) do if not isMyBaseAnimalSemi(animalData) then local pos = animalPosSemi(animalData) local dist = pos and (root.Position - pos).Magnitude or math.huge if dist <= (SemiSteal.radius or 9) and dist < bestDist then best, bestDist = animalData, dist end end end return best end local function buildCallbacksSemi(prompt) if SemiSteal.internalCache[prompt] then return end local data = { holdCallbacks = {}, triggerCallbacks = {}, ready = true } local okH, holds = pcall(getconnections, prompt.PromptButtonHoldBegan) if okH and type(holds) == "table" then for _, conn in ipairs(holds) do if type(conn.Function) == "function" then table.insert(data.holdCallbacks, conn.Function) end end end local okT, triggers = pcall(getconnections, prompt.Triggered) if okT and type(triggers) == "table" then for _, conn in ipairs(triggers) do if type(conn.Function) == "function" then table.insert(data.triggerCallbacks, conn.Function) end end end if #data.holdCallbacks > 0 or #data.triggerCallbacks > 0 then SemiSteal.internalCache[prompt] = data end end local function executeSemiSteal(prompt, animalData) if not prompt or not prompt.Parent or not animalData then return false end buildCallbacksSemi(prompt) local data = SemiSteal.internalCache[prompt] if not data or not data.ready then return false end data.ready = false SemiSteal.state.active = true SemiSteal.state.startTime = tick() SemiSteal.state.phase = "holding" VonData.IsStealing = true task.spawn(function() local startTime = SemiSteal.state.startTime local holdMin = 1.3 local holdMax = 2.6 for _, fn in ipairs(data.holdCallbacks) do task.spawn(function() pcall(fn) end) end while SemiSteal.enabled and State.stealMode == "Semi" and tick() - startTime < holdMin do if barProgress then barProgress((tick() - startTime) / holdMax) end task.wait() end SemiSteal.state.phase = "waitingRange" local alreadyInRange = distToAnimalSemi(animalData) <= (tonumber(SemiSteal.radius) or 9) local fired = false while SemiSteal.enabled and State.stealMode == "Semi" and prompt.Parent do local elapsed = tick() - startTime if elapsed > holdMax then break end if barProgress then barProgress(elapsed / holdMax) end if distToAnimalSemi(animalData) <= (tonumber(SemiSteal.radius) or 9) then if not alreadyInRange then task.wait(SemiSteal.entryDelay or 0.3) end if SemiSteal.enabled and State.stealMode == "Semi" then for _, fn in ipairs(data.triggerCallbacks) do task.spawn(function() pcall(fn) end) end fired = true end break end task.wait() end SemiSteal.state.active = false SemiSteal.state.phase = "idle" VonData.IsStealing = false if fired and barProgress then barProgress(1) end task.wait(SemiSteal.cooldown or 0.05) data.ready = true if barReset then barReset() end end) return true end local function ensureScanThreadSemi() if SemiSteal.scanThread then return end SemiSteal.scanThread = task.spawn(function() while true do if SemiSteal.enabled or State.stealMode == "Semi" then pcall(scanAllPlotsSemi) end task.wait(5) end end) end local function startSemiSteal() SemiSteal.radius = (State.stealRadii and State.stealRadii.Semi) or 9 SemiSteal.enabled = true task.spawn(function() pcall(ensureSyncSemi) end) ensureScanThreadSemi() pcall(scanAllPlotsSemi) if SemiSteal.conn then SemiSteal.conn:Disconnect() SemiSteal.conn = nil end SemiSteal.conn = RunService.Heartbeat:Connect(function() if not SemiSteal.enabled then return end if State.stealMode ~= "Semi" then SemiSteal.enabled = false if SemiSteal.conn then SemiSteal.conn:Disconnect() SemiSteal.conn = nil end return end if SemiSteal.state.active then return end local target = pickClosestSemi() if not target then return end local prompt = findPromptForAnimalSemi(target) if prompt then executeSemiSteal(prompt, target) end end) end local function stopSemiSteal() SemiSteal.enabled = false if SemiSteal.conn then SemiSteal.conn:Disconnect() SemiSteal.conn = nil end SemiSteal.state.active = false SemiSteal.state.phase = "idle" VonData.IsStealing = false if barReset then barReset() end end autoGrabSync = function() if not State.vonEnabled then stopNormalSteal() stopSemiSteal() return end if State.stealMode == "Normal" then stopSemiSteal() startNormalSteal() elseif State.stealMode == "Semi" then stopNormalSteal() startSemiSteal() end end autoGrabStop = function() stopNormalSteal() stopSemiSteal() end getNearestStealable = function() local root = getRootPart() if not root then return nil end local radius = (State.stealMode == "Semi") and ((State.stealRadii and State.stealRadii.Semi) or 9) or ((State.stealRadii and State.stealRadii.Normal) or 62) local plots = workspace:FindFirstChild("Plots") if not plots then return nil end local nearestPrompt, minDist = nil, radius for _, plot in ipairs(plots:GetChildren()) do if not isMyPlot(plot) then local podiums = plot:FindFirstChild("AnimalPodiums") if podiums then for _, podium in ipairs(podiums:GetChildren()) do local base = podium:FindFirstChild("Base") local spawnPoint = base and base:FindFirstChild("Spawn") local attachment = spawnPoint and spawnPoint:FindFirstChild("PromptAttachment") if attachment then for _, child in ipairs(attachment:GetChildren()) do if child:IsA("ProximityPrompt") then pcall(function() child.MaxActivationDistance = radius end) local dist = (root.Position - child.Parent.WorldPosition).Magnitude if dist < minDist then minDist = dist nearestPrompt = child end end end end end end end end return nearestPrompt end executeVonSteal = function() end end formatSpeedNum = function(n) n = tonumber(n) if not n then return "0" end local rounded = math.floor(n * 10 + 0.5) / 10 if math.abs(rounded - math.floor(rounded + 0.5)) < 1e-9 then return tostring(math.floor(rounded + 0.5)) end return string.format("%.1f", rounded) end local espEnabled, BoxedESPOptions, startPlayerESP, stopPlayerESP, refreshBoxedESP do espEnabled = false BoxedESPOptions = { box = false, tracer = false } local PlayerESP = { enabled = false, playerData = {}, conns = {} } local BoxedESPData = {} local BoxedESPConn = nil local function _espColor() return Color3.fromRGB(230, 230, 230) end local function _safeDrawing(kind, props) if not Drawing or not Drawing.new then return nil end local ok, obj = pcall(function() return Drawing.new(kind) end) if not ok or not obj then return nil end for k, v in pairs(props or {}) do pcall(function() obj[k] = v end) end return obj end local function _cleanupESPPlayer(player) local data = BoxedESPData[player] if not data then return end for _, obj in pairs(data) do pcall(function() obj.Visible = false if obj.Remove then obj:Remove() end end) end BoxedESPData[player] = nil end local function _cleanupBoxedESP() for player, _ in pairs(BoxedESPData) do _cleanupESPPlayer(player) end end local function _updateBoxedESP() local cam = workspace.CurrentCamera if not cam then return end local anyOn = BoxedESPOptions.box or BoxedESPOptions.tracer if not anyOn then _cleanupBoxedESP() return end for _, player in ipairs(Players:GetPlayers()) do if player == LP then continue end local char = player.Character local root = char and char:FindFirstChild("HumanoidRootPart") local head = char and char:FindFirstChild("Head") if not root or not head then _cleanupESPPlayer(player) continue end local rootPos, onScreen = cam:WorldToViewportPoint(root.Position) local headPos = cam:WorldToViewportPoint(head.Position + Vector3.new(0, 0.55, 0)) local data = BoxedESPData[player] if not data then data = { box = _safeDrawing( "Square", { Thickness = 2, Filled = false, Transparency = 1, Color = _espColor() } ), tracer = _safeDrawing("Line", { Thickness = 2, Transparency = 1, Color = _espColor() }), } BoxedESPData[player] = data end local color = _espColor() local height = math.abs(headPos.Y - rootPos.Y) * 2.15 if height < 20 or height ~= height then height = 65 end local width = height / 2.15 local view = cam.ViewportSize local centerX, centerY = view.X / 2, view.Y / 2 local targetX, targetY = rootPos.X, rootPos.Y + height / 2 local targetVisible = onScreen and rootPos.Z > 0 if not targetVisible then local dx = rootPos.X - centerX local dy = rootPos.Y - centerY if rootPos.Z <= 0 then dx = -dx dy = -dy end if math.abs(dx) < 1 and math.abs(dy) < 1 then local rel = cam.CFrame:PointToObjectSpace(root.Position) dx = rel.X dy = -rel.Y if rootPos.Z <= 0 then dx = -dx dy = -dy end end local edgePad = 10 local scaleX = (dx ~= 0) and ((view.X / 2 - edgePad) / math.abs(dx)) or math.huge local scaleY = (dy ~= 0) and ((view.Y / 2 - edgePad) / math.abs(dy)) or math.huge local scale = math.min(scaleX, scaleY) if scale == math.huge or scale ~= scale then scale = 1 end targetX = math.clamp(centerX + dx * scale, edgePad, view.X - edgePad) targetY = math.clamp(centerY + dy * scale, edgePad, view.Y - edgePad) end if data.box then data.box.Color = color data.box.Size = Vector2.new(width, height) data.box.Position = Vector2.new(rootPos.X - width / 2, rootPos.Y - height / 2) data.box.Visible = BoxedESPOptions.box == true and targetVisible end if data.tracer then data.tracer.Color = color local lChar = LP.Character local lRoot = lChar and lChar:FindFirstChild("HumanoidRootPart") local fromX, fromY if lRoot then local ls = cam:WorldToViewportPoint(lRoot.Position) fromX = ls.X fromY = ls.Y + 15 end if not fromX or not fromY then fromX = cam.ViewportSize.X / 2 fromY = cam.ViewportSize.Y - 88 end data.tracer.From = Vector2.new(fromX, fromY) data.tracer.To = Vector2.new(targetX, targetY) data.tracer.Visible = BoxedESPOptions.tracer == true end end end refreshBoxedESP = function() local anyOn = BoxedESPOptions.box or BoxedESPOptions.tracer if anyOn and not BoxedESPConn then BoxedESPConn = RunService.RenderStepped:Connect(_updateBoxedESP) elseif (not anyOn) and BoxedESPConn then BoxedESPConn:Disconnect() BoxedESPConn = nil _cleanupBoxedESP() end end startPlayerESP = function() if PlayerESP.enabled then return end PlayerESP.enabled = true local function cleanup(plr) local d = PlayerESP.playerData[plr] if not d then return end pcall(function() if d.highlight then d.highlight:Destroy() end end) pcall(function() if d.billboard then d.billboard:Destroy() end end) if d.conns then for _, c in ipairs(d.conns) do pcall(function() c:Disconnect() end) end end PlayerESP.playerData[plr] = nil end local function setup(plr, char) if not PlayerESP.enabled or plr == LP then return end cleanup(plr) local hrp = char and (char:FindFirstChild("HumanoidRootPart") or char:WaitForChild("HumanoidRootPart", 5)) local head = char and (char:FindFirstChild("Head") or char:WaitForChild("Head", 5)) if not hrp or not head then return end local hl = Instance.new("Highlight") hl.Name = "SpeedHubESP" hl.Adornee = char hl.FillColor = Color3.fromRGB(35, 35, 35) hl.FillTransparency = 0.72 hl.OutlineColor = Color3.fromRGB(245, 245, 245) hl.OutlineTransparency = 0 hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop hl.Parent = char PlayerESP.playerData[plr] = { highlight = hl, conns = {} } end for _, plr in ipairs(Players:GetPlayers()) do if plr ~= LP then if plr.Character then setup(plr, plr.Character) end table.insert( PlayerESP.conns, plr.CharacterAdded:Connect(function(c) task.defer(setup, plr, c) end) ) end end table.insert( PlayerESP.conns, Players.PlayerAdded:Connect(function(plr) if plr ~= LP then table.insert( PlayerESP.conns, plr.CharacterAdded:Connect(function(c) task.defer(setup, plr, c) end) ) end end) ) table.insert(PlayerESP.conns, Players.PlayerRemoving:Connect(cleanup)) end stopPlayerESP = function() PlayerESP.enabled = false for _, c in ipairs(PlayerESP.conns or {}) do pcall(function() c:Disconnect() end) end PlayerESP.conns = {} for _, d in pairs(PlayerESP.playerData or {}) do pcall(function() if d.highlight then d.highlight:Destroy() end end) end PlayerESP.playerData = {} end Players.PlayerRemoving:Connect(function(plr) _cleanupESPPlayer(plr) end) end do local Anims = { idle1 = "rbxassetid://133806214992291", idle2 = "rbxassetid://94970088341563", walk = "rbxassetid://707897309", run = "rbxassetid://707861613", jump = "rbxassetid://116936326516985", fall = "rbxassetid://116936326516985", climb = "rbxassetid://116936326516985", swim = "rbxassetid://116936326516985", swimidle = "rbxassetid://116936326516985", } task.spawn(function() pcall(function() ContentProvider:PreloadAsync({ Anims.idle1, Anims.idle2, Anims.walk, Anims.run, Anims.jump, Anims.fall, Anims.climb, Anims.swim, Anims.swimidle, }) end) end) local animHeartbeatConn = nil local savedAnimate = nil local originalAnims = nil local function isPackAnim(id) if not id then return false end for _, v in pairs(Anims) do if v == id then return true end end return false end saveOriginalAnims = function(char) local animate = char:FindFirstChild("Animate") if not animate then return end local function g(obj) return obj and obj.AnimationId or nil end local ids = { idle1 = g(animate.idle and animate.idle.Animation1), idle2 = g(animate.idle and animate.idle.Animation2), walk = g(animate.walk and animate.walk.WalkAnim), run = g(animate.run and animate.run.RunAnim), jump = g(animate.jump and animate.jump.JumpAnim), fall = g(animate.fall and animate.fall.FallAnim), climb = g(animate.climb and animate.climb.ClimbAnim), swim = g(animate.swim and animate.swim.Swim), swimidle = g(animate.swimidle and animate.swimidle.SwimIdle), } if not isPackAnim(ids.walk) then originalAnims = ids end end applyAnimPack = function(char) local animate = char:FindFirstChild("Animate") if not animate then return end local function s(obj, id) if obj then obj.AnimationId = id end end s(animate.idle and animate.idle.Animation1, Anims.idle1) s(animate.idle and animate.idle.Animation2, Anims.idle2) s(animate.walk and animate.walk.WalkAnim, Anims.walk) s(animate.run and animate.run.RunAnim, Anims.run) s(animate.jump and animate.jump.JumpAnim, Anims.jump) s(animate.fall and animate.fall.FallAnim, Anims.fall) s(animate.climb and animate.climb.ClimbAnim, Anims.climb) s(animate.swim and animate.swim.Swim, Anims.swim) s(animate.swimidle and animate.swimidle.SwimIdle, Anims.swimidle) end restoreOriginalAnims = function(char) if not originalAnims then return end local animate = char:FindFirstChild("Animate") if not animate then return end local function s(obj, id) if obj and id then obj.AnimationId = id end end s(animate.idle and animate.idle.Animation1, originalAnims.idle1) s(animate.idle and animate.idle.Animation2, originalAnims.idle2) s(animate.walk and animate.walk.WalkAnim, originalAnims.walk) s(animate.run and animate.run.RunAnim, originalAnims.run) s(animate.jump and animate.jump.JumpAnim, originalAnims.jump) s(animate.fall and animate.fall.FallAnim, originalAnims.fall) s(animate.climb and animate.climb.ClimbAnim, originalAnims.climb) s(animate.swim and animate.swim.Swim, originalAnims.swim) s(animate.swimidle and animate.swimidle.SwimIdle, originalAnims.swimidle) local hum2 = char:FindFirstChildOfClass("Humanoid") if hum2 then for _, track in ipairs(hum2:GetPlayingAnimationTracks()) do track:Stop(0) end hum2:ChangeState(Enum.HumanoidStateType.Running) end end startAnimToggle = function() if animHeartbeatConn then animHeartbeatConn:Disconnect() animHeartbeatConn = nil end local char = LP.Character if char then saveOriginalAnims(char) applyAnimPack(char) local hum2 = char:FindFirstChildOfClass("Humanoid") if hum2 then for _, track in ipairs(hum2:GetPlayingAnimationTracks()) do track:Stop(0) end hum2:ChangeState(Enum.HumanoidStateType.Running) end end animHeartbeatConn = RunService.Heartbeat:Connect(function() if not State.animEnabled then return end local c = LP.Character if c then applyAnimPack(c) end end) end stopAnimToggle = function() if animHeartbeatConn then animHeartbeatConn:Disconnect() animHeartbeatConn = nil end local char = LP.Character if char then restoreOriginalAnims(char) end end startUnwalk = function() if State.unwalkEnabled then return end State.unwalkEnabled = true local c = LP.Character if not c then return end local hum = c:FindFirstChildOfClass("Humanoid") if hum then for _, t in ipairs(hum:GetPlayingAnimationTracks()) do t:Stop() end end local anim = c:FindFirstChild("Animate") if anim then savedAnimate = anim:Clone() anim:Destroy() end end stopUnwalk = function() if not State.unwalkEnabled then return end State.unwalkEnabled = false local c = LP.Character if c and savedAnimate then savedAnimate.Parent = c savedAnimate.Disabled = false savedAnimate = nil end task.spawn(function() task.wait(0.15) local char = LP.Character if not char then return end if State.animEnabled then saveOriginalAnims(char) applyAnimPack(char) else restoreOriginalAnims(char) end end) end end local MOVE_KEYS = { [Enum.KeyCode.W] = true, [Enum.KeyCode.A] = true, [Enum.KeyCode.S] = true, [Enum.KeyCode.D] = true, [Enum.KeyCode.Up] = true, [Enum.KeyCode.Left] = true, [Enum.KeyCode.Down] = true, [Enum.KeyCode.Right] = true, } local DROP_ASCEND_DURATION = 0.2 local DROP_ASCEND_SPEED = 150 local POS = { L1 = Vector3.new(-476.48, -6.28, 92.73), L2 = Vector3.new(-483.12, -4.95, 94.80), R1 = Vector3.new(-476.16, -6.52, 25.62), R2 = Vector3.new(-483.04, -5.09, 23.14), } local isLoadingConfig = false local Conns = { antiRag = nil, autoLeft = nil, autoRight = nil, anchor = {}, aimbot = nil, batCounter = nil, antiBat = nil, infJumpLoop = nil, batV2 = nil, tpBat = nil, autoMedusaReset = nil, autoMedusaResetDesc = nil, } local h, hrp, speedLbl local setAutoLeft, setAutoRight local setInstaGrab, setAutoBat, setInfJump, setAntiRag, setFps, setMedusaCounter local setAnimToggle, setUnwalkToggle, setDarkMode, setPurpleSky local setIntroToggle local setupMedusaCounter, stopMedusaCounter, startAntiRagdoll, stopAntiRagdoll, applyFPSBoost local startAutoMedusaReset, stopAutoMedusaReset local startAutoLeft, stopAutoLeft, startAutoRight, stopAutoRight local startAntiBat, stopAntiBat local setBypassBatUI, bypassBatKeyBtn, setBypassBatMode local setBodyLockUI, bodyLockSetVisual local setAutoMedusaResetUI, autoMedusaResetKeyBtn local setSafeMode local applySafeModeRestore local safeModeBlockOn local saveOriginalAnims, applyAnimPack, restoreOriginalAnims local applyDarkMode, applyPurpleSky local tpToGround, runDropBrainrot, getCurrentSpeed, getAutoMoveSpeed local startBatAimbot, stopBatAimbot, startBatCounter, stopBatCounter, startBatV2Aimbot, stopBatV2Aimbot local startTPBat, stopTPBat local startBodyLock, stopBodyLock local modeValLbl, normalBox, carryBox, carryLaggerBox local autoBatKeyBtn, speedKeyBtn, laggerKeyBtn local autoLeftKeyBtn, autoRightKeyBtn local guiHideKeyBtn local dropBrainrotKeyBtn, tpDownKeyBtn local setSpeedToggleUI, setLaggerToggleUI local progressRadLbl local progressFill, progressPct barProgress = function(p) if progressFill then progressFill.Size = UDim2.new(math.clamp(tonumber(p) or 0, 0, 1), 0, 1, 0) end end barReset = function() if progressFill then progressFill.Size = UDim2.new(0, 0, 1, 0) end end local setAutoTpDown, autoTpDownYBox local instaResetKeyBtn local aimbotSpeedBox local bodyLockEnabled = false local bodyLockRange = 50 local _bodyLockConn = nil local _bodyLockSuppressed = false local _isResetting = false local function instantReset() if _isResetting then return end local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end _isResetting = true pcall(function() hum.Health = 0 end) task.delay(0.8, function() _isResetting = false end) local conn conn = LP.CharacterAdded:Connect(function() _isResetting = false if conn then conn:Disconnect() end end) end do local darkModeScreenGui = nil local purpleSkyObj = nil local purpleCcObj = nil local OriginalLighting = { ClockTime = Lighting.ClockTime, Brightness = Lighting.Brightness, Ambient = Lighting.Ambient, FogColor = Lighting.FogColor, } applyDarkMode = function() if State.darkModeEnabled then if not darkModeScreenGui then darkModeScreenGui = Instance.new("ScreenGui") darkModeScreenGui.Name = "DIVINE.CC_DarkMode" darkModeScreenGui.Parent = LP:WaitForChild("PlayerGui") darkModeScreenGui.ResetOnSpawn = false darkModeScreenGui.IgnoreGuiInset = true darkModeScreenGui.DisplayOrder = -100 local frame = Instance.new("Frame") frame.Size = UDim2.new(1, 0, 1, 0) frame.BackgroundColor3 = Color3.new(0, 0, 0) frame.BackgroundTransparency = 0.7 frame.BorderSizePixel = 0 frame.Parent = darkModeScreenGui end if darkModeScreenGui then darkModeScreenGui.Enabled = true end Lighting.ClockTime = 0 Lighting.Brightness = 0.2 Lighting.Ambient = Color3.fromRGB(20, 20, 35) else if darkModeScreenGui then darkModeScreenGui.Enabled = false end Lighting.ClockTime = OriginalLighting.ClockTime Lighting.Brightness = OriginalLighting.Brightness Lighting.Ambient = OriginalLighting.Ambient end end applyPurpleSky = function() if State.purpleSkyEnabled then Lighting.Ambient = Color3.fromRGB(60, 30, 90) Lighting.FogColor = Color3.fromRGB(80, 40, 120) purpleSkyObj = Lighting:FindFirstChildOfClass("Sky") or Instance.new("Sky") purpleSkyObj.SkyboxBk = "rbxassetid://1534951537" purpleSkyObj.SkyboxDn = "rbxassetid://1534951537" purpleSkyObj.SkyboxFt = "rbxassetid://1534951537" purpleSkyObj.SkyboxLf = "rbxassetid://1534951537" purpleSkyObj.SkyboxRt = "rbxassetid://1534951537" purpleSkyObj.SkyboxUp = "rbxassetid://1534951537" purpleSkyObj.StarCount = 8000 purpleSkyObj.Parent = Lighting purpleCcObj = Lighting:FindFirstChildOfClass("ColorCorrectionEffect") or Instance.new("ColorCorrectionEffect") purpleCcObj.TintColor = Color3.fromRGB(180, 140, 255) purpleCcObj.Saturation = 0.6 purpleCcObj.Contrast = 0.15 purpleCcObj.Parent = Lighting else Lighting.Ambient = OriginalLighting.Ambient Lighting.FogColor = OriginalLighting.FogColor if purpleSkyObj then purpleSkyObj:Destroy() purpleSkyObj = nil end if purpleCcObj then purpleCcObj:Destroy() purpleCcObj = nil end end end end local stretchRezConn = nil local function enableStretchRez() State.stretchRezEnabled = true local cam = workspace.CurrentCamera if not cam then return end if stretchRezConn then stretchRezConn:Disconnect() end stretchRezConn = RunService.RenderStepped:Connect(function() if not State.stretchRezEnabled then stretchRezConn:Disconnect() stretchRezConn = nil return end if cam then cam.CFrame = cam.CFrame * CFrame.new(0, 0, 0, 1, 0, 0, 0, 0.7, 0, 0, 0, 1) end end) end local function disableStretchRez() State.stretchRezEnabled = false if stretchRezConn then stretchRezConn:Disconnect() stretchRezConn = nil end end local function addSpeedShimmer(lbl, color1, color2) local gr = Instance.new("UIGradient", lbl) gr.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, color1 or Color3.fromRGB(160, 80, 255)), ColorSequenceKeypoint.new(0.5, color2 or Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(1, color1 or Color3.fromRGB(160, 80, 255)), }) gr.Transparency = NumberSequence.new({ NumberSequenceKeypoint.new(0, 0.3, 0), NumberSequenceKeypoint.new(0.5, 0, 0), NumberSequenceKeypoint.new(1, 0.3, 0), }) return gr end local function setupOtherPlayerEsp(player) if player == LP then return end local function addBillboard(char) task.wait(0.2) local head = char:FindFirstChild("Head") local hrp2 = char:FindFirstChild("HumanoidRootPart") if not head or not hrp2 then return end local oldBB = head:FindFirstChild("SpeedEspBillboard") if oldBB then oldBB:Destroy() end local bb = Instance.new("BillboardGui", head) bb.Name = "SpeedEspBillboard" bb.Size = UDim2.new(0, 100, 0, 30) bb.StudsOffset = Vector3.new(0, 3, 0) bb.AlwaysOnTop = true local lbl = Instance.new("TextLabel", bb) lbl.Size = UDim2.new(1, 0, 1, 0) lbl.BackgroundTransparency = 1 lbl.Text = "0.0" lbl.TextColor3 = Color3.fromRGB(255, 255, 255) lbl.Font = Enum.Font.GothamBlack lbl.TextScaled = true lbl.TextStrokeTransparency = 0 lbl.TextStrokeColor3 = Color3.new(0, 0, 0) local shimmerGr = addSpeedShimmer(lbl, Color3.fromRGB(60, 60, 60), Color3.fromRGB(255, 255, 255)) task.spawn(function() local t = 0 while bb and bb.Parent do t = t + 0.03 shimmerGr.Offset = Vector2.new(math.sin(t) * 0.4, 0) task.wait(0.04) end end) local conn = RunService.RenderStepped:Connect(function() if not hrp2 or not hrp2.Parent then conn:Disconnect() return end local hspd = Vector3.new(hrp2.AssemblyLinearVelocity.X, 0, hrp2.AssemblyLinearVelocity.Z).Magnitude lbl.Text = string.format("%.1f", hspd) end) end player.CharacterAdded:Connect(addBillboard) if player.Character then task.spawn(addBillboard, player.Character) end end for _, plr in ipairs(Players:GetPlayers()) do setupOtherPlayerEsp(plr) end Players.PlayerAdded:Connect(setupOtherPlayerEsp) Players.PlayerRemoving:Connect(function(plr) if plr.Character then local head = plr.Character:FindFirstChild("Head") if head then local bb = head:FindFirstChild("SpeedEspBillboard") if bb then bb:Destroy() end end end end) do tpToGround = function() local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local rot = root.CFrame.Rotation local raycastParams = RaycastParams.new() raycastParams.FilterType = Enum.RaycastFilterType.Exclude raycastParams.FilterDescendantsInstances = { char } local rayResult = workspace:Raycast(root.Position, Vector3.new(0, -500, 0), raycastParams) if rayResult then root.CFrame = CFrame.new(rayResult.Position + Vector3.new(0, 3, 0)) * rot else root.CFrame = CFrame.new(root.Position + Vector3.new(0, -20, 0)) * rot end end runDropBrainrot = function() if State.dropBrainrotActive then return end local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end State.dropBrainrotActive = true if VonData then VonData.IsStealing = false end State._grabStable = false State._grabOnSince = 0 State._grabOffSince = 1 State.isGrabbing = false if State.safeModeActive then pcall(applySafeModeRestore) end local t0 = tick() local dc dc = RunService.Heartbeat:Connect(function() local r = char and char:FindFirstChild("HumanoidRootPart") if not r then dc:Disconnect() State.dropBrainrotActive = false return end if tick() - t0 >= DROP_ASCEND_DURATION then dc:Disconnect() local rp = RaycastParams.new() rp.FilterDescendantsInstances = { char } rp.FilterType = Enum.RaycastFilterType.Exclude local rr = workspace:Raycast(r.Position, Vector3.new(0, -2000, 0), rp) if rr then local hum2 = char:FindFirstChildOfClass("Humanoid") local off = (hum2 and hum2.HipHeight or 2) + (r.Size.Y / 2) r.CFrame = CFrame.new(r.Position.X, rr.Position.Y + off, r.Position.Z) r.AssemblyLinearVelocity = Vector3.new(0, 0, 0) end State.dropBrainrotActive = false return end r.AssemblyLinearVelocity = Vector3.new(r.AssemblyLinearVelocity.X, DROP_ASCEND_SPEED, r.AssemblyLinearVelocity.Z) end) end getCurrentSpeed = function() if State.laggerActive then return State.laggerSpeed else return State.speedType == "normal" and State.normalSpeed or State.carrySpeed end end local function refreshSpeedModeLabel() if not modeValLbl then return end if State.laggerActive then modeValLbl.Text = "Lagger" else modeValLbl.Text = State.speedType == "carry" and "Carry" or "Normal" end end State.applyLiveWalkSpeed = function() pcall(function() local c = LP.Character local hum = c and c:FindFirstChildOfClass("Humanoid") if hum and not (VonData and VonData.IsStealing) then hum.WalkSpeed = getCurrentSpeed() end end) refreshSpeedModeLabel() end getAutoMoveSpeed = function() if State.laggerActive then return State.laggerSpeed end return State.normalSpeed end State._claimSpeedOwnership = function(root) pcall(function() root:SetNetworkOwner(LP) end) end State._resetSpeedTracking = function(root) State._speedLastPosition = root and root.Position or nil State._speedLastTime = root and tick() or nil State._speedLagbackCooldown = 0 end local function findBat() local char = LP.Character if not char then return nil end for _, tool in ipairs(char:GetChildren()) do if tool:IsA("Tool") and (tool.Name:lower():find("bat") or tool.Name:lower():find("slap")) then return tool end end local bp = LP:FindFirstChild("Backpack") if bp then for _, tool in ipairs(bp:GetChildren()) do if tool:IsA("Tool") and (tool.Name:lower():find("bat") or tool.Name:lower():find("slap")) then return tool end end end return nil end local function getClosestTarget() local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart") if not root then return nil end local closest, minDist = nil, math.huge for _, plr in ipairs(Players:GetPlayers()) do if plr ~= LP and plr.Character then local tRoot = plr.Character:FindFirstChild("HumanoidRootPart") local hum2 = plr.Character:FindFirstChildOfClass("Humanoid") if tRoot and hum2 and hum2.Health > 0 then local dist = (tRoot.Position - root.Position).Magnitude if dist < minDist then minDist = dist closest = tRoot end end end end return closest end local function tryHitBat() if State.hittingCooldown then return end State.hittingCooldown = true pcall(function() local char = LP.Character if not char then return end local hum2 = char:FindFirstChildOfClass("Humanoid") local bat = findBat() if bat then if bat.Parent ~= char and hum2 then pcall(function() hum2:EquipTool(bat) end) end local remote = bat:FindFirstChildOfClass("RemoteEvent") if remote then pcall(function() remote:FireServer() end) else pcall(function() bat:Activate() end) end end end) task.delay(0.08, function() State.hittingCooldown = false end) end local _aimbotTarget = nil local _batAimbotConn = nil startBatAimbot = function() if safeModeTryStart and not safeModeTryStart() then return end if _batAimbotConn then _batAimbotConn:Disconnect() end local hum0 = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid") if hum0 then hum0.AutoRotate = false end _batAimbotConn = RunService.RenderStepped:Connect(function() if not State.autoBatToggled then return end if State.safeModeEnabled and State.isGrabbing then return end local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end if not char:FindFirstChildOfClass("Tool") then local bat = findBat() if bat then pcall(function() hum:EquipTool(bat) end) end end local target = getClosestTarget() if not target then return end _aimbotTarget = target local targetVel = target.AssemblyLinearVelocity local myPos = root.Position local targetPos = target.Position local predictPos = targetPos + targetVel * 0.14 predictPos = predictPos + target.CFrame.LookVector * 0.3 local direction = predictPos - myPos local flatDir = Vector3.new(direction.X, 0, direction.Z).Unit local chaseSpeed = State.aimbotSpeed local desiredHeight = targetPos.Y + 3.7 local yVel = (desiredHeight - myPos.Y) * 19.5 + targetVel.Y * 0.8 if hum.FloorMaterial ~= Enum.Material.Air then yVel = math.max(yVel, 13) end yVel = math.clamp(yVel, -70, 110) local desiredVel = Vector3.new(flatDir.X * chaseSpeed, yVel, flatDir.Z * chaseSpeed) root.AssemblyLinearVelocity = root.AssemblyLinearVelocity:Lerp(desiredVel, 0.8) local speed3 = targetVel.Magnitude local predictTime = math.clamp(speed3 / 150, 0.05, 0.2) local predictedPos = targetPos + targetVel * predictTime local toPredict = predictedPos - myPos if toPredict.Magnitude > 0.1 then local goalCF = CFrame.lookAt(myPos, predictedPos) local diffCF = root.CFrame:Inverse() * goalCF local rx, ry, rz = diffCF:ToEulerAnglesXYZ() rx = math.clamp(rx, -2.5, 2.5) ry = math.clamp(ry, -2.5, 2.5) rz = math.clamp(rz, -2.5, 2.5) root.AssemblyAngularVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(rx * 42, ry * 42, rz * 42)) end end) end stopBatAimbot = function() if _batAimbotConn then _batAimbotConn:Disconnect() _batAimbotConn = nil end _aimbotTarget = nil local c = LP.Character local root = c and c:FindFirstChild("HumanoidRootPart") if root then local y = root.AssemblyLinearVelocity.Y root.AssemblyLinearVelocity = Vector3.new(0, y, 0) root.AssemblyAngularVelocity = Vector3.zero end local hum2 = c and c:FindFirstChildOfClass("Humanoid") if hum2 then hum2.AutoRotate = true hum2.PlatformStand = false end end local function _bodyLockTick() local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end local target = getClosestTarget() if not target then if not hum.AutoRotate then hum.AutoRotate = true end return end local dist = (target.Position - root.Position).Magnitude if dist > bodyLockRange then if not hum.AutoRotate then hum.AutoRotate = true end return end if hum.AutoRotate then hum.AutoRotate = false end local targetVel = target.AssemblyLinearVelocity local speed3 = targetVel.Magnitude local predictTime = math.clamp(speed3 / 150, 0.05, 0.2) local predictedPos = target.Position + targetVel * predictTime local flatTarget = Vector3.new(predictedPos.X, root.Position.Y, predictedPos.Z) local toPredict = flatTarget - root.Position if toPredict.Magnitude > 0.1 then local goalCF = CFrame.lookAt(root.Position, flatTarget) local diffCF = root.CFrame:Inverse() * goalCF local _, ry, _ = diffCF:ToEulerAnglesXYZ() ry = math.clamp(ry, -2.5, 2.5) root.AssemblyAngularVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(0, ry * 42, 0)) end end startBodyLock = function() if _bodyLockConn then _bodyLockConn:Disconnect() end _bodyLockConn = RunService.RenderStepped:Connect(function() if not bodyLockEnabled then return end _bodyLockTick() end) end stopBodyLock = function() if _bodyLockConn then _bodyLockConn:Disconnect() _bodyLockConn = nil end local c = LP.Character local root = c and c:FindFirstChild("HumanoidRootPart") if root then root.AssemblyAngularVelocity = Vector3.zero end local hum2 = c and c:FindFirstChildOfClass("Humanoid") if hum2 then hum2.AutoRotate = true end end local BAT_COUNTER_SLAP_LIST = { "Bat", "Slap", "Iron Slap", "Gold Slap", "Diamond Slap", "Emerald Slap", "Ruby Slap", "Dark Matter Slap", "Flame Slap", "Nuclear Slap", "Galaxy Slap", "Glitched Slap", } local function findBatForCounter() local c = LP.Character if not c then return nil end local bp = LP:FindFirstChildOfClass("Backpack") for _, name in ipairs(BAT_COUNTER_SLAP_LIST) do local t = c:FindFirstChild(name) or (bp and bp:FindFirstChild(name)) if t then return t end end for _, ch in ipairs(c:GetChildren()) do if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end end if bp then for _, ch in ipairs(bp:GetChildren()) do if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end end end return nil end local function swingBatForCounter(bat, char) local hum2 = char:FindFirstChildOfClass("Humanoid") if bat.Parent ~= char then if hum2 then pcall(function() hum2:EquipTool(bat) end) end task.wait(0.05) end local remote = bat:FindFirstChildOfClass("RemoteEvent") or bat:FindFirstChildOfClass("RemoteFunction") if remote and remote:IsA("RemoteEvent") then pcall(function() remote:FireServer() end) task.wait(0.15) pcall(function() remote:FireServer() end) else pcall(function() bat:Activate() end) task.wait(0.15) pcall(function() bat:Activate() end) end end startBatCounter = function() if Conns.batCounter then return end Conns.batCounter = RunService.Heartbeat:Connect(function() if not State.batCounterEnabled then return end if State.batCounterDebounce then return end local char = LP.Character if not char then return end local hum2 = char:FindFirstChildOfClass("Humanoid") if not hum2 then return end local st = hum2:GetState() local isRagdolled = st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll or st == Enum.HumanoidStateType.FallingDown if isRagdolled then State.batCounterDebounce = true task.spawn(function() local bat = findBatForCounter() if bat then swingBatForCounter(bat, char) end task.wait(0.5) State.batCounterDebounce = false end) end end) end stopBatCounter = function() if Conns.batCounter then Conns.batCounter:Disconnect() Conns.batCounter = nil end State.batCounterDebounce = false end startAntiBat = function() if Conns.antiBat then Conns.antiBat:Disconnect() end Conns.antiBat = RunService.Heartbeat:Connect(function() local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local hrp2 = char:FindFirstChild("HumanoidRootPart") if not hum or not hrp2 then return end if hum.MoveDirection.Magnitude <= 0 then return end local vel = hrp2.Velocity hrp2.Velocity = Vector3.new(vel.X * 50, 50, vel.Z * 50) RunService.RenderStepped:Wait() hrp2.Velocity = vel + Vector3.new(0, 0.05, 0) end) end stopAntiBat = function() if Conns.antiBat then Conns.antiBat:Disconnect() Conns.antiBat = nil end end local BAT_V2_SWING_CD = 0.35 local BAT_V2_HIT_DIST = 8 local batV2PrevAutoRotate = nil local function tryBatV2Swing() if State.batV2HitCD then return end State.batV2HitCD = true pcall(function() local char = LP.Character if not char then return end local bat = findBat() if bat then if bat.Parent ~= char then local hum2 = char:FindFirstChildOfClass("Humanoid") if hum2 then pcall(function() hum2:EquipTool(bat) end) end end pcall(function() bat:Activate() end) end end) task.delay(BAT_V2_SWING_CD, function() State.batV2HitCD = false end) end startBatV2Aimbot = function() if safeModeTryStart and not safeModeTryStart() then return end if Conns.batV2 then Conns.batV2:Disconnect() end local char0 = LP.Character local hum0 = char0 and char0:FindFirstChildOfClass("Humanoid") if hum0 then if batV2PrevAutoRotate == nil then batV2PrevAutoRotate = hum0.AutoRotate end hum0.AutoRotate = false end Conns.batV2 = RunService.RenderStepped:Connect(function() if not State.batV2Toggled then return end if State.safeModeEnabled and State.isGrabbing then return end local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end if not char:FindFirstChildOfClass("Tool") then local bat = findBat() if bat then pcall(function() hum:EquipTool(bat) end) end end local target = getClosestTarget() if not target then return end local myPos = root.Position local targetPos = target.Position local direction = targetPos - myPos local flatDir = Vector3.new(direction.X, 0, direction.Z) if flatDir.Magnitude > 0 then flatDir = flatDir.Unit else flatDir = Vector3.zero end local chaseSpeed = 58 local desiredHeight = targetPos.Y + 3.7 local yVel = (desiredHeight - myPos.Y) * 19.5 if hum.FloorMaterial ~= Enum.Material.Air then yVel = math.max(yVel, 13) end yVel = math.clamp(yVel, -70, 110) local desiredVel = Vector3.new(flatDir.X * chaseSpeed, yVel, flatDir.Z * chaseSpeed) root.AssemblyLinearVelocity = root.AssemblyLinearVelocity:Lerp(desiredVel, 0.8) local toTarget = targetPos - myPos if toTarget.Magnitude > 0.1 then local goalCF = CFrame.lookAt(myPos, targetPos) local diffCF = root.CFrame:Inverse() * goalCF local rx, ry, rz = diffCF:ToEulerAnglesXYZ() rx = math.clamp(rx, -2.5, 2.5) ry = math.clamp(ry, -2.5, 2.5) rz = math.clamp(rz, -2.5, 2.5) root.AssemblyAngularVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(rx * 42, ry * 42, rz * 42)) end local dist = (targetPos - myPos).Magnitude if dist <= BAT_V2_HIT_DIST then tryBatV2Swing() end end) end stopBatV2Aimbot = function() if Conns.batV2 then Conns.batV2:Disconnect() Conns.batV2 = nil end local c = LP.Character local root = c and c:FindFirstChild("HumanoidRootPart") if root then local y = root.AssemblyLinearVelocity.Y root.AssemblyLinearVelocity = Vector3.new(0, y, 0) root.AssemblyAngularVelocity = Vector3.zero end local hum = c and c:FindFirstChildOfClass("Humanoid") if hum then hum.AutoRotate = (batV2PrevAutoRotate == nil) and true or batV2PrevAutoRotate hum.PlatformStand = false local st = hum:GetState() if st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll or st == Enum.HumanoidStateType.FallingDown then pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp) end) end end batV2PrevAutoRotate = nil State.batV2HitCD = false end LP.CharacterAdded:Connect(function() if State.batV2Toggled then task.wait(0.5) if State.bypassBatMode == "tp" then pcall(startTPBat) else startBatV2Aimbot() end end if bodyLockEnabled then stopBodyLock() task.wait(0.2) pcall(startBodyLock) end end) startTPBat = function() if Conns.tpBat then Conns.tpBat:Disconnect() end State.tpBatActive = true Conns.tpBat = RunService.Heartbeat:Connect(function() if not State.tpBatActive then return end local char = LP.Character if not char then return end local hrp = char:FindFirstChild("HumanoidRootPart") if not hrp then return end local target = getClosestTarget() if target then pcall(function() if sethiddenproperty then sethiddenproperty(hrp, "PhysicsRepRootPart", target) end local targetPos = target.Position + Vector3.new(0, 0.9, 0) if (hrp.Position - targetPos).Magnitude > 8 then hrp.CFrame = CFrame.new(targetPos) end workspace.CurrentCamera.CFrame = CFrame.new(workspace.CurrentCamera.CFrame.Position, target.Position) tryHitBat() end) end end) end stopTPBat = function() State.tpBatActive = false if Conns.tpBat then Conns.tpBat:Disconnect() Conns.tpBat = nil end end end for _, name in pairs({ "DIVINE.CC_GUI", "DIVINE.CC_MobilePanel", "DIVINE.CC_StealGUI", "DIVINE.CC_GUI", "DIVINE.CC_DarkMode" }) do local old = game:GetService("CoreGui"):FindFirstChild(name) if old then old:Destroy() end local old2 = LP.PlayerGui and LP.PlayerGui:FindFirstChild(name) if old2 then old2:Destroy() end end local function createGrabFloatingPanel() local function dragFrame(f) local dn, ds, sp, di = false, nil, nil, nil f.InputBegan:Connect(function(i) if State.mobileLocked then return end if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then dn = true ds = i.Position sp = f.Position i.Changed:Connect(function() if i.UserInputState == Enum.UserInputState.End then dn = false end end) end end) f.InputChanged:Connect(function(i) if i.UserInputType == Enum.UserInputType.MouseMovement or i.UserInputType == Enum.UserInputType.Touch then di = i end end) UIS.InputChanged:Connect(function(i) if State.mobileLocked then dn = false return end if i == di and dn then local nX = sp.X.Offset + (i.Position.X - ds.X) local nY = sp.Y.Offset + (i.Position.Y - ds.Y) f.Position = UDim2.new(sp.X.Scale, nX, sp.Y.Scale, nY) end end) end local screenGui = Instance.new("ScreenGui") screenGui.Name = "DIVINE.CC_StealGUI" screenGui.ResetOnSpawn = false screenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling if not pcall(function() screenGui.Parent = game:GetService("CoreGui") end) then screenGui.Parent = LP:WaitForChild("PlayerGui") end local pbFrame = Instance.new("Frame", screenGui) pbFrame.Size = UDim2.new(0, 300, 0, 38) pbFrame.Position = UDim2.new(0.5, -150, 1, -48) local _genv = pcall(getgenv) and getgenv() or _G if _genv.SpeedHubVonPanelPos then local p = _genv.SpeedHubVonPanelPos pbFrame.Position = UDim2.new(p[1], p[2], p[3], p[4]) else pcall(function() local s = readfile("SpeedHubStealPanelPos.txt") if s and s ~= "" then local parts = {} for p in string.gmatch(s, "[^,]+") do table.insert(parts, p) end if #parts >= 4 then pbFrame.Position = UDim2.new(tonumber(parts[1]), tonumber(parts[2]), tonumber(parts[3]), tonumber(parts[4])) end end end) end pbFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 8) pbFrame.BorderSizePixel = 0 pbFrame.Active = true pbFrame.ClipsDescendants = true Instance.new("UICorner", pbFrame).CornerRadius = UDim.new(1, 0) local pbSt = Instance.new("UIStroke", pbFrame) pbSt.Color = Color3.fromRGB(255, 255, 255) pbSt.Thickness = 1.2 pbSt.Transparency = 0.35 stealPanelFrame = pbFrame dragFrame(pbFrame) UIS.InputEnded:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then local px = pbFrame.Position _genv.SpeedHubVonPanelPos = { px.X.Scale, px.X.Offset, px.Y.Scale, px.Y.Offset } pcall(function() writefile( "SpeedHubStealPanelPos.txt", string.format("%.3f,%.1f,%.3f,%.1f", px.X.Scale, px.X.Offset, px.Y.Scale, px.Y.Offset) ) end) end end) local fillRegion = Instance.new("Frame", pbFrame) fillRegion.Size = UDim2.new(0, 170, 1, -8) fillRegion.Position = UDim2.new(0, 5, 0, 4) fillRegion.BackgroundColor3 = Color3.fromRGB(18, 18, 18) fillRegion.BorderSizePixel = 0 fillRegion.ClipsDescendants = true fillRegion.ZIndex = 2 Instance.new("UICorner", fillRegion).CornerRadius = UDim.new(1, 0) local fillRegGradient = Instance.new("UIGradient", fillRegion) fillRegGradient.Color = ColorSequence.new(Color3.fromRGB(28, 28, 28), Color3.fromRGB(12, 12, 12)) fillRegGradient.Rotation = 90 local fillRegStroke = Instance.new("UIStroke", fillRegion) fillRegStroke.Color = Color3.fromRGB(0, 0, 0) fillRegStroke.Thickness = 1 fillRegStroke.Transparency = 0.6 progressFill = Instance.new("Frame", fillRegion) progressFill.Size = UDim2.new(0, 0, 1, 0) progressFill.Position = UDim2.new(0, 0, 0, 0) progressFill.BackgroundColor3 = C.ACCENT progressFill.BorderSizePixel = 0 progressFill.ZIndex = 3 Instance.new("UICorner", progressFill).CornerRadius = UDim.new(1, 0) local fillGradient = Instance.new("UIGradient", progressFill) fillGradient.Color = ColorSequence.new(C.ACCENT, C.ACCENT2) fillGradient.Rotation = 90 local stealLbl = Instance.new("TextLabel", fillRegion) stealLbl.Size = UDim2.new(0, 60, 1, 0) stealLbl.Position = UDim2.new(0, 12, 0, 0) stealLbl.BackgroundTransparency = 1 stealLbl.Text = "STEAL" stealLbl.TextColor3 = Color3.fromRGB(220, 220, 220) stealLbl.Font = Enum.Font.GothamBlack stealLbl.TextSize = 11 stealLbl.TextXAlignment = Enum.TextXAlignment.Left stealLbl.ZIndex = 5 progressPct = Instance.new("TextLabel", fillRegion) progressPct.BackgroundTransparency = 1 progressPct.Text = "" progressPct.ZIndex = 5 local pbDiv = Instance.new("Frame", pbFrame) pbDiv.Size = UDim2.new(0, 1, 0, 14) pbDiv.Position = UDim2.new(0, 182, 0.5, -7) pbDiv.BackgroundColor3 = C.ACCENT pbDiv.BackgroundTransparency = 0.7 pbDiv.BorderSizePixel = 0 pbDiv.ZIndex = 3 progressRadLbl = Instance.new("TextLabel", pbFrame) progressRadLbl.Size = UDim2.new(0, 110, 1, 0) progressRadLbl.Position = UDim2.new(0, 186, 0, 0) progressRadLbl.BackgroundTransparency = 1 progressRadLbl.Text = "-- · --" progressRadLbl.TextColor3 = Color3.fromRGB(230, 230, 230) progressRadLbl.Font = Enum.Font.GothamBold progressRadLbl.TextSize = 10 progressRadLbl.TextXAlignment = Enum.TextXAlignment.Center progressRadLbl.ZIndex = 4 local _pbState = "IDLE" local function setBarState(state, distance) if state == _pbState and state ~= "READY" then return end _pbState = state if state == "STEALING" then TweenService:Create(stealLbl, TweenInfo.new(0.2), { TextColor3 = Color3.fromRGB(255, 255, 255) }):Play() TweenService:Create(fillRegion, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(22, 26, 36) }) :Play() elseif state == "READY" then TweenService:Create(stealLbl, TweenInfo.new(0.2), { TextColor3 = Color3.fromRGB(255, 255, 255) }):Play() TweenService:Create(fillRegion, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(30, 50, 80) }) :Play() if progressPct then progressPct.Text = distance and (math.floor(distance) .. "m") or "READY" progressPct.TextColor3 = Color3.fromRGB(235, 235, 235) end else TweenService:Create(stealLbl, TweenInfo.new(0.2), { TextColor3 = Color3.fromRGB(220, 220, 220) }):Play() TweenService:Create(fillRegion, TweenInfo.new(0.2), { BackgroundColor3 = Color3.fromRGB(22, 26, 36) }) :Play() if progressPct and not VonData.IsStealing then progressPct.Text = "—" progressPct.TextColor3 = Color3.fromRGB(150, 150, 150) end end end task.spawn(function() while task.wait(0.25) do if VonData.IsStealing then setBarState("STEALING") else local target = getNearestStealable() if target then local root = getRootPart() local dist = root and (root.Position - target.Parent.WorldPosition).Magnitude or nil setBarState("READY", dist) else setBarState("IDLE") end end end end) task.spawn(function() local lastFrame = tick() local fpsSamples = {} local fpsAvg = 60 RunService.RenderStepped:Connect(function() local now = tick() local dt = now - lastFrame lastFrame = now if dt > 0 then table.insert(fpsSamples, 1 / dt) if #fpsSamples > 30 then table.remove(fpsSamples, 1) end local sum = 0 for _, v in ipairs(fpsSamples) do sum = sum + v end fpsAvg = sum / #fpsSamples end end) while true do local ping = 0 pcall(function() ping = LP:GetNetworkPing() * 1000 end) if progressRadLbl then progressRadLbl.Text = string.format("%d FPS | %dms", math.floor(fpsAvg + 0.5), math.floor(ping + 0.5)) end task.wait(0.5) end end) end local mbPanel = nil local stealPanelFrame = nil local MB_BTN_W = 55 local MB_BTN_H = 55 local MB_COLS = 2 local MB_ROWS = 4 local MB_PAD = 8 local MB_CORNER = 14 local MB_TOTAL_W = MB_COLS * MB_BTN_W + (MB_COLS + 1) * MB_PAD local MB_TOTAL_H = MB_ROWS * MB_BTN_H + (MB_ROWS + 1) * MB_PAD local function repositionMbButtons() if not mbPanel then return end local newW = MB_COLS * MB_BTN_W + (MB_COLS + 1) * MB_PAD local newH = MB_ROWS * MB_BTN_H + (MB_ROWS + 1) * MB_PAD mbPanel.Size = UDim2.new(0, newW, 0, newH) local idx = 0 for _, child in ipairs(mbPanel:GetChildren()) do if child:IsA("TextButton") then local row = math.floor(idx / MB_COLS) + 1 local col = (idx % MB_COLS) + 1 local x = MB_PAD + (col - 1) * (MB_BTN_W + MB_PAD) local y = MB_PAD + (row - 1) * (MB_BTN_H + MB_PAD) child.Size = UDim2.new(0, MB_BTN_W, 0, MB_BTN_H) child.Position = UDim2.new(0, x, 0, y) idx = idx + 1 end end end local function createMobilePanel() local panel = Instance.new("ScreenGui") panel.Name = "DIVINE.CC_MobilePanel" panel.Parent = LP:WaitForChild("PlayerGui") panel.ResetOnSpawn = false panel.ZIndexBehavior = Enum.ZIndexBehavior.Sibling mbPanel = Instance.new("Frame", panel) mbPanel.Name = "MobilePanel" mbPanel.Size = UDim2.new(0, MB_TOTAL_W, 0, MB_TOTAL_H) mbPanel.Position = UDim2.new(1, -(MB_TOTAL_W + 8), 0.5, -(MB_TOTAL_H / 2)) pcall(function() local s = readfile("SpeedHubMobilePos.txt") if s and s ~= "" then local parts = {} for p in string.gmatch(s, "[^,]+") do table.insert(parts, p) end if #parts >= 4 then mbPanel.Position = UDim2.new(tonumber(parts[1]), tonumber(parts[2]), tonumber(parts[3]), tonumber(parts[4])) end end end) if State.mbPanelPos then local p = State.mbPanelPos mbPanel.Position = UDim2.new(p[1], p[2], p[3], p[4]) end mbPanel.BackgroundTransparency = 1 mbPanel.BorderSizePixel = 0 mbPanel.Active = false mbPanel.ZIndex = 50 mbPanel.Visible = State.mobileVisible local dragging, dragInput, dragStart, startPos, moved = false, nil, nil, nil, false local DRAG_THRESHOLD = 8 mbPanel.InputBegan:Connect(function(inp) if State.mobileDragEnabled or State.mobileLocked then return end if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then dragging = true dragStart = inp.Position startPos = mbPanel.Position moved = false inp.Changed:Connect(function() if inp.UserInputState == Enum.UserInputState.End or inp.UserInputState == Enum.UserInputState.Cancelled then if moved then State.mbPanelPos = { mbPanel.Position.X.Scale, mbPanel.Position.X.Offset, mbPanel.Position.Y.Scale, mbPanel.Position.Y.Offset, } pcall(function() writefile( "SpeedHubMobilePos.txt", string.format( "%.3f,%.1f,%.3f,%.1f", mbPanel.Position.X.Scale, mbPanel.Position.X.Offset, mbPanel.Position.Y.Scale, mbPanel.Position.Y.Offset ) ) end) if autoSaveConfig then autoSaveConfig() end end dragging = false dragStart = nil moved = false end end) end end) mbPanel.InputChanged:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then dragInput = inp end end) UIS.InputChanged:Connect(function(inp) if State.mobileDragEnabled or State.mobileLocked or not dragging then return end if inp == dragInput then local d = inp.Position - dragStart if not moved and (math.abs(d.X) + math.abs(d.Y)) > DRAG_THRESHOLD then moved = true end if moved then mbPanel.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + d.X, startPos.Y.Scale, startPos.Y.Offset + d.Y) end end end) pcall(function() local s = readfile("SpeedHubBtnPos.json") if s and s ~= "" then local ok, data = pcall(function() return HttpService:JSONDecode(s) end) if ok and data then for k, v in pairs(data) do State.mbBtnPositions[k] = v end end end end) local function makeMobileBtn(label, row, col, onClick) local x = MB_PAD + (col - 1) * (MB_BTN_W + MB_PAD) local y = MB_PAD + (row - 1) * (MB_BTN_H + MB_PAD) local btn = Instance.new("TextButton", mbPanel) btn.Size = UDim2.new(0, MB_BTN_W, 0, MB_BTN_H) btn.Position = UDim2.new(0, x, 0, y) if State.mbBtnPositions and State.mbBtnPositions[label] then local p = State.mbBtnPositions[label] btn.Position = UDim2.new(p[1], p[2], p[3], p[4]) end btn.BackgroundColor3 = C.MB_BTN btn.BorderSizePixel = 0 btn.Text = "" btn.AutoButtonColor = false btn.ZIndex = 52 Instance.new("UICorner", btn).CornerRadius = UDim.new(0, MB_CORNER) local bs = Instance.new("UIStroke", btn) bs.Name = "MB_WhiteStroke" bs.Color = Color3.fromRGB(255, 255, 255) bs.Thickness = 1.5 bs.Transparency = 0 bs.ApplyStrokeMode = Enum.ApplyStrokeMode.Border pcall(function() bs.LineJoinMode = Enum.LineJoinMode.Round end) local lbl = Instance.new("TextLabel", btn) lbl.Size = UDim2.new(1, 0, 1, 0) lbl.BackgroundTransparency = 1 lbl.Text = label lbl.TextColor3 = C.MB_TEXT lbl.Font = Enum.Font.GothamBold lbl.TextSize = 9 lbl.TextWrapped = true lbl.ZIndex = 56 local btnDragStart, btnDragStartPos, btnMoved = nil, nil, false local BTN_DRAG_THRESHOLD = 6 btn.InputBegan:Connect(function(inp) if not State.mobileDragEnabled or State.mobileLocked then return end if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then btnDragStart = inp.Position btnDragStartPos = btn.Position btnMoved = false end end) btn.InputChanged:Connect(function(inp) if not State.mobileDragEnabled or State.mobileLocked or not btnDragStart then return end if inp.UserInputType == Enum.UserInputType.MouseMovement or inp.UserInputType == Enum.UserInputType.Touch then local d = inp.Position - btnDragStart if not btnMoved and (math.abs(d.X) + math.abs(d.Y)) > BTN_DRAG_THRESHOLD then btnMoved = true end if btnMoved then btn.Position = UDim2.new( btnDragStartPos.X.Scale, btnDragStartPos.X.Offset + d.X, btnDragStartPos.Y.Scale, btnDragStartPos.Y.Offset + d.Y ) end end end) UIS.InputEnded:Connect(function(inp) if not ( inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch ) then return end if btnDragStart then if btnMoved then State.mbBtnPositions[label] = { btn.Position.X.Scale, btn.Position.X.Offset, btn.Position.Y.Scale, btn.Position.Y.Offset } pcall(function() writefile("SpeedHubBtnPos.json", HttpService:JSONEncode(State.mbBtnPositions)) end) if autoSaveConfig then autoSaveConfig() end end btnDragStart = nil btnMoved = false end end) btn.MouseButton1Click:Connect(function() if btnMoved then return end if onClick then pcall(onClick) end end) btn:SetAttribute("MB_On", false) btn.AttributeChanged:Connect(function(a) if a == "MB_On" then local on = btn:GetAttribute("MB_On") if on then TweenService:Create(btn, TweenInfo.new(0.15), { BackgroundColor3 = Color3.fromRGB(255, 255, 255) }) :Play() TweenService:Create(lbl, TweenInfo.new(0.15), { TextColor3 = Color3.fromRGB(15, 15, 20) }):Play() TweenService :Create(bs, TweenInfo.new(0.15), { Transparency = 0, Color = Color3.fromRGB(255, 255, 255) }) :Play() bs.Thickness = 1.5 else TweenService:Create(btn, TweenInfo.new(0.15), { BackgroundColor3 = C.MB_BTN }):Play() TweenService:Create(lbl, TweenInfo.new(0.15), { TextColor3 = C.MB_TEXT }):Play() TweenService :Create( bs, TweenInfo.new(0.15), { Transparency = 0, Color = Color3.fromRGB(255, 255, 255) } ) :Play() bs.Thickness = 1.5 end end end) return btn end local alMbBtn = makeMobileBtn("AUTO\nLEFT", 1, 1, function() if State.safeModeEnabled and State.safeModeActive and not State.autoLeftEnabled then return end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end State.autoLeftEnabled = not State.autoLeftEnabled if setAutoLeft then setAutoLeft(State.autoLeftEnabled) end if State.autoLeftEnabled then startAutoLeft() else stopAutoLeft() end end) local arMbBtn = makeMobileBtn("AUTO\nRIGHT", 1, 2, function() if State.safeModeEnabled and State.safeModeActive and not State.autoRightEnabled then return end if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end State.autoRightEnabled = not State.autoRightEnabled if setAutoRight then setAutoRight(State.autoRightEnabled) end if State.autoRightEnabled then startAutoRight() else stopAutoRight() end end) local batMbBtn = makeMobileBtn("BAT\nAIMBOT", 2, 1, function() if State.safeModeEnabled and State.safeModeActive and not State.autoBatToggled then return end if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end State.autoBatToggled = not State.autoBatToggled if setAutoBat then setAutoBat(State.autoBatToggled) end if State.autoBatToggled then startBatAimbot() else stopBatAimbot() end end) makeMobileBtn("DROP\nBR", 2, 2, function() runDropBrainrot() end) local carryMbBtn = makeMobileBtn("CARRY\nSPD", 3, 1, function() State.speedType = (State.speedType == "normal") and "carry" or "normal" if State.speedType == "carry" and State.laggerActive then State.laggerActive = false if setLaggerToggleUI then setLaggerToggleUI(false) end end if setSpeedToggleUI then setSpeedToggleUI(State.speedType == "carry") end State.applyLiveWalkSpeed() if autoSaveConfig then autoSaveConfig() end end) local lagMbBtn = makeMobileBtn("LAGGER\nMODE", 3, 2, function() State.laggerActive = not State.laggerActive if State.laggerActive and State.speedType == "carry" then State.speedType = "normal" if setSpeedToggleUI then setSpeedToggleUI(false) end end if setLaggerToggleUI then setLaggerToggleUI(State.laggerActive) end State.applyLiveWalkSpeed() if autoSaveConfig then autoSaveConfig() end end) makeMobileBtn("TP\nDOWN", 4, 1, function() tpToGround() end) local bypassBatMbBtn = makeMobileBtn("BYPASS\nBAT", 4, 2, function() local nv = not State.batV2Toggled if nv and State.safeModeEnabled and (State.safeModeActive or State.isGrabbing or (safeModeIsLocked and safeModeIsLocked())) then return end if nv then if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end end if setBypassBatUI then setBypassBatUI(nv) end end) task.spawn(function() while task.wait(0.2) do pcall(function() if alMbBtn then alMbBtn:SetAttribute("MB_On", State.autoLeftEnabled) end if arMbBtn then arMbBtn:SetAttribute("MB_On", State.autoRightEnabled) end if batMbBtn then batMbBtn:SetAttribute("MB_On", State.autoBatToggled) end if carryMbBtn then carryMbBtn:SetAttribute("MB_On", State.speedType == "carry") end if lagMbBtn then lagMbBtn:SetAttribute("MB_On", State.laggerActive) end if bypassBatMbBtn then bypassBatMbBtn:SetAttribute("MB_On", State.batV2Toggled) end end) end end) end local SIDENAV_W = 0 local TAB_BAR_H = 38 local GUI_W = 348 local GUI_H = 500 local gui = Instance.new("ScreenGui") gui.Name = "DIVINE.CC_GUI" gui.ResetOnSpawn = false gui.DisplayOrder = 10 gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling gui.IgnoreGuiInset = true gui.Parent = LP:WaitForChild("PlayerGui") local main = Instance.new("ImageLabel", gui) main.Name = "Main" main.Size = UDim2.new(0, GUI_W, 0, GUI_H) main.Position = UDim2.new(0, 20, 0, 20) main.BackgroundColor3 = C.BG main.BackgroundTransparency = 0 main.BorderSizePixel = 0 main.Active = true main.ClipsDescendants = true main.Image = "rbxassetid://130946629000664" main.ScaleType = Enum.ScaleType.Stretch main.ImageTransparency = 0 main.Visible = false local GUI_SCALE = 0.8 local guiScaleObj = Instance.new("UIScale", main) guiScaleObj.Scale = GUI_SCALE local mainStroke = Instance.new("UIStroke", main) mainStroke.Color = C.BORDER2 mainStroke.Thickness = 1.5 Instance.new("UICorner", main).CornerRadius = UDim.new(0, 18) local header = Instance.new("Frame", main) header.Name = "Header" header.Size = UDim2.new(1, 0, 0, 36) header.BackgroundColor3 = C.HEADER header.BackgroundTransparency = 1 header.BorderSizePixel = 0 header.ZIndex = 5 header.Active = true Instance.new("UICorner", header).CornerRadius = UDim.new(0, 18) local titleLbl = Instance.new("TextLabel", header) titleLbl.Size = UDim2.new(1, -48, 0, 20) titleLbl.Position = UDim2.new(0, 12, 0.5, -10) titleLbl.BackgroundTransparency = 1 titleLbl.Text = "DIVINE.CC" titleLbl.TextColor3 = C.ACCENT titleLbl.Font = Enum.Font.GothamBlack titleLbl.TextSize = 18 titleLbl.TextXAlignment = Enum.TextXAlignment.Left titleLbl.ZIndex = 6 titleLbl.Active = false local titleShimmer = addSpeedShimmer(titleLbl, Color3.fromRGB(60, 60, 60), Color3.fromRGB(255, 255, 255)) task.spawn(function() local t = 0 while titleLbl and titleLbl.Parent do t = t + 0.03 titleShimmer.Offset = Vector2.new(math.sin(t) * 0.4, 0) task.wait(0.04) end end) local SearchIndex = {} local Decorations = {} local closeBtn = Instance.new("TextButton", main) closeBtn.Name = "CloseBtn" closeBtn.Size = UDim2.new(0, 26, 0, 26) closeBtn.Position = UDim2.new(1, -32, 0, 5) closeBtn.AnchorPoint = Vector2.new(0, 0) closeBtn.BackgroundColor3 = Color3.fromRGB(0, 0, 0) closeBtn.BorderSizePixel = 0 closeBtn.Text = "-" closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255) closeBtn.Font = Enum.Font.GothamBlack closeBtn.TextSize = 13 closeBtn.ZIndex = 50 closeBtn.Active = true closeBtn.AutoButtonColor = false Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 16) local closeBtnStroke = Instance.new("UIStroke", closeBtn) closeBtnStroke.Color = C.BORDER closeBtnStroke.Thickness = 1.5 closeBtn.MouseEnter:Connect(function() TweenService :Create( closeBtn, TweenInfo.new(0.12), { BackgroundColor3 = C.ACCENT, TextColor3 = Color3.fromRGB(255, 255, 255) } ) :Play() end) closeBtn.MouseLeave:Connect(function() TweenService:Create( closeBtn, TweenInfo.new(0.12), { BackgroundColor3 = Color3.fromRGB(0, 0, 0), TextColor3 = Color3.fromRGB(255, 255, 255) } ):Play() end) local miniBtn = Instance.new("TextButton", gui) miniBtn.Name = "SpeedHubMiniButton" miniBtn.Size = UDim2.new(0, 160, 0, 30) miniBtn.Position = UDim2.new(0, 50, 0, 50) miniBtn.BackgroundColor3 = Color3.fromRGB(8, 8, 8) miniBtn.BorderSizePixel = 0 miniBtn.Text = "DIVINE.CC" miniBtn.TextColor3 = Color3.fromRGB(255, 255, 255) miniBtn.Font = Enum.Font.GothamBold miniBtn.TextSize = 11 miniBtn.TextXAlignment = Enum.TextXAlignment.Center miniBtn.ZIndex = 20 miniBtn.Visible = false miniBtn.AutoButtonColor = false Instance.new("UICorner", miniBtn).CornerRadius = UDim.new(0, 18) local miniBtnStroke = Instance.new("UIStroke", miniBtn) miniBtnStroke.Color = Color3.fromRGB(0, 0, 0) miniBtnStroke.Thickness = 1 local function showGui() main.Visible = true miniBtn.Visible = false State.guiVisible = true end local function hideGui() main.Visible = false miniBtn.Visible = true State.guiVisible = false end closeBtn.InputBegan:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then pcall(function() inp:StopPropagation() end) end end) closeBtn.MouseButton1Click:Connect(function() hideGui() end) miniBtn.MouseButton1Click:Connect(showGui) miniBtn.MouseEnter:Connect(function() TweenService:Create(miniBtn, TweenInfo.new(0.1), { BackgroundColor3 = Color3.fromRGB(24, 24, 24) }):Play() end) miniBtn.MouseLeave:Connect(function() TweenService:Create(miniBtn, TweenInfo.new(0.1), { BackgroundColor3 = Color3.fromRGB(8, 8, 8) }):Play() end) local function makeDraggable(frame, handle) handle = handle or frame local dragging = false local dragStart = nil local startPos = nil local function beginDrag(inp) if inp.UserInputType ~= Enum.UserInputType.MouseButton1 and inp.UserInputType ~= Enum.UserInputType.Touch then return end dragging = true dragStart = inp.Position startPos = frame.Position end handle.InputBegan:Connect(beginDrag) if handle ~= frame then frame.InputBegan:Connect(beginDrag) end UIS.InputChanged:Connect(function(inp) if not dragging then return end if inp.UserInputType ~= Enum.UserInputType.MouseMovement and inp.UserInputType ~= Enum.UserInputType.Touch then return end if not dragStart or not startPos then return end local dx = inp.Position.X - dragStart.X local dy = inp.Position.Y - dragStart.Y frame.Position = UDim2.new( startPos.X.Scale, startPos.X.Offset + dx, startPos.Y.Scale, startPos.Y.Offset + dy ) end) UIS.InputEnded:Connect(function(inp) if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then dragging = false dragStart = nil startPos = nil end end) end makeDraggable(main, header) makeDraggable(miniBtn) local function saveMainPos() local pos = main.Position pcall(function() writefile( "DIVINE.CC_GUIPos.txt", string.format("%.3f,%.1f,%.3f,%.1f", pos.X.Scale, pos.X.Offset, pos.Y.Scale, pos.Y.Offset) ) end) end local function saveMiniPos() local pos = miniBtn.Position pcall(function() writefile( "SpeedHubMiniPos.txt", string.format("%.3f,%.1f,%.3f,%.1f", pos.X.Scale, pos.X.Offset, pos.Y.Scale, pos.Y.Offset) ) end) end local function loadSavedPos(frame, filename) local s pcall(function() s = readfile(filename) end) if s and s ~= "" then local parts = {} for p in string.gmatch(s, "[^,]+") do table.insert(parts, p) end if #parts >= 4 then local sx = tonumber(parts[1]) or 0 local ox = tonumber(parts[2]) or 0 local sy = tonumber(parts[3]) or 0 local oy = tonumber(parts[4]) or 0 local cam = workspace.CurrentCamera local vw = (cam and cam.ViewportSize.X) or 1280 local vh = (cam and cam.ViewportSize.Y) or 720 local fw = (frame.Size and frame.Size.X.Offset) or 100 local fh = (frame.Size and frame.Size.Y.Offset) or 40 local absX = sx * vw + ox local absY = sy * vh + oy absX = math.clamp(absX, -fw + 40, vw - 40) absY = math.clamp(absY, 0, vh - 40) frame.Position = UDim2.new(0, absX, 0, absY) end end end local function updateClosePos() closeBtn.Visible = main.Visible closeBtn.Position = UDim2.new(1, -32, 0, 5) end local lastSave = 0 local lastMiniSave = 0 main:GetPropertyChangedSignal("Position"):Connect(function() if tick() - lastSave > 0.5 then lastSave = tick() saveMainPos() updateClosePos() end end) main:GetPropertyChangedSignal("Visible"):Connect(updateClosePos) miniBtn:GetPropertyChangedSignal("Position"):Connect(function() if tick() - lastMiniSave > 0.5 then lastMiniSave = tick() saveMiniPos() end end) local TAB_NAMES = { "Speed", "Bat\nAimbot", "Mechanics", "Movement", "Settings" } local TAB_ICONS = { "", "", "", "", "" } local sideNav = Instance.new("Frame", main) sideNav.Name = "SideNav" sideNav.Size = UDim2.new(1, 0, 0, TAB_BAR_H) sideNav.Position = UDim2.new(0, 0, 0, 36) sideNav.BackgroundColor3 = C.SIDENAV sideNav.BackgroundTransparency = 0.7 sideNav.BorderSizePixel = 0 sideNav.ZIndex = 6 Instance.new("UICorner", sideNav).CornerRadius = UDim.new(0, 18) local pageHolder = Instance.new("Frame", main) pageHolder.Size = UDim2.new(1, 0, 1, -(36 + TAB_BAR_H)) pageHolder.Position = UDim2.new(0, 0, 0, 36 + TAB_BAR_H) pageHolder.BackgroundTransparency = 1 pageHolder.BorderSizePixel = 0 pageHolder.ZIndex = 2 pageHolder.ClipsDescendants = true local tabBtns = {} local tabPages = {} local tabListLayouts = {} local activeTab = "" for i, name in ipairs(TAB_NAMES) do local TAB_COUNT = #TAB_NAMES local btn = Instance.new("TextButton", sideNav) btn.Size = UDim2.new(1 / TAB_COUNT, 0, 1, 0) btn.Position = UDim2.new((i - 1) / TAB_COUNT, 0, 0, 0) btn.BackgroundColor3 = C.SIDENAV btn.BackgroundTransparency = 0.6 btn.BorderSizePixel = 0 btn.Text = "" btn.AutoButtonColor = false btn.ZIndex = 7 local indicator = Instance.new("Frame", btn) indicator.Size = UDim2.new(0.6, 0, 0, 3) indicator.Position = UDim2.new(0.2, 0, 1, -3) indicator.BackgroundColor3 = Color3.fromRGB(70, 70, 75) indicator.BackgroundTransparency = 0.5 indicator.BorderSizePixel = 0 indicator.ZIndex = 8 indicator.Visible = false Instance.new("UICorner", indicator).CornerRadius = UDim.new(0, 14) local iconLbl = Instance.new("TextLabel", btn) iconLbl.Size = UDim2.new(0, 0, 0, 0) iconLbl.BackgroundTransparency = 1 iconLbl.Text = "" iconLbl.ZIndex = 8 local nameLbl = Instance.new("TextLabel", btn) nameLbl.Size = UDim2.new(1, 0, 1, -4) nameLbl.Position = UDim2.new(0, 0, 0, 0) nameLbl.BackgroundTransparency = 1 nameLbl.Text = name:gsub("\n", " ") nameLbl.TextColor3 = Color3.fromRGB(200, 200, 210) nameLbl.Font = Enum.Font.GothamBold nameLbl.TextSize = 10 nameLbl.TextWrapped = true nameLbl.LineHeight = 1.1 nameLbl.ZIndex = 8 nameLbl.TextXAlignment = Enum.TextXAlignment.Center nameLbl.TextYAlignment = Enum.TextYAlignment.Center tabBtns[name] = { btn = btn, indicator = indicator, icon = iconLbl, label = nameLbl } local pageKey = name:gsub("\n", " ") local page = Instance.new("ScrollingFrame", pageHolder) page.Name = pageKey .. "Page" page.Size = UDim2.new(1, 0, 1, 0) page.BackgroundTransparency = 1 page.BorderSizePixel = 0 page.ScrollBarThickness = 2 page.ScrollBarImageColor3 = C.BORDER2 page.AutomaticCanvasSize = Enum.AutomaticSize.Y page.CanvasSize = UDim2.new(0, 0, 0, 0) page.ZIndex = 2 page.Visible = false local ll = Instance.new("UIListLayout", page) ll.SortOrder = Enum.SortOrder.LayoutOrder ll.Padding = UDim.new(0, 2) local pd = Instance.new("UIPadding", page) pd.PaddingLeft = UDim.new(0, 8) pd.PaddingRight = UDim.new(0, 8) pd.PaddingTop = UDim.new(0, 10) pd.PaddingBottom = UDim.new(0, 12) tabPages[name] = page tabListLayouts[name] = ll end do local settingsBgImg = Instance.new("ImageLabel", main) settingsBgImg.Size = UDim2.new(1, 0, 1, -(36 + TAB_BAR_H)) settingsBgImg.Position = UDim2.new(0, 0, 0, 36 + TAB_BAR_H) settingsBgImg.BackgroundTransparency = 1 settingsBgImg.Image = "rbxassetid://94187149763780" settingsBgImg.ScaleType = Enum.ScaleType.Crop settingsBgImg.ImageTransparency = 1 settingsBgImg.ZIndex = 1 settingsBgImg.Visible = false settingsBgImg.Active = false Instance.new("UICorner", settingsBgImg).CornerRadius = UDim.new(0, 16) end local tabTransOverlay = Instance.new("Frame", pageHolder) tabTransOverlay.Size = UDim2.new(1, 0, 1, 0) tabTransOverlay.BackgroundColor3 = Color3.fromRGB(10, 10, 12) tabTransOverlay.BackgroundTransparency = 1 tabTransOverlay.BorderSizePixel = 0 tabTransOverlay.ZIndex = 9 tabTransOverlay.Visible = false local function switchTab(name, instant) activeTab = name for tname, t in pairs(tabBtns) do if tname == name then if instant then t.btn.BackgroundColor3 = C.ROW t.icon.TextColor3 = C.ACCENT t.label.TextColor3 = C.ACCENT else TweenService:Create(t.btn, TweenInfo.new(0.15, Enum.EasingStyle.Quad), { BackgroundColor3 = C.ROW }) :Play() TweenService:Create(t.icon, TweenInfo.new(0.15), { TextColor3 = C.ACCENT }):Play() TweenService:Create(t.label, TweenInfo.new(0.15), { TextColor3 = C.ACCENT }):Play() end else if instant then t.btn.BackgroundColor3 = C.SIDENAV t.icon.TextColor3 = Color3.fromRGB(180, 180, 190) t.label.TextColor3 = Color3.fromRGB(200, 200, 210) else TweenService:Create(t.btn, TweenInfo.new(0.15, Enum.EasingStyle.Quad), { BackgroundColor3 = C.SIDENAV }) :Play() TweenService:Create(t.icon, TweenInfo.new(0.15), { TextColor3 = Color3.fromRGB(180, 180, 190) }):Play() TweenService:Create(t.label, TweenInfo.new(0.15), { TextColor3 = Color3.fromRGB(200, 200, 210) }):Play() end end end local function doSwap() for tname, page in pairs(tabPages) do page.Visible = (tname == name) end end if instant then doSwap() else tabTransOverlay.BackgroundTransparency = 1 tabTransOverlay.Visible = true TweenService :Create(tabTransOverlay, TweenInfo.new(0.07, Enum.EasingStyle.Quad), { BackgroundTransparency = 0.5 }) :Play() task.delay(0.07, function() doSwap() TweenService :Create(tabTransOverlay, TweenInfo.new(0.10, Enum.EasingStyle.Quad), { BackgroundTransparency = 1 }) :Play() task.delay(0.10, function() tabTransOverlay.Visible = false end) end) end end for _, name in ipairs(TAB_NAMES) do local t = tabBtns[name] t.btn.MouseEnter:Connect(function() if activeTab ~= name then TweenService:Create(t.btn, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW_HOV }):Play() end end) t.btn.MouseLeave:Connect(function() if activeTab ~= name then TweenService:Create(t.btn, TweenInfo.new(0.1), { BackgroundColor3 = C.SIDENAV }):Play() end end) t.btn.MouseButton1Click:Connect(function() switchTab(name) end) end local loCounters = { Speed = 0, ["Bat\nAimbot"] = 0, Mechanics = 0, Movement = 0, Performance = 0, Settings = 0 } local function LO(tab) loCounters[tab] = loCounters[tab] + 1 return loCounters[tab] end local function makeGap(tab, px) local f = Instance.new("Frame", tabPages[tab]) f.Size = UDim2.new(1, 0, 0, px or 4) f.BackgroundTransparency = 1 f.BorderSizePixel = 0 f.LayoutOrder = LO(tab) table.insert(Decorations, { frame = f, tab = tab }) end local function makeDivider(tab) local f = Instance.new("Frame", tabPages[tab]) f.Size = UDim2.new(1, 0, 0, 1) f.BackgroundColor3 = C.BORDER f.BorderSizePixel = 0 f.LayoutOrder = LO(tab) table.insert(Decorations, { frame = f, tab = tab }) end local function makeSectionLabel(tab, text) local row = Instance.new("Frame", tabPages[tab]) row.Size = UDim2.new(1, 0, 0, 26) row.BackgroundColor3 = Color3.fromRGB(0, 0, 0) row.BackgroundTransparency = 0.35 row.BorderSizePixel = 0 row.LayoutOrder = LO(tab) Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8) local lbl = Instance.new("TextLabel", row) lbl.Size = UDim2.new(1, -16, 1, 0) lbl.Position = UDim2.new(0, 10, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = text:upper() lbl.TextColor3 = Color3.fromRGB(255, 255, 255) lbl.Font = Enum.Font.GothamBold lbl.TextSize = 11 lbl.TextXAlignment = Enum.TextXAlignment.Left lbl.TextTransparency = 0 lbl.TextStrokeTransparency = 0.5 lbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) table.insert(Decorations, { frame = row, tab = tab }) end local autoSaveConfig local function makeInputRow(tab, label, default, onChange, allowDecimal) local row = Instance.new("Frame", tabPages[tab]) row.Size = UDim2.new(1, 0, 0, 42) row.BackgroundColor3 = C.ROW row.BackgroundTransparency = 0.15 row.BorderSizePixel = 0 row.LayoutOrder = LO(tab) Instance.new("UICorner", row).CornerRadius = UDim.new(0, 16) local rs = Instance.new("UIStroke", row) rs.Color = C.BORDER rs.Thickness = 1 local leftBar = Instance.new("Frame", row) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) local lbl = Instance.new("TextLabel", row) lbl.Size = UDim2.new(0.55, 0, 1, 0) lbl.Position = UDim2.new(0, 12, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = label lbl.TextColor3 = Color3.fromRGB(255, 255, 255) lbl.Font = Enum.Font.GothamBold lbl.TextSize = 12 lbl.TextXAlignment = Enum.TextXAlignment.Left lbl.TextStrokeTransparency = 0.4 lbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) lbl.ZIndex = 2 local box = Instance.new("TextBox", row) box.Size = UDim2.new(0, 90, 0, 34) box.Position = UDim2.new(1, -96, 0.5, -17) box.BackgroundColor3 = C.KEY_BG box.BorderSizePixel = 0 box.Text = allowDecimal and formatSpeedNum(default) or tostring(default) box.TextColor3 = Color3.fromRGB(255, 255, 255) box.Font = Enum.Font.Gotham box.TextSize = 14 box.ClearTextOnFocus = true box.PlaceholderText = "0" box.ZIndex = 3 Instance.new("UICorner", box).CornerRadius = UDim.new(0, 16) local bs = Instance.new("UIStroke", box) bs.Color = C.BORDER bs.Thickness = 1 box.InputBegan:Connect(function(inp) inp:StopPropagation() end) box.Focused:Connect(function() TweenService:Create(bs, TweenInfo.new(0.15), { Color = C.ACCENT }):Play() box.BackgroundColor3 = Color3.fromRGB(50, 50, 55) end) box.FocusLost:Connect(function() TweenService:Create(bs, TweenInfo.new(0.15), { Color = C.BORDER }):Play() box.BackgroundColor3 = C.KEY_BG local num = tonumber(box.Text) if num ~= nil then local fv = allowDecimal and (math.floor(math.clamp(num, 0, 9999) * 100 + 0.5) / 100) or math.floor(math.clamp(num, 0, 9999)) box.Text = allowDecimal and formatSpeedNum(fv) or tostring(fv) if onChange then onChange(tostring(fv)) end if autoSaveConfig then autoSaveConfig() end else box.Text = tostring(default) end end) box.Active = true box.Selectable = true table.insert(SearchIndex, { frame = row, label = label, tab = tab }) return box end local function makeStatusRow(tab, label, valTxt) local row = Instance.new("Frame", tabPages[tab]) row.Size = UDim2.new(1, 0, 0, 36) row.BackgroundColor3 = C.ROW row.BackgroundTransparency = 0.15 row.BorderSizePixel = 0 row.LayoutOrder = LO(tab) Instance.new("UICorner", row).CornerRadius = UDim.new(0, 16) Instance.new("UIStroke", row).Color = C.BORDER local leftBar = Instance.new("Frame", row) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) local lbl = Instance.new("TextLabel", row) lbl.Size = UDim2.new(0.5, 0, 1, 0) lbl.Position = UDim2.new(0, 12, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = label lbl.TextColor3 = C.ACCENT lbl.Font = Enum.Font.GothamBold lbl.TextSize = 12 lbl.TextXAlignment = Enum.TextXAlignment.Left local val = Instance.new("TextLabel", row) val.Size = UDim2.new(0.45, -10, 1, 0) val.Position = UDim2.new(0.52, 0, 0, 0) val.BackgroundTransparency = 1 val.Text = valTxt val.TextColor3 = C.ACCENT2 val.Font = Enum.Font.GothamBlack val.TextSize = 12 val.TextXAlignment = Enum.TextXAlignment.Right table.insert(SearchIndex, { frame = row, label = label, tab = tab }) return val end local function makeActionBtn(tab, label, onClick) local btn = Instance.new("TextButton", tabPages[tab]) btn.Size = UDim2.new(1, 0, 0, 36) btn.BackgroundColor3 = C.PANEL btn.BackgroundTransparency = 0.15 btn.BorderSizePixel = 0 btn.LayoutOrder = LO(tab) btn.Text = label btn.TextColor3 = C.TEXT btn.Font = Enum.Font.GothamBold btn.TextSize = 13 Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 16) Instance.new("UIStroke", btn).Color = C.BORDER2 local leftBar = Instance.new("Frame", btn) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) btn.MouseButton1Click:Connect(function() TweenService:Create(btn, TweenInfo.new(0.08), { BackgroundColor3 = C.BORDER2 }):Play() task.delay(0.16, function() TweenService:Create(btn, TweenInfo.new(0.12), { BackgroundColor3 = C.PANEL }):Play() end) if onClick then pcall(onClick) end end) btn.MouseEnter:Connect(function() TweenService:Create(btn, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW_HOV }):Play() end) btn.MouseLeave:Connect(function() TweenService:Create(btn, TweenInfo.new(0.1), { BackgroundColor3 = C.PANEL }):Play() end) table.insert(SearchIndex, { frame = btn, label = label, tab = tab, btn = true }) return btn end local function makeKeybindRow(tab, label, currentKey, onChanged) local row = Instance.new("Frame", tabPages[tab]) row.Size = UDim2.new(1, 0, 0, 38) row.BackgroundColor3 = C.ROW row.BackgroundTransparency = 0.15 row.LayoutOrder = LO(tab) Instance.new("UICorner", row).CornerRadius = UDim.new(0, 16) Instance.new("UIStroke", row).Color = C.BORDER local leftBar = Instance.new("Frame", row) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) local lbl = Instance.new("TextLabel", row) lbl.Size = UDim2.new(0.55, 0, 1, 0) lbl.Position = UDim2.new(0, 12, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = label lbl.TextColor3 = C.ACCENT lbl.Font = Enum.Font.GothamBold lbl.TextSize = 12 lbl.TextXAlignment = Enum.TextXAlignment.Left local btn = Instance.new("TextButton", row) btn.Size = UDim2.new(0, 82, 0, 26) btn.Position = UDim2.new(1, -88, 0.5, -13) btn.BackgroundColor3 = C.KEY_BG btn.BorderSizePixel = 0 btn.Text = currentKey.Name btn.TextColor3 = Color3.fromRGB(255, 255, 255) btn.Font = Enum.Font.GothamBold btn.TextSize = 11 Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 14) local bs = Instance.new("UIStroke", btn) bs.Color = C.BORDER2 bs.Thickness = 1 local listening = false local listenConn local function stopListen(key) listening = false if listenConn then listenConn:Disconnect() listenConn = nil end TweenService:Create(bs, TweenInfo.new(0.12), { Color = C.BORDER2 }):Play() btn.TextColor3 = Color3.fromRGB(255, 255, 255) if key then btn.Text = key.Name if onChanged then onChanged(key) end if autoSaveConfig then autoSaveConfig() end end end btn.MouseButton1Click:Connect(function() if listening then stopListen(nil) return end listening = true btn.Text = "..." btn.TextColor3 = C.ACCENT2 TweenService:Create(bs, TweenInfo.new(0.12), { Color = C.ACCENT2 }):Play() listenConn = UIS.InputBegan:Connect(function(inp, gp) if not listening then return end if inp.UserInputType == Enum.UserInputType.Keyboard then if inp.KeyCode == Enum.KeyCode.Escape then stopListen(nil) return end stopListen(inp.KeyCode) elseif inp.UserInputType == Enum.UserInputType.Gamepad1 then if inp.KeyCode == Enum.KeyCode.Escape then stopListen(nil) return end stopListen(inp.KeyCode) end end) end) table.insert(SearchIndex, { frame = row, label = label, tab = tab }) return btn end local function makeChoiceRow(tab, label, options, defaultVal, onChange) local row = Instance.new("Frame", tabPages[tab]) row.Size = UDim2.new(1, 0, 0, 38) row.BackgroundColor3 = C.ROW row.BackgroundTransparency = 0.15 row.LayoutOrder = LO(tab) Instance.new("UICorner", row).CornerRadius = UDim.new(0, 16) local rowStroke = Instance.new("UIStroke", row) rowStroke.Color = C.BORDER rowStroke.Thickness = 1 local leftBar = Instance.new("Frame", row) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) local lbl = Instance.new("TextLabel", row) lbl.Size = UDim2.new(0, 130, 1, 0) lbl.Position = UDim2.new(0, 12, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = label lbl.TextColor3 = C.ACCENT lbl.Font = Enum.Font.GothamBold lbl.TextSize = 12 lbl.TextXAlignment = Enum.TextXAlignment.Left local wrapW = 140 local wrap = Instance.new("Frame", row) wrap.Size = UDim2.new(0, wrapW, 0, 26) wrap.Position = UDim2.new(1, -(wrapW + 10), 0.5, -13) wrap.BackgroundTransparency = 1 local current = defaultVal local btns = {} local function refresh() for _, o in ipairs(options) do local b = btns[o] local on = (o == current) TweenService:Create(b, TweenInfo.new(0.12), { BackgroundColor3 = on and C.ACCENT or C.KEY_BG }):Play() b.TextColor3 = on and Color3.fromRGB(15, 15, 20) or Color3.fromRGB(255, 255, 255) end end local bw = wrapW / #options for i, o in ipairs(options) do local b = Instance.new("TextButton", wrap) b.Size = UDim2.new(0, bw - 4, 1, 0) b.Position = UDim2.new(0, (i - 1) * bw + (i > 1 and 4 or 0), 0, 0) b.BackgroundColor3 = C.KEY_BG b.BorderSizePixel = 0 b.Text = o b.TextColor3 = Color3.fromRGB(255, 255, 255) b.Font = Enum.Font.GothamBold b.TextSize = 11 b.AutoButtonColor = false Instance.new("UICorner", b).CornerRadius = UDim.new(0, 12) b.MouseButton1Click:Connect(function() if current == o then return end current = o refresh() if onChange then pcall(onChange, o) end if autoSaveConfig then autoSaveConfig() end end) btns[o] = b end refresh() table.insert(SearchIndex, { frame = row, label = label, tab = tab }) return function(v) current = v refresh() end end local function makeToggleRow(tab, label, defaultKey, defaultOn, onToggle, onKeyChanged) local row = Instance.new("Frame", tabPages[tab]) row.Size = UDim2.new(1, 0, 0, 38) row.BackgroundColor3 = C.ROW row.BackgroundTransparency = 0.15 row.LayoutOrder = LO(tab) Instance.new("UICorner", row).CornerRadius = UDim.new(0, 16) local rowStroke = Instance.new("UIStroke", row) rowStroke.Color = C.BORDER rowStroke.Thickness = 1 local leftBar = Instance.new("Frame", row) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) local lbl = Instance.new("TextLabel", row) lbl.Size = UDim2.new(0, 130, 1, 0) lbl.Position = UDim2.new(0, 12, 0, 0) lbl.BackgroundTransparency = 1 lbl.Text = label lbl.TextColor3 = C.ACCENT lbl.Font = Enum.Font.GothamBold lbl.TextSize = 12 lbl.TextXAlignment = Enum.TextXAlignment.Left local keyBtn = nil if defaultKey then keyBtn = Instance.new("TextButton", row) keyBtn.Size = UDim2.new(0, 72, 0, 24) keyBtn.Position = UDim2.new(1, -130, 0.5, -12) keyBtn.BackgroundColor3 = C.KEY_BG keyBtn.BorderSizePixel = 0 keyBtn.Text = defaultKey.Name keyBtn.TextColor3 = Color3.fromRGB(255, 255, 255) keyBtn.Font = Enum.Font.GothamBold keyBtn.TextSize = 10 keyBtn.ZIndex = 5 Instance.new("UICorner", keyBtn).CornerRadius = UDim.new(0, 18) local ks = Instance.new("UIStroke", keyBtn) ks.Color = C.BORDER ks.Thickness = 1 local kL = false local kC local function kStop(key) kL = false if kC then kC:Disconnect() kC = nil end TweenService:Create(ks, TweenInfo.new(0.12), { Color = C.BORDER }):Play() keyBtn.TextColor3 = Color3.fromRGB(255, 255, 255) if key then keyBtn.Text = key.Name if onKeyChanged then onKeyChanged(key) end if autoSaveConfig then autoSaveConfig() end end end keyBtn.MouseButton1Click:Connect(function() if kL then kStop(nil) return end kL = true keyBtn.Text = "..." keyBtn.TextColor3 = C.TEXT TweenService:Create(ks, TweenInfo.new(0.12), { Color = C.ACCENT }):Play() kC = UIS.InputBegan:Connect(function(inp, gp) if not kL then return end if inp.UserInputType == Enum.UserInputType.Keyboard then if inp.KeyCode == Enum.KeyCode.Escape then kStop(nil) return end kStop(inp.KeyCode) elseif inp.UserInputType == Enum.UserInputType.Gamepad1 then if inp.KeyCode == Enum.KeyCode.Escape then kStop(nil) return end kStop(inp.KeyCode) end end) end) end local pillBg = Instance.new("Frame", row) pillBg.Size = UDim2.new(0, 40, 0, 20) pillBg.Position = UDim2.new(1, -46, 0.5, -10) pillBg.BackgroundColor3 = defaultOn and C.ON_BG or C.OFF_BG pillBg.BorderSizePixel = 0 pillBg.ZIndex = 5 Instance.new("UICorner", pillBg).CornerRadius = UDim.new(1, 0) local pStroke = Instance.new("UIStroke", pillBg) pStroke.Color = defaultOn and C.ON_BG or C.BORDER pStroke.Thickness = 1.2 local dot = Instance.new("Frame", pillBg) dot.Size = UDim2.new(0, 14, 0, 14) dot.Position = defaultOn and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7) dot.BackgroundColor3 = Color3.fromRGB(0, 0, 0) dot.BorderSizePixel = 0 dot.ZIndex = 6 Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0) local isOn = defaultOn or false if defaultOn then leftBar.Size = UDim2.new(0, 4, 0.65, 0) leftBar.Position = UDim2.new(0, 0, 0.175, 0) leftBar.BackgroundColor3 = Color3.fromRGB(255, 255, 255) rowStroke.Color = Color3.fromRGB(255, 255, 255) rowStroke.Thickness = 1.2 end local function setV(on) isOn = on TweenService :Create( pillBg, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { BackgroundColor3 = on and C.ON_BG or C.OFF_BG } ) :Play() TweenService:Create(pStroke, TweenInfo.new(0.2), { Color = on and C.ON_BG or C.BORDER }):Play() TweenService:Create(dot, TweenInfo.new(0.2, Enum.EasingStyle.Back), { Position = on and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(0, 0, 0), }):Play() TweenService:Create(leftBar, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { Size = on and UDim2.new(0, 4, 0.65, 0) or UDim2.new(0, 3, 0.5, 0), Position = on and UDim2.new(0, 0, 0.175, 0) or UDim2.new(0, 0, 0.25, 0), BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(80, 80, 80), }):Play() TweenService:Create(rowStroke, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { Color = on and Color3.fromRGB(255, 255, 255) or C.BORDER, Thickness = on and 1.2 or 1, }):Play() end local clk = Instance.new("TextButton", row) clk.Size = UDim2.new(1, 0, 1, 0) clk.BackgroundTransparency = 1 clk.Text = "" clk.ZIndex = 3 clk.MouseButton1Click:Connect(function() isOn = not isOn setV(isOn) if onToggle then pcall(onToggle, isOn) end if autoSaveConfig then autoSaveConfig() end end) if keyBtn then keyBtn.ZIndex = 6 end pillBg.ZIndex = 5 dot.ZIndex = 6 clk.ZIndex = 3 clk.MouseEnter:Connect(function() TweenService:Create(row, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW_HOV }):Play() end) clk.MouseLeave:Connect(function() TweenService:Create(row, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW }):Play() end) table.insert(SearchIndex, { frame = row, label = label, tab = tab, btn = true }) return setV, keyBtn end makeSectionLabel("Speed", "Speed") normalBox = makeInputRow("Speed", "Normal Speed", State.normalSpeed, function(v) local n = tonumber(v) if n and n > 0 then State.normalSpeed = n pcall(function() local c = LP.Character local hum = c and c:FindFirstChildOfClass("Humanoid") if hum and not (VonData and VonData.IsStealing) then hum.WalkSpeed = getCurrentSpeed() end end) end end, true) carryBox = makeInputRow("Speed", "Carry Speed", State.carrySpeed, function(v) local n = tonumber(v) if n and n > 0 then State.carrySpeed = n pcall(function() local c = LP.Character local hum = c and c:FindFirstChildOfClass("Humanoid") if hum and not (VonData and VonData.IsStealing) then hum.WalkSpeed = getCurrentSpeed() end end) end end, true) carryLaggerBox = makeInputRow("Speed", "Lagger Speed", State.laggerSpeed, function(v) local n = tonumber(v) if n and n > 0 then State.laggerSpeed = n State.applyLiveWalkSpeed() end end, true) setSpeedToggleUI, speedKeyBtn = makeToggleRow("Speed", "Speed Toggle (Normal/Carry)", Keys.speed, false, function(on) State.speedType = (on and "carry" or "normal") if on and State.laggerActive then State.laggerActive = false if setLaggerToggleUI then setLaggerToggleUI(false) end end State.applyLiveWalkSpeed() end, function(k) Keys.speed = k end) setLaggerToggleUI, laggerKeyBtn = makeToggleRow("Speed", "Lagger Toggle", Keys.lagger, false, function(on) State.laggerActive = on if on and State.speedType == "carry" then State.speedType = "normal" if setSpeedToggleUI then setSpeedToggleUI(false) end end State.applyLiveWalkSpeed() end, function(k) Keys.lagger = k end) modeValLbl = makeStatusRow("Speed", "Mode", "Normal") makeGap("Speed", 4) makeDivider("Speed") makeGap("Speed", 4) makeSectionLabel("Bat\nAimbot", "Aimbot Settings") setAutoBat, autoBatKeyBtn = makeToggleRow("Bat\nAimbot", "Enable Aimbot", Keys.autoBat, false, function(on) if safeModeBlockOn(on, setAutoBat) then return end State.autoBatToggled = on if on then if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end if bodyLockEnabled then _bodyLockSuppressed = true bodyLockEnabled = false stopBodyLock() if bodyLockSetVisual then bodyLockSetVisual(false) end end startBatAimbot() else stopBatAimbot() if _bodyLockSuppressed then _bodyLockSuppressed = false bodyLockEnabled = true startBodyLock() if bodyLockSetVisual then bodyLockSetVisual(true) end end end end, function(k) Keys.autoBat = k end) makeGap("Bat\nAimbot", 6) setBatCounterUI = makeToggleRow("Bat\nAimbot", "Bat Counter", nil, false, function(on) State.batCounterEnabled = on if on then startBatCounter() else stopBatCounter() end end, nil) do local bbRow = Instance.new("Frame", tabPages["Mechanics"]) bbRow.Size = UDim2.new(1, 0, 0, 38) bbRow.BackgroundColor3 = C.ROW bbRow.BackgroundTransparency = 0.15 bbRow.BorderSizePixel = 0 bbRow.LayoutOrder = LO("Mechanics") Instance.new("UICorner", bbRow).CornerRadius = UDim.new(0, 16) local bbRowStroke = Instance.new("UIStroke", bbRow) bbRowStroke.Color = C.BORDER bbRowStroke.Thickness = 1 local bbBar = Instance.new("Frame", bbRow) bbBar.Size = UDim2.new(0, 3, 0.5, 0) bbBar.Position = UDim2.new(0, 0, 0.25, 0) bbBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) bbBar.BorderSizePixel = 0 bbBar.ZIndex = 4 Instance.new("UICorner", bbBar).CornerRadius = UDim.new(0, 14) local bbLbl = Instance.new("TextLabel", bbRow) bbLbl.Size = UDim2.new(0, 95, 1, 0) bbLbl.Position = UDim2.new(0, 12, 0, 0) bbLbl.BackgroundTransparency = 1 bbLbl.Text = "Bypass Bat" bbLbl.TextColor3 = C.ACCENT bbLbl.Font = Enum.Font.GothamBold bbLbl.TextSize = 12 bbLbl.TextXAlignment = Enum.TextXAlignment.Left local bbModeLbl = Instance.new("TextLabel", bbRow) bbModeLbl.Size = UDim2.new(0, 52, 1, 0) bbModeLbl.Position = UDim2.new(0, 110, 0, 0) bbModeLbl.BackgroundTransparency = 1 bbModeLbl.Text = State.bypassBatMode == "tp" and "TP Bat" or "Bypass" bbModeLbl.TextColor3 = C.DIM bbModeLbl.Font = Enum.Font.GothamBold bbModeLbl.TextSize = 11 bbModeLbl.TextXAlignment = Enum.TextXAlignment.Left local bbDropBtn = Instance.new("TextButton", bbRow) bbDropBtn.Size = UDim2.new(0, 22, 0, 22) bbDropBtn.Position = UDim2.new(0, 165, 0.5, -11) bbDropBtn.BackgroundColor3 = C.KEY_BG bbDropBtn.BorderSizePixel = 0 bbDropBtn.Text = "v" bbDropBtn.TextColor3 = C.ACCENT bbDropBtn.Font = Enum.Font.GothamBold bbDropBtn.TextSize = 11 bbDropBtn.ZIndex = 6 Instance.new("UICorner", bbDropBtn).CornerRadius = UDim.new(0, 6) Instance.new("UIStroke", bbDropBtn).Color = C.BORDER2 local bbPill = Instance.new("Frame", bbRow) bbPill.Size = UDim2.new(0, 40, 0, 20) bbPill.Position = UDim2.new(1, -46, 0.5, -10) bbPill.BackgroundColor3 = C.OFF_BG bbPill.BorderSizePixel = 0 bbPill.ZIndex = 5 Instance.new("UICorner", bbPill).CornerRadius = UDim.new(1, 0) local bbPS = Instance.new("UIStroke", bbPill) bbPS.Color = C.BORDER bbPS.Thickness = 1.2 local bbDot = Instance.new("Frame", bbPill) bbDot.Size = UDim2.new(0, 14, 0, 14) bbDot.Position = UDim2.new(0, 3, 0.5, -7) bbDot.BackgroundColor3 = Color3.fromRGB(0, 0, 0) bbDot.BorderSizePixel = 0 bbDot.ZIndex = 6 Instance.new("UICorner", bbDot).CornerRadius = UDim.new(1, 0) local bbDrop = Instance.new("Frame", tabPages["Mechanics"]) bbDrop.Size = UDim2.new(1, 0, 0, 36) bbDrop.BackgroundColor3 = C.PANEL bbDrop.BackgroundTransparency = 0.3 bbDrop.BorderSizePixel = 0 bbDrop.Visible = false bbDrop.LayoutOrder = LO("Mechanics") Instance.new("UICorner", bbDrop).CornerRadius = UDim.new(0, 12) Instance.new("UIStroke", bbDrop).Color = C.BORDER2 local bbTpBtn = Instance.new("TextButton", bbDrop) bbTpBtn.Size = UDim2.new(0.5, -8, 0, 24) bbTpBtn.Position = UDim2.new(0, 6, 0.5, -12) bbTpBtn.BorderSizePixel = 0 bbTpBtn.Text = "TP Bat" bbTpBtn.Font = Enum.Font.GothamBold bbTpBtn.TextSize = 11 Instance.new("UICorner", bbTpBtn).CornerRadius = UDim.new(0, 10) local bbBypassBtn = Instance.new("TextButton", bbDrop) bbBypassBtn.Size = UDim2.new(0.5, -8, 0, 24) bbBypassBtn.Position = UDim2.new(0.5, 2, 0.5, -12) bbBypassBtn.BorderSizePixel = 0 bbBypassBtn.Text = "Bypass Bat" bbBypassBtn.Font = Enum.Font.GothamBold bbBypassBtn.TextSize = 11 Instance.new("UICorner", bbBypassBtn).CornerRadius = UDim.new(0, 10) local function bbRefreshMode() local isTp = State.bypassBatMode == "tp" TweenService:Create(bbTpBtn, TweenInfo.new(0.12), { BackgroundColor3 = isTp and C.ACCENT or C.KEY_BG }):Play() bbTpBtn.TextColor3 = isTp and Color3.fromRGB(0, 0, 0) or C.TEXT TweenService :Create(bbBypassBtn, TweenInfo.new(0.12), { BackgroundColor3 = (not isTp) and C.ACCENT or C.KEY_BG }) :Play() bbBypassBtn.TextColor3 = (not isTp) and Color3.fromRGB(0, 0, 0) or C.TEXT bbModeLbl.Text = isTp and "TP Bat" or "Bypass" end bbRefreshMode() local function bbStopActive() if State.bypassBatMode == "tp" then stopTPBat() else stopBatV2Aimbot() end end local function bbStartActive() if State.bypassBatMode == "tp" then startTPBat() else startBatV2Aimbot() end end bbTpBtn.MouseButton1Click:Connect(function() if State.bypassBatMode == "tp" then return end if State.batV2Toggled then bbStopActive() end State.bypassBatMode = "tp" bbRefreshMode() if State.batV2Toggled then bbStartActive() end if autoSaveConfig then autoSaveConfig() end end) bbBypassBtn.MouseButton1Click:Connect(function() if State.bypassBatMode == "bypass" then return end if State.batV2Toggled then bbStopActive() end State.bypassBatMode = "bypass" bbRefreshMode() if State.batV2Toggled then bbStartActive() end if autoSaveConfig then autoSaveConfig() end end) local bbOpen = false bbDropBtn.MouseButton1Click:Connect(function() bbOpen = not bbOpen bbDrop.Visible = bbOpen bbDropBtn.Text = bbOpen and "^" or "v" end) local bbIsOn = false local function bbSetV(on) bbIsOn = on State.batV2Toggled = on TweenService :Create( bbPill, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { BackgroundColor3 = on and C.ON_BG or C.OFF_BG } ) :Play() TweenService:Create(bbPS, TweenInfo.new(0.2), { Color = on and C.ON_BG or C.BORDER }):Play() TweenService:Create(bbDot, TweenInfo.new(0.2, Enum.EasingStyle.Back), { Position = on and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(0, 0, 0), }):Play() TweenService:Create(bbBar, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { Size = on and UDim2.new(0, 4, 0.65, 0) or UDim2.new(0, 3, 0.5, 0), Position = on and UDim2.new(0, 0, 0.175, 0) or UDim2.new(0, 0, 0.25, 0), BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(80, 80, 80), }):Play() TweenService:Create(bbRowStroke, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { Color = on and Color3.fromRGB(255, 255, 255) or C.BORDER, Thickness = on and 1.2 or 1, }):Play() end local bbClk = Instance.new("TextButton", bbRow) bbClk.Size = UDim2.new(1, 0, 1, 0) bbClk.BackgroundTransparency = 1 bbClk.Text = "" bbClk.ZIndex = 3 bbClk.MouseButton1Click:Connect(function() if safeModeBlockOn(not bbIsOn, bbSetV) then return end local on = not bbIsOn if on then if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if bodyLockEnabled then _bodyLockSuppressed = true bodyLockEnabled = false stopBodyLock() if bodyLockSetVisual then bodyLockSetVisual(false) end end else if _bodyLockSuppressed then _bodyLockSuppressed = false bodyLockEnabled = true startBodyLock() if bodyLockSetVisual then bodyLockSetVisual(true) end end end bbSetV(on) if on then bbStartActive() else bbStopActive() end if autoSaveConfig then autoSaveConfig() end end) bbDropBtn.ZIndex = 6 bbPill.ZIndex = 5 bbDot.ZIndex = 6 bbClk.ZIndex = 3 bbClk.MouseEnter:Connect(function() TweenService:Create(bbRow, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW_HOV }):Play() end) bbClk.MouseLeave:Connect(function() TweenService:Create(bbRow, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW }):Play() end) table.insert(SearchIndex, { frame = bbRow, label = "Bypass Bat", tab = "Mechanics", btn = true }) setBypassBatUI = function(on) bbSetV(on) if on then bbStartActive() else bbStopActive() end end setBypassBatMode = function(mode) State.bypassBatMode = mode bbRefreshMode() end bypassBatKeyBtn = nil end setBodyLockUI = makeToggleRow("Bat\nAimbot", "Body Lock", nil, false, function(on) bodyLockEnabled = on if on then startBodyLock() else stopBodyLock() end end, nil) bodyLockSetVisual = setBodyLockUI makeInputRow("Bat\nAimbot", "Body Lock Range", bodyLockRange, function(v) local n = tonumber(v) if n and n > 0 then bodyLockRange = math.floor(n) end end) makeGap("Bat\nAimbot", 4) aimbotSpeedBox = makeInputRow("Bat\nAimbot", "Aimbot Speed", State.aimbotSpeed, function(v) local n = tonumber(v) if n and n > 0 then State.aimbotSpeed = n end end) makeGap("Bat\nAimbot", 6) makeSectionLabel("Mechanics", "Auto Grab Settings") stealRadiusBox = nil updateModePills = nil do local modeRow = Instance.new("Frame", tabPages["Mechanics"]) modeRow.Size = UDim2.new(1, 0, 0, 38) modeRow.BackgroundColor3 = C.ROW modeRow.BackgroundTransparency = 0.15 modeRow.BorderSizePixel = 0 modeRow.LayoutOrder = LO("Mechanics") Instance.new("UICorner", modeRow).CornerRadius = UDim.new(0, 16) Instance.new("UIStroke", modeRow).Color = C.BORDER local modeLbl = Instance.new("TextLabel", modeRow) modeLbl.Size = UDim2.new(0, 90, 1, 0) modeLbl.Position = UDim2.new(0, 12, 0, 0) modeLbl.BackgroundTransparency = 1 modeLbl.Text = "Grab Mode" modeLbl.TextColor3 = C.ACCENT modeLbl.Font = Enum.Font.GothamBold modeLbl.TextSize = 12 modeLbl.TextXAlignment = Enum.TextXAlignment.Left local pillContainer = Instance.new("Frame", modeRow) pillContainer.Size = UDim2.new(0, 156, 0, 26) pillContainer.Position = UDim2.new(1, -166, 0.5, -13) pillContainer.BackgroundTransparency = 1 pillContainer.BorderSizePixel = 0 pillContainer.ZIndex = 2 local function makeModePill(label, xPos) local btn = Instance.new("TextButton", pillContainer) btn.Size = UDim2.new(0, 74, 1, 0) btn.Position = UDim2.new(0, xPos, 0, 0) btn.BorderSizePixel = 0 btn.Text = label btn.Font = Enum.Font.GothamSemibold btn.TextSize = 11 btn.ZIndex = 3 btn.AutoButtonColor = false Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8) return btn end local normalModeBtn = makeModePill("Normal", 0) local semiModeBtn = makeModePill("Semi", 80) updateModePills = function() local isNormal = State.stealMode == "Normal" normalModeBtn.BackgroundColor3 = isNormal and C.ACCENT or C.KEY_BG normalModeBtn.TextColor3 = isNormal and C.BG or C.ACCENT2 semiModeBtn.BackgroundColor3 = (not isNormal) and C.ACCENT or C.KEY_BG semiModeBtn.TextColor3 = (not isNormal) and C.BG or C.ACCENT2 end updateModePills() normalModeBtn.MouseButton1Click:Connect(function() if State.stealMode ~= "Normal" then State.stealMode = "Normal" updateModePills() if stealRadiusBox then stealRadiusBox.Text = tostring(State.stealRadii.Normal) end if NormalSteal then NormalSteal.radius = State.stealRadii.Normal end if State.vonEnabled and autoGrabSync then autoGrabSync() end autoSaveConfig() end end) semiModeBtn.MouseButton1Click:Connect(function() if State.stealMode ~= "Semi" then State.stealMode = "Semi" updateModePills() if stealRadiusBox then stealRadiusBox.Text = tostring(State.stealRadii.Semi) end if SemiSteal then SemiSteal.radius = State.stealRadii.Semi end if State.vonEnabled and autoGrabSync then autoGrabSync() end autoSaveConfig() end end) end stealRadiusBox = makeInputRow("Mechanics", "Steal Radius", State.stealRadii[State.stealMode] or 62, function(v) local n = tonumber(v) if n and n >= 1 then State.stealRadii[State.stealMode] = math.floor(n) if State.stealMode == "Normal" and NormalSteal then NormalSteal.radius = State.stealRadii.Normal elseif State.stealMode == "Semi" and SemiSteal then SemiSteal.radius = State.stealRadii.Semi end else if stealRadiusBox then stealRadiusBox.Text = tostring(State.stealRadii[State.stealMode] or (State.stealMode == "Semi" and 9 or 62)) end end end, true) setInstaGrab = makeToggleRow("Mechanics", "Enable Auto Grab", nil, State.vonEnabled, function(on) State.vonEnabled = on if autoGrabSync then autoGrabSync() end end, nil) makeGap("Mechanics", 4) makeDivider("Mechanics") makeGap("Mechanics", 4) makeSectionLabel("Mechanics", "Mechanics") setSafeMode = makeToggleRow("Mechanics", "Safe Mode", nil, State.safeModeEnabled, function(on) State.safeModeEnabled = on if not on and State.safeModeActive then applySafeModeRestore() end end, nil) do local ijRow = Instance.new("Frame", tabPages["Mechanics"]) ijRow.Size = UDim2.new(1, 0, 0, 38) ijRow.BackgroundColor3 = C.ROW ijRow.BackgroundTransparency = 0.15 ijRow.BorderSizePixel = 0 ijRow.LayoutOrder = LO("Mechanics") Instance.new("UICorner", ijRow).CornerRadius = UDim.new(0, 16) Instance.new("UIStroke", ijRow).Color = C.BORDER local ijBar = Instance.new("Frame", ijRow) ijBar.Size = UDim2.new(0, 3, 0.5, 0) ijBar.Position = UDim2.new(0, 0, 0.25, 0) ijBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) ijBar.BorderSizePixel = 0 ijBar.ZIndex = 4 Instance.new("UICorner", ijBar).CornerRadius = UDim.new(0, 14) local ijLbl = Instance.new("TextLabel", ijRow) ijLbl.Size = UDim2.new(0, 95, 1, 0) ijLbl.Position = UDim2.new(0, 12, 0, 0) ijLbl.BackgroundTransparency = 1 ijLbl.Text = "Infinite Jump" ijLbl.TextColor3 = C.ACCENT ijLbl.Font = Enum.Font.GothamBold ijLbl.TextSize = 12 ijLbl.TextXAlignment = Enum.TextXAlignment.Left local ijModeLbl = Instance.new("TextLabel", ijRow) ijModeLbl.Size = UDim2.new(0, 36, 1, 0) ijModeLbl.Position = UDim2.new(0, 112, 0, 0) ijModeLbl.BackgroundTransparency = 1 ijModeLbl.Text = State.infJumpMode == "tap" and "Tap" or "Hold" ijModeLbl.TextColor3 = C.DIM ijModeLbl.Font = Enum.Font.GothamBold ijModeLbl.TextSize = 11 ijModeLbl.TextXAlignment = Enum.TextXAlignment.Left local ijDropBtn = Instance.new("TextButton", ijRow) ijDropBtn.Size = UDim2.new(0, 22, 0, 22) ijDropBtn.Position = UDim2.new(0, 150, 0.5, -11) ijDropBtn.BackgroundColor3 = C.KEY_BG ijDropBtn.BorderSizePixel = 0 ijDropBtn.Text = "v" ijDropBtn.TextColor3 = C.ACCENT ijDropBtn.Font = Enum.Font.GothamBold ijDropBtn.TextSize = 11 ijDropBtn.ZIndex = 6 Instance.new("UICorner", ijDropBtn).CornerRadius = UDim.new(0, 6) Instance.new("UIStroke", ijDropBtn).Color = C.BORDER2 local ijPill = Instance.new("Frame", ijRow) ijPill.Size = UDim2.new(0, 40, 0, 20) ijPill.Position = UDim2.new(1, -46, 0.5, -10) ijPill.BackgroundColor3 = C.OFF_BG ijPill.BorderSizePixel = 0 ijPill.ZIndex = 5 Instance.new("UICorner", ijPill).CornerRadius = UDim.new(1, 0) local ijPS = Instance.new("UIStroke", ijPill) ijPS.Color = C.BORDER ijPS.Thickness = 1.2 local ijDot = Instance.new("Frame", ijPill) ijDot.Size = UDim2.new(0, 14, 0, 14) ijDot.Position = UDim2.new(0, 3, 0.5, -7) ijDot.BackgroundColor3 = Color3.fromRGB(0, 0, 0) ijDot.BorderSizePixel = 0 ijDot.ZIndex = 6 Instance.new("UICorner", ijDot).CornerRadius = UDim.new(1, 0) local ijDrop = Instance.new("Frame", tabPages["Mechanics"]) ijDrop.Size = UDim2.new(1, 0, 0, 36) ijDrop.BackgroundColor3 = C.PANEL ijDrop.BackgroundTransparency = 0.3 ijDrop.BorderSizePixel = 0 ijDrop.Visible = false ijDrop.LayoutOrder = LO("Mechanics") Instance.new("UICorner", ijDrop).CornerRadius = UDim.new(0, 12) Instance.new("UIStroke", ijDrop).Color = C.BORDER2 local ijHoldBtn = Instance.new("TextButton", ijDrop) ijHoldBtn.Size = UDim2.new(0.5, -8, 0, 24) ijHoldBtn.Position = UDim2.new(0, 6, 0.5, -12) ijHoldBtn.BorderSizePixel = 0 ijHoldBtn.Text = "Hold" ijHoldBtn.Font = Enum.Font.GothamBold ijHoldBtn.TextSize = 11 Instance.new("UICorner", ijHoldBtn).CornerRadius = UDim.new(0, 10) local ijTapBtn = Instance.new("TextButton", ijDrop) ijTapBtn.Size = UDim2.new(0.5, -8, 0, 24) ijTapBtn.Position = UDim2.new(0.5, 2, 0.5, -12) ijTapBtn.BorderSizePixel = 0 ijTapBtn.Text = "Tap" ijTapBtn.Font = Enum.Font.GothamBold ijTapBtn.TextSize = 11 Instance.new("UICorner", ijTapBtn).CornerRadius = UDim.new(0, 10) local function ijRefreshMode() local h = State.infJumpMode == "hold" TweenService:Create(ijHoldBtn, TweenInfo.new(0.12), { BackgroundColor3 = h and C.ACCENT or C.KEY_BG }):Play() ijHoldBtn.TextColor3 = h and Color3.fromRGB(0, 0, 0) or C.TEXT TweenService:Create(ijTapBtn, TweenInfo.new(0.12), { BackgroundColor3 = (not h) and C.ACCENT or C.KEY_BG }) :Play() ijTapBtn.TextColor3 = (not h) and Color3.fromRGB(0, 0, 0) or C.TEXT ijModeLbl.Text = h and "Hold" or "Tap" end ijRefreshMode() ijHoldBtn.MouseButton1Click:Connect(function() if State.infJumpMode == "hold" then return end State.infJumpMode = "hold" ijRefreshMode() if autoSaveConfig then autoSaveConfig() end end) ijTapBtn.MouseButton1Click:Connect(function() if State.infJumpMode == "tap" then return end State.infJumpMode = "tap" ijRefreshMode() if autoSaveConfig then autoSaveConfig() end end) local ijOpen = false ijDropBtn.MouseButton1Click:Connect(function() ijOpen = not ijOpen ijDrop.Visible = ijOpen ijDropBtn.Text = ijOpen and "^" or "v" end) local ijIsOn = false local function ijSetV(on) ijIsOn = on State.infJumpEnabled = on TweenService :Create( ijPill, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { BackgroundColor3 = on and C.ON_BG or C.OFF_BG } ) :Play() TweenService:Create(ijPS, TweenInfo.new(0.2), { Color = on and C.ON_BG or C.BORDER }):Play() TweenService:Create(ijDot, TweenInfo.new(0.2, Enum.EasingStyle.Back), { Position = on and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(0, 0, 0), }):Play() TweenService:Create(ijBar, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { Size = on and UDim2.new(0, 4, 0.65, 0) or UDim2.new(0, 3, 0.5, 0), Position = on and UDim2.new(0, 0, 0.175, 0) or UDim2.new(0, 0, 0.25, 0), BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(80, 80, 80), }):Play() end local ijClk = Instance.new("TextButton", ijRow) ijClk.Size = UDim2.new(1, 0, 1, 0) ijClk.BackgroundTransparency = 1 ijClk.Text = "" ijClk.ZIndex = 3 ijClk.MouseButton1Click:Connect(function() ijIsOn = not ijIsOn ijSetV(ijIsOn) if autoSaveConfig then autoSaveConfig() end end) ijDropBtn.ZIndex = 6 ijPill.ZIndex = 5 ijDot.ZIndex = 6 ijClk.ZIndex = 3 ijClk.MouseEnter:Connect(function() TweenService:Create(ijRow, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW_HOV }):Play() end) ijClk.MouseLeave:Connect(function() TweenService:Create(ijRow, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW }):Play() end) table.insert(SearchIndex, { frame = ijRow, label = "Infinite Jump", tab = "Mechanics", btn = true }) setInfJump = ijSetV setInfJumpMode = function(v) State.infJumpMode = (v == "Tap") and "tap" or "hold" ijRefreshMode() end end do local arRow = Instance.new("Frame", tabPages["Mechanics"]) arRow.Size = UDim2.new(1, 0, 0, 38) arRow.BackgroundColor3 = C.ROW arRow.BackgroundTransparency = 0.15 arRow.BorderSizePixel = 0 arRow.LayoutOrder = LO("Mechanics") Instance.new("UICorner", arRow).CornerRadius = UDim.new(0, 16) Instance.new("UIStroke", arRow).Color = C.BORDER local arBar = Instance.new("Frame", arRow) arBar.Size = UDim2.new(0, 3, 0.5, 0) arBar.Position = UDim2.new(0, 0, 0.25, 0) arBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) arBar.BorderSizePixel = 0 arBar.ZIndex = 4 Instance.new("UICorner", arBar).CornerRadius = UDim.new(0, 14) local arLbl = Instance.new("TextLabel", arRow) arLbl.Size = UDim2.new(0, 95, 1, 0) arLbl.Position = UDim2.new(0, 12, 0, 0) arLbl.BackgroundTransparency = 1 arLbl.Text = "Anti Ragdoll" arLbl.TextColor3 = C.ACCENT arLbl.Font = Enum.Font.GothamBold arLbl.TextSize = 12 arLbl.TextXAlignment = Enum.TextXAlignment.Left local arModeLbl = Instance.new("TextLabel", arRow) arModeLbl.Size = UDim2.new(0, 72, 1, 0) arModeLbl.Position = UDim2.new(0, 112, 0, 0) arModeLbl.BackgroundTransparency = 1 arModeLbl.Text = (State.antiRagdollMode == "nosplatter") and "No Splatter" or "Splatter" arModeLbl.TextColor3 = C.DIM arModeLbl.Font = Enum.Font.GothamBold arModeLbl.TextSize = 11 arModeLbl.TextXAlignment = Enum.TextXAlignment.Left local arDropBtn = Instance.new("TextButton", arRow) arDropBtn.Size = UDim2.new(0, 22, 0, 22) arDropBtn.Position = UDim2.new(0, 188, 0.5, -11) arDropBtn.BackgroundColor3 = C.KEY_BG arDropBtn.BorderSizePixel = 0 arDropBtn.Text = "v" arDropBtn.TextColor3 = C.ACCENT arDropBtn.Font = Enum.Font.GothamBold arDropBtn.TextSize = 11 arDropBtn.ZIndex = 6 Instance.new("UICorner", arDropBtn).CornerRadius = UDim.new(0, 6) Instance.new("UIStroke", arDropBtn).Color = C.BORDER2 local arPill = Instance.new("Frame", arRow) arPill.Size = UDim2.new(0, 40, 0, 20) arPill.Position = UDim2.new(1, -46, 0.5, -10) arPill.BackgroundColor3 = C.OFF_BG arPill.BorderSizePixel = 0 arPill.ZIndex = 5 Instance.new("UICorner", arPill).CornerRadius = UDim.new(1, 0) local arPS = Instance.new("UIStroke", arPill) arPS.Color = C.BORDER arPS.Thickness = 1.2 local arDot = Instance.new("Frame", arPill) arDot.Size = UDim2.new(0, 14, 0, 14) arDot.Position = UDim2.new(0, 3, 0.5, -7) arDot.BackgroundColor3 = Color3.fromRGB(0, 0, 0) arDot.BorderSizePixel = 0 arDot.ZIndex = 6 Instance.new("UICorner", arDot).CornerRadius = UDim.new(1, 0) local arDrop = Instance.new("Frame", tabPages["Mechanics"]) arDrop.Size = UDim2.new(1, 0, 0, 36) arDrop.BackgroundColor3 = C.PANEL arDrop.BackgroundTransparency = 0.3 arDrop.BorderSizePixel = 0 arDrop.Visible = false arDrop.LayoutOrder = LO("Mechanics") Instance.new("UICorner", arDrop).CornerRadius = UDim.new(0, 12) Instance.new("UIStroke", arDrop).Color = C.BORDER2 local arSplatterBtn = Instance.new("TextButton", arDrop) arSplatterBtn.Size = UDim2.new(0.5, -8, 0, 24) arSplatterBtn.Position = UDim2.new(0, 6, 0.5, -12) arSplatterBtn.BorderSizePixel = 0 arSplatterBtn.Text = "Splatter" arSplatterBtn.Font = Enum.Font.GothamBold arSplatterBtn.TextSize = 11 Instance.new("UICorner", arSplatterBtn).CornerRadius = UDim.new(0, 10) local arNoSplatterBtn = Instance.new("TextButton", arDrop) arNoSplatterBtn.Size = UDim2.new(0.5, -8, 0, 24) arNoSplatterBtn.Position = UDim2.new(0.5, 2, 0.5, -12) arNoSplatterBtn.BorderSizePixel = 0 arNoSplatterBtn.Text = "No Splatter" arNoSplatterBtn.Font = Enum.Font.GothamBold arNoSplatterBtn.TextSize = 11 Instance.new("UICorner", arNoSplatterBtn).CornerRadius = UDim.new(0, 10) local function arRefreshMode() local s = State.antiRagdollMode ~= "nosplatter" TweenService:Create(arSplatterBtn, TweenInfo.new(0.12), { BackgroundColor3 = s and C.ACCENT or C.KEY_BG }):Play() arSplatterBtn.TextColor3 = s and Color3.fromRGB(0, 0, 0) or C.TEXT TweenService:Create(arNoSplatterBtn, TweenInfo.new(0.12), { BackgroundColor3 = (not s) and C.ACCENT or C.KEY_BG }):Play() arNoSplatterBtn.TextColor3 = (not s) and Color3.fromRGB(0, 0, 0) or C.TEXT arModeLbl.Text = s and "Splatter" or "No Splatter" end arRefreshMode() arSplatterBtn.MouseButton1Click:Connect(function() if State.antiRagdollMode == "splatter" then return end State.antiRagdollMode = "splatter" arRefreshMode() if autoSaveConfig then autoSaveConfig() end end) arNoSplatterBtn.MouseButton1Click:Connect(function() if State.antiRagdollMode == "nosplatter" then return end State.antiRagdollMode = "nosplatter" arRefreshMode() if autoSaveConfig then autoSaveConfig() end end) local arOpen = false arDropBtn.MouseButton1Click:Connect(function() arOpen = not arOpen arDrop.Visible = arOpen arDropBtn.Text = arOpen and "^" or "v" end) local arIsOn = false local function arSetV(on) arIsOn = on State.antiRagdollEnabled = on TweenService:Create(arPill, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { BackgroundColor3 = on and C.ON_BG or C.OFF_BG, }):Play() TweenService:Create(arPS, TweenInfo.new(0.2), { Color = on and C.ON_BG or C.BORDER }):Play() TweenService:Create(arDot, TweenInfo.new(0.2, Enum.EasingStyle.Back), { Position = on and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = Color3.fromRGB(0, 0, 0), }):Play() TweenService:Create(arBar, TweenInfo.new(0.2, Enum.EasingStyle.Quad), { Size = on and UDim2.new(0, 4, 0.65, 0) or UDim2.new(0, 3, 0.5, 0), Position = on and UDim2.new(0, 0, 0.175, 0) or UDim2.new(0, 0, 0.25, 0), BackgroundColor3 = on and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(80, 80, 80), }):Play() if on then if startAntiRagdoll then startAntiRagdoll() end else if stopAntiRagdoll then stopAntiRagdoll() end end end local arClk = Instance.new("TextButton", arRow) arClk.Size = UDim2.new(1, 0, 1, 0) arClk.BackgroundTransparency = 1 arClk.Text = "" arClk.ZIndex = 3 arClk.MouseButton1Click:Connect(function() arIsOn = not arIsOn arSetV(arIsOn) if autoSaveConfig then autoSaveConfig() end end) arDropBtn.ZIndex = 6 arPill.ZIndex = 5 arDot.ZIndex = 6 arClk.ZIndex = 3 arClk.MouseEnter:Connect(function() TweenService:Create(arRow, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW_HOV }):Play() end) arClk.MouseLeave:Connect(function() TweenService:Create(arRow, TweenInfo.new(0.1), { BackgroundColor3 = C.ROW }):Play() end) table.insert(SearchIndex, { frame = arRow, label = "Anti Ragdoll", tab = "Mechanics", btn = true }) setAntiRag = arSetV setAntiRagMode = function(v) State.antiRagdollMode = (v == "No Splatter" or v == "nosplatter") and "nosplatter" or "splatter" arRefreshMode() end end setMedusaCounter = makeToggleRow("Mechanics", "Medusa Counter", nil, false, function(on) State.medusaCounterEnabled = on if on then setupMedusaCounter(LP.Character) else stopMedusaCounter() end end) setAutoMedusaResetUI, autoMedusaResetKeyBtn = makeToggleRow( "Mechanics", "Auto Reset After Medusa", Keys.autoMedusaReset, false, function(on) State.autoMedusaResetEnabled = on if on then startAutoMedusaReset(LP.Character) else stopAutoMedusaReset() end end, function(k) Keys.autoMedusaReset = k end ) setAnimToggle = makeToggleRow("Mechanics", "Tryhard Anim", nil, false, function(on) State.animEnabled = on if on then startAnimToggle() else stopAnimToggle() end end) setUnwalkToggle = makeToggleRow("Mechanics", "Unwalk", nil, false, function(on) if on then startUnwalk() else stopUnwalk() end end) makeGap("Mechanics", 4) makeDivider("Mechanics") makeGap("Mechanics", 4) makeGap("Mechanics", 6) makeSectionLabel("Movement", "Movement") setAutoLeft, autoLeftKeyBtn = makeToggleRow("Movement", "Auto Left", Keys.autoLeft, false, function(on) if safeModeBlockOn(on, setAutoLeft) then return end State.autoLeftEnabled = on if on then if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end startAutoLeft() else stopAutoLeft() end end, function(k) Keys.autoLeft = k end) setAutoRight, autoRightKeyBtn = makeToggleRow("Movement", "Auto Right", Keys.autoRight, false, function(on) if safeModeBlockOn(on, setAutoRight) then return end State.autoRightEnabled = on if on then if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end startAutoRight() else stopAutoRight() end end, function(k) Keys.autoRight = k end) makeGap("Movement", 4) makeDivider("Movement") makeGap("Movement", 4) makeSectionLabel("Movement", "Teleport") setAutoTpDown = makeToggleRow("Movement", "Auto TP Down (Height)", nil, false, function(on) State.autoTpDownEnabled = on end, nil) autoTpDownYBox = makeInputRow("Movement", "TP Down Height (Y)", State.autoTpDownY, function(v) local n = tonumber(v) if n then State.autoTpDownY = n if autoSaveConfig then autoSaveConfig() end end end) makeGap("Movement", 4) tpDownKeyBtn = makeKeybindRow("Movement", "TP Down (Ground)", Keys.tpDown, function(k) Keys.tpDown = k end) dropBrainrotKeyBtn = makeKeybindRow("Movement", "Drop Brainrot", Keys.dropBrainrot, function(k) Keys.dropBrainrot = k end) makeGap("Movement", 6) makeSectionLabel("Settings", "Visuals & FPS") setIntroToggle = makeToggleRow("Settings", "Intro Music", nil, State.introEnabled, function(on) State.introEnabled = on if not on and _activeIntroSound then pcall(function() _activeIntroSound:Stop() _activeIntroSound:Destroy() end) _activeIntroSound = nil end autoSaveConfig() end) makeGap("Settings", 4) setDarkMode = makeToggleRow("Settings", "Dark Mode", nil, false, function(on) State.darkModeEnabled = on applyDarkMode() end) State._espSetter = makeToggleRow("Settings", "ESP + Tracers", nil, false, function(on) espEnabled = on if on then startPlayerESP() BoxedESPOptions.box = true BoxedESPOptions.tracer = true else stopPlayerESP() BoxedESPOptions.box = false BoxedESPOptions.tracer = false end refreshBoxedESP() autoSaveConfig() end) setPurpleSky = makeToggleRow("Settings", "Purple Sky", nil, false, function(on) State.purpleSkyEnabled = on applyPurpleSky() end) makeGap("Settings", 4) setFps = makeToggleRow("Settings", "FPS Boost", nil, false, function(on) State.fpsBoostEnabled = on if on then pcall(applyFPSBoost) end end) makeGap("Settings", 4) setStretchRez = makeToggleRow("Settings", "Stretch Rez", nil, false, function(on) State.stretchRezEnabled = on if on then enableStretchRez() else disableStretchRez() end end) makeGap("Settings", 4) makeGap("Settings", 6) makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) makeSectionLabel("Settings", "Mobile Button Size") mbSizeRow = Instance.new("Frame", tabPages["Settings"]) mbSizeRow.Size = UDim2.new(1, 0, 0, 42) mbSizeRow.BackgroundColor3 = C.ROW mbSizeRow.BackgroundTransparency = 0.15 mbSizeRow.BorderSizePixel = 0 mbSizeRow.LayoutOrder = LO("Settings") Instance.new("UICorner", mbSizeRow).CornerRadius = UDim.new(0, 16) mbSizeRowStroke = Instance.new("UIStroke", mbSizeRow) mbSizeRowStroke.Color = C.BORDER mbSizeRowStroke.Thickness = 1 mbSizeLbl = Instance.new("TextLabel", mbSizeRow) mbSizeLbl.Size = UDim2.new(0.55, 0, 1, 0) mbSizeLbl.Position = UDim2.new(0, 12, 0, 0) mbSizeLbl.BackgroundTransparency = 1 mbSizeLbl.Text = "Button Size" mbSizeLbl.TextColor3 = Color3.fromRGB(255, 255, 255) mbSizeLbl.Font = Enum.Font.GothamBold mbSizeLbl.TextSize = 12 mbSizeLbl.TextXAlignment = Enum.TextXAlignment.Left mbSizeLbl.TextStrokeTransparency = 0.4 mbSizeLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) mbSizeLbl.ZIndex = 2 mbSizeBox = Instance.new("TextBox", mbSizeRow) mbSizeBox.Size = UDim2.new(0, 90, 0, 34) mbSizeBox.Position = UDim2.new(1, -96, 0.5, -17) mbSizeBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0) mbSizeBox.BorderSizePixel = 0 mbSizeBox.Text = tostring(MB_BTN_W) mbSizeBox.TextColor3 = Color3.fromRGB(255, 255, 255) mbSizeBox.Font = Enum.Font.Gotham mbSizeBox.TextSize = 14 mbSizeBox.ClearTextOnFocus = true mbSizeBox.PlaceholderText = "55" mbSizeBox.ZIndex = 3 Instance.new("UICorner", mbSizeBox).CornerRadius = UDim.new(0, 16) mbSizeBoxStroke = Instance.new("UIStroke", mbSizeBox) mbSizeBoxStroke.Color = Color3.fromRGB(255, 255, 255) mbSizeBoxStroke.Thickness = 1.2 mbSizeBoxStroke.Transparency = 0.3 mbSizeBox.InputBegan:Connect(function(inp) inp:StopPropagation() end) mbSizeBox.Focused:Connect(function() TweenService:Create(mbSizeBoxStroke, TweenInfo.new(0.15), { Color = C.ACCENT }):Play() mbSizeBox.BackgroundColor3 = Color3.fromRGB(50, 50, 55) end) local function updateMbSize(w, h) MB_BTN_W = math.clamp(w, 40, 100) MB_BTN_H = math.clamp(h, 36, 90) MB_TOTAL_W = MB_COLS * MB_BTN_W + (MB_COLS + 1) * MB_PAD MB_TOTAL_H = MB_ROWS * MB_BTN_H + (MB_ROWS + 1) * MB_PAD mbSizeBox.Text = tostring(MB_BTN_W) if mbPanel then mbPanel.Size = UDim2.new(0, MB_TOTAL_W, 0, MB_TOTAL_H) repositionMbButtons() end if autoSaveConfig then autoSaveConfig() end end mbSizeBox.FocusLost:Connect(function() TweenService:Create(mbSizeBoxStroke, TweenInfo.new(0.15), { Color = C.BORDER }):Play() mbSizeBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0) local n = tonumber(mbSizeBox.Text) if n then updateMbSize(math.floor(n), math.floor(n)) else mbSizeBox.Text = tostring(MB_BTN_W) end end) mbSizeBox.Active = true mbSizeBox.Selectable = true makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) makeSectionLabel("Settings", "UI Scale") uiScaleRow = Instance.new("Frame", tabPages["Settings"]) uiScaleRow.Size = UDim2.new(1, 0, 0, 42) uiScaleRow.BackgroundColor3 = C.ROW uiScaleRow.BackgroundTransparency = 0.15 uiScaleRow.BorderSizePixel = 0 uiScaleRow.LayoutOrder = LO("Settings") Instance.new("UICorner", uiScaleRow).CornerRadius = UDim.new(0, 16) uiScaleRowStroke = Instance.new("UIStroke", uiScaleRow) uiScaleRowStroke.Color = C.BORDER uiScaleRowStroke.Thickness = 1 uiScaleLbl = Instance.new("TextLabel", uiScaleRow) uiScaleLbl.Size = UDim2.new(0.55, 0, 1, 0) uiScaleLbl.Position = UDim2.new(0, 12, 0, 0) uiScaleLbl.BackgroundTransparency = 1 uiScaleLbl.Text = "Menu Scale" uiScaleLbl.TextColor3 = Color3.fromRGB(255, 255, 255) uiScaleLbl.Font = Enum.Font.GothamBold uiScaleLbl.TextSize = 12 uiScaleLbl.TextXAlignment = Enum.TextXAlignment.Left uiScaleLbl.TextStrokeTransparency = 0.4 uiScaleLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0) uiScaleLbl.ZIndex = 2 uiScaleBox = Instance.new("TextBox", uiScaleRow) uiScaleBox.Size = UDim2.new(0, 90, 0, 34) uiScaleBox.Position = UDim2.new(1, -96, 0.5, -17) uiScaleBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0) uiScaleBox.BorderSizePixel = 0 uiScaleBox.Text = tostring(math.floor(GUI_SCALE * 100 + 0.5)) uiScaleBox.TextColor3 = Color3.fromRGB(255, 255, 255) uiScaleBox.Font = Enum.Font.Gotham uiScaleBox.TextSize = 14 uiScaleBox.ClearTextOnFocus = true uiScaleBox.PlaceholderText = "80" uiScaleBox.ZIndex = 3 Instance.new("UICorner", uiScaleBox).CornerRadius = UDim.new(0, 16) uiScaleBoxStroke = Instance.new("UIStroke", uiScaleBox) uiScaleBoxStroke.Color = Color3.fromRGB(255, 255, 255) uiScaleBoxStroke.Thickness = 1.2 uiScaleBoxStroke.Transparency = 0.3 uiScaleBox.InputBegan:Connect(function(inp) inp:StopPropagation() end) uiScaleBox.Focused:Connect(function() TweenService:Create(uiScaleBoxStroke, TweenInfo.new(0.15), { Color = C.ACCENT }):Play() uiScaleBox.BackgroundColor3 = Color3.fromRGB(50, 50, 55) end) uiScaleBox.FocusLost:Connect(function() TweenService:Create(uiScaleBoxStroke, TweenInfo.new(0.15), { Color = C.BORDER }):Play() uiScaleBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0) local n = tonumber(uiScaleBox.Text) if n then local pct = math.clamp(math.floor(n + 0.5), 50, 300) GUI_SCALE = pct / 100 uiScaleBox.Text = tostring(pct) guiScaleObj.Scale = GUI_SCALE updateClosePos() if autoSaveConfig then autoSaveConfig() end else uiScaleBox.Text = tostring(math.floor(GUI_SCALE * 100 + 0.5)) end end) uiScaleBox.Active = true uiScaleBox.Selectable = true makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) makeSectionLabel("Settings", "Mobile Panel") local setMobileVisible = makeToggleRow("Settings", "Show Buttons", nil, State.mobileVisible, function(on) State.mobileVisible = on if mbPanel then mbPanel.Visible = on end end, nil) local setMobileLocked = makeToggleRow("Settings", "Lock Panel", nil, State.mobileLocked, function(on) State.mobileLocked = on end, nil) local setMobileDrag = makeToggleRow("Settings", "Drag Buttons", nil, State.mobileDragEnabled, function(on) State.mobileDragEnabled = on end, nil) makeGap("Settings", 4) makeActionBtn("Settings", "Reset Button Positions", function() State.mbBtnPositions = {} State.mbPanelPos = nil pcall(function() writefile("SpeedHubBtnPos.json", "{}") end) pcall(function() writefile("SpeedHubMobilePos.txt", "") end) pcall(function() writefile("SpeedHubStealPanelPos.txt", "") end) if mbPanel then mbPanel.Position = UDim2.new( 1, -(MB_COLS * MB_BTN_W + (MB_COLS + 1) * MB_PAD + 8), 0.5, -((MB_ROWS * MB_BTN_H + (MB_ROWS + 1) * MB_PAD) / 2) ) repositionMbButtons() end if stealPanelFrame then stealPanelFrame.Position = UDim2.new(0.5, -150, 1, -48) local _genv = pcall(getgenv) and getgenv() or _G _genv.SpeedHubVonPanelPos = nil pcall(function() writefile("SpeedHubStealPanelPos.txt", "") end) end if autoSaveConfig then autoSaveConfig() end end) makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) makeSectionLabel("Settings", "Camera") fovBox = makeInputRow("Settings", "Field of View", State.currentFOV, function(v) local n = tonumber(v) if n then State.currentFOV = n pcall(function() workspace.CurrentCamera.FieldOfView = n end) end end) makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) makeSectionLabel("Settings", "Visibility") guiHideKeyBtn = makeKeybindRow("Settings", "Hide / Show GUI", Keys.guiHide, function(k) Keys.guiHide = k end) makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) makeGap("Settings", 4) makeDivider("Settings") makeGap("Settings", 4) local function loadConfig() local s pcall(function() s = readfile("SpeedHubConfig.json") end) if not s or s == "" then pcall(function() s = readfile("DIVINECCConfig.json") end) end if not s or s == "" then return end local ok, cfg = pcall(function() return HttpService:JSONDecode(s) end) if not ok or not cfg then return end isLoadingConfig = true local success, err = pcall(function() if type(cfg.uiScale) == "number" then GUI_SCALE = math.clamp(cfg.uiScale, 0.5, 3) if guiScaleObj then guiScaleObj.Scale = GUI_SCALE end if uiScaleBox then uiScaleBox.Text = tostring(math.floor(GUI_SCALE * 100 + 0.5)) end pcall(updateClosePos) end if cfg.introEnabled ~= nil then State.introEnabled = cfg.introEnabled if setIntroToggle then setIntroToggle(cfg.introEnabled) end end if cfg.darkModeEnabled ~= nil then State.darkModeEnabled = cfg.darkModeEnabled if setDarkMode then setDarkMode(State.darkModeEnabled) end pcall(applyDarkMode) end if cfg.purpleSkyEnabled ~= nil then State.purpleSkyEnabled = cfg.purpleSkyEnabled if setPurpleSky then setPurpleSky(State.purpleSkyEnabled) end pcall(applyPurpleSky) end if cfg.espEnabled ~= nil then espEnabled = cfg.espEnabled if State._espSetter then State._espSetter(cfg.espEnabled) end if cfg.espEnabled then startPlayerESP() BoxedESPOptions.box = true BoxedESPOptions.tracer = true else stopPlayerESP() BoxedESPOptions.box = false BoxedESPOptions.tracer = false end pcall(refreshBoxedESP) end if cfg.mbBtnPositions and type(cfg.mbBtnPositions) == "table" then State.mbBtnPositions = cfg.mbBtnPositions end if cfg.mbPanelPos then State.mbPanelPos = cfg.mbPanelPos end if cfg.normalSpeed then State.normalSpeed = cfg.normalSpeed if normalBox then normalBox.Text = formatSpeedNum(cfg.normalSpeed) end end if cfg.carrySpeed then State.carrySpeed = cfg.carrySpeed if carryBox then carryBox.Text = formatSpeedNum(cfg.carrySpeed) end end if cfg.laggerCarrySpeed then State.laggerCarrySpeed = cfg.laggerCarrySpeed end if cfg.laggerSpeed then State.laggerSpeed = cfg.laggerSpeed if carryLaggerBox then carryLaggerBox.Text = formatSpeedNum(cfg.laggerSpeed) end end if cfg.speedType then State.speedType = cfg.speedType if setSpeedToggleUI then setSpeedToggleUI(cfg.speedType == "carry") end if modeValLbl then modeValLbl.Text = cfg.speedType == "carry" and "Carry" or "Normal" end end State.laggerActive = false if setLaggerToggleUI then setLaggerToggleUI(false) end if cfg.vonRadius then State.vonRadius = cfg.vonRadius if radiusBox then radiusBox.Text = tostring(cfg.vonRadius) end end if cfg.vonDelay then State.vonDelay = cfg.vonDelay if delayBox then delayBox.Text = tostring(cfg.vonDelay) end end if cfg.stealMode == "Semi" or cfg.stealMode == "Normal" then State.stealMode = cfg.stealMode end if cfg.stealRadiiNormal then State.stealRadii.Normal = cfg.stealRadiiNormal end if cfg.stealRadiiSemi then State.stealRadii.Semi = cfg.stealRadiiSemi end if updateModePills then updateModePills() end if stealRadiusBox then stealRadiusBox.Text = tostring(State.stealRadii[State.stealMode] or (State.stealMode == "Semi" and 9 or 62)) end if cfg.vonEnabled ~= nil then State.vonEnabled = cfg.vonEnabled if setInstaGrab then setInstaGrab(cfg.vonEnabled) end end if cfg.infJump then State.infJumpEnabled = cfg.infJump if setInfJump then setInfJump(cfg.infJump) end end if cfg.infJumpMode == "tap" or cfg.infJumpMode == "hold" then State.infJumpMode = cfg.infJumpMode if setInfJumpMode then setInfJumpMode(cfg.infJumpMode == "tap" and "Tap" or "Hold") end end if cfg.antiRagdollMode == "splatter" or cfg.antiRagdollMode == "nosplatter" then State.antiRagdollMode = cfg.antiRagdollMode if setAntiRagMode then setAntiRagMode(cfg.antiRagdollMode == "nosplatter" and "No Splatter" or "Splatter") end end if cfg.antiRagdoll then State.antiRagdollEnabled = cfg.antiRagdoll if setAntiRag then setAntiRag(cfg.antiRagdoll) end if cfg.antiRagdoll then pcall(startAntiRagdoll) end end if cfg.fpsBoost then State.fpsBoostEnabled = cfg.fpsBoost if setFps then setFps(cfg.fpsBoost) end if cfg.fpsBoost then pcall(applyFPSBoost) end end if cfg.stretchRezEnabled ~= nil then State.stretchRezEnabled = cfg.stretchRezEnabled if setStretchRez then setStretchRez(cfg.stretchRezEnabled) end if cfg.stretchRezEnabled then pcall(enableStretchRez) else pcall(disableStretchRez) end end if cfg.medusaCounter then State.medusaCounterEnabled = cfg.medusaCounter if setMedusaCounter then setMedusaCounter(cfg.medusaCounter) end end if cfg.batCounterEnabled ~= nil then State.batCounterEnabled = cfg.batCounterEnabled if setBatCounterUI then setBatCounterUI(cfg.batCounterEnabled) end if cfg.batCounterEnabled then pcall(startBatCounter) end end if cfg.autoBatKey and Enum.KeyCode[cfg.autoBatKey] then Keys.autoBat = Enum.KeyCode[cfg.autoBatKey] if autoBatKeyBtn then autoBatKeyBtn.Text = cfg.autoBatKey end end if cfg.speedKey and Enum.KeyCode[cfg.speedKey] then Keys.speed = Enum.KeyCode[cfg.speedKey] if speedKeyBtn then speedKeyBtn.Text = cfg.speedKey end end if cfg.laggerKey and Enum.KeyCode[cfg.laggerKey] then Keys.lagger = Enum.KeyCode[cfg.laggerKey] if laggerKeyBtn then laggerKeyBtn.Text = cfg.laggerKey end end if cfg.dropBrainrotKey and Enum.KeyCode[cfg.dropBrainrotKey] then Keys.dropBrainrot = Enum.KeyCode[cfg.dropBrainrotKey] if dropBrainrotKeyBtn then dropBrainrotKeyBtn.Text = cfg.dropBrainrotKey end end if cfg.autoLeftKey and Enum.KeyCode[cfg.autoLeftKey] then Keys.autoLeft = Enum.KeyCode[cfg.autoLeftKey] if autoLeftKeyBtn then autoLeftKeyBtn.Text = cfg.autoLeftKey end end if cfg.autoRightKey and Enum.KeyCode[cfg.autoRightKey] then Keys.autoRight = Enum.KeyCode[cfg.autoRightKey] if autoRightKeyBtn then autoRightKeyBtn.Text = cfg.autoRightKey end end if cfg.guiHideKey and Enum.KeyCode[cfg.guiHideKey] then Keys.guiHide = Enum.KeyCode[cfg.guiHideKey] if guiHideKeyBtn then guiHideKeyBtn.Text = cfg.guiHideKey end end if cfg.tpDownKey and Enum.KeyCode[cfg.tpDownKey] then Keys.tpDown = Enum.KeyCode[cfg.tpDownKey] if tpDownKeyBtn then tpDownKeyBtn.Text = cfg.tpDownKey end end if cfg.instaResetKey and Enum.KeyCode[cfg.instaResetKey] then Keys.instaReset = Enum.KeyCode[cfg.instaResetKey] if instaResetKeyBtn then instaResetKeyBtn.Text = cfg.instaResetKey end end if cfg.batV2Key and Enum.KeyCode[cfg.batV2Key] then Keys.batV2 = Enum.KeyCode[cfg.batV2Key] if bypassBatKeyBtn then bypassBatKeyBtn.Text = cfg.batV2Key end end if cfg.bypassBatMode == "tp" or cfg.bypassBatMode == "bypass" then State.bypassBatMode = cfg.bypassBatMode if setBypassBatMode then setBypassBatMode(cfg.bypassBatMode) end end if cfg.batV2Enabled ~= nil then State.batV2Toggled = cfg.batV2Enabled if setBypassBatUI then setBypassBatUI(cfg.batV2Enabled) end end if cfg.autoMedusaResetKey and Enum.KeyCode[cfg.autoMedusaResetKey] then Keys.autoMedusaReset = Enum.KeyCode[cfg.autoMedusaResetKey] if autoMedusaResetKeyBtn then autoMedusaResetKeyBtn.Text = cfg.autoMedusaResetKey end end if cfg.autoMedusaResetEnabled ~= nil then State.autoMedusaResetEnabled = cfg.autoMedusaResetEnabled if setAutoMedusaResetUI then setAutoMedusaResetUI(cfg.autoMedusaResetEnabled) end if cfg.autoMedusaResetEnabled then pcall(function() startAutoMedusaReset(LP.Character) end) end end if cfg.aimbotSpeed then State.aimbotSpeed = cfg.aimbotSpeed if aimbotSpeedBox then aimbotSpeedBox.Text = formatSpeedNum(cfg.aimbotSpeed) end end if cfg.mobileDragEnabled ~= nil then State.mobileDragEnabled = cfg.mobileDragEnabled if setMobileDrag then setMobileDrag(cfg.mobileDragEnabled) end end if cfg.animEnabled then State.animEnabled = cfg.animEnabled if setAnimToggle then setAnimToggle(cfg.animEnabled) end if cfg.animEnabled then pcall(startAnimToggle) end end if cfg.unwalkEnabled then if setUnwalkToggle then setUnwalkToggle(true) end pcall(startUnwalk) end if cfg.mobileVisible ~= nil then State.mobileVisible = cfg.mobileVisible if setMobileVisible then setMobileVisible(cfg.mobileVisible) end if mbPanel then mbPanel.Visible = cfg.mobileVisible end end if cfg.mobileLocked ~= nil then State.mobileLocked = cfg.mobileLocked if setMobileLocked then setMobileLocked(cfg.mobileLocked) end end if cfg.mbBtnW and cfg.mbBtnH then pcall(function() updateMbSize(cfg.mbBtnW, cfg.mbBtnH) end) end if cfg.currentFOV then State.currentFOV = cfg.currentFOV if fovBox then fovBox.Text = tostring(cfg.currentFOV) end pcall(function() workspace.CurrentCamera.FieldOfView = cfg.currentFOV end) end if cfg.autoTpDownEnabled ~= nil then State.autoTpDownEnabled = cfg.autoTpDownEnabled if setAutoTpDown then setAutoTpDown(cfg.autoTpDownEnabled) end end if cfg.autoTpDownY then State.autoTpDownY = cfg.autoTpDownY if autoTpDownYBox then autoTpDownYBox.Text = tostring(cfg.autoTpDownY) end end if cfg.safeModeEnabled ~= nil then State.safeModeEnabled = cfg.safeModeEnabled if setSafeMode then setSafeMode(cfg.safeModeEnabled) end end if cfg.bodyLockRange then bodyLockRange = cfg.bodyLockRange end if cfg.bodyLockEnabled then bodyLockEnabled = true if setBodyLockUI then setBodyLockUI(true) end pcall(startBodyLock) end end) isLoadingConfig = false if not success then warn("[SpeedHub] loadConfig partial error: " .. tostring(err)) end task.defer(function() isLoadingConfig = false pcall(function() if autoSaveConfig then autoSaveConfig() end end) end) end autoSaveConfig = function() if isLoadingConfig then return end local function keyName(k) local ok, n = pcall(function() return k and k.Name end) return (ok and n) or nil end local cfg = { normalSpeed = State.normalSpeed, carrySpeed = State.carrySpeed, laggerSpeed = State.laggerSpeed, laggerCarrySpeed = State.laggerCarrySpeed, speedType = State.speedType, laggerActive = State.laggerActive, autoBatKey = keyName(Keys.autoBat), speedKey = keyName(Keys.speed), laggerKey = keyName(Keys.lagger), vonRadius = State.vonRadius, vonDelay = State.vonDelay, vonEnabled = State.vonEnabled, stealMode = State.stealMode, stealRadiiNormal = (State.stealRadii and State.stealRadii.Normal) or 62, stealRadiiSemi = (State.stealRadii and State.stealRadii.Semi) or 9, infJump = State.infJumpEnabled, infJumpMode = State.infJumpMode, antiRagdoll = State.antiRagdollEnabled, antiRagdollMode = State.antiRagdollMode, fpsBoost = State.fpsBoostEnabled, medusaCounter = State.medusaCounterEnabled, batCounterEnabled = State.batCounterEnabled, aimbotSpeed = State.aimbotSpeed, batV2Key = keyName(Keys.batV2), batV2Enabled = State.batV2Toggled, bypassBatMode = State.bypassBatMode, autoMedusaResetKey = keyName(Keys.autoMedusaReset), autoMedusaResetEnabled = State.autoMedusaResetEnabled, mobileDragEnabled = State.mobileDragEnabled, dropBrainrotKey = keyName(Keys.dropBrainrot), autoLeftKey = keyName(Keys.autoLeft), autoRightKey = keyName(Keys.autoRight), guiHideKey = keyName(Keys.guiHide), animEnabled = State.animEnabled, unwalkEnabled = State.unwalkEnabled, tpDownKey = keyName(Keys.tpDown), instaResetKey = keyName(Keys.instaReset), autoLeftEnabled = State.autoLeftEnabled, autoRightEnabled = State.autoRightEnabled, mobileVisible = State.mobileVisible, mobileLocked = State.mobileLocked, mbBtnW = MB_BTN_W, mbBtnH = MB_BTN_H, darkModeEnabled = State.darkModeEnabled, purpleSkyEnabled = State.purpleSkyEnabled, introEnabled = State.introEnabled, currentFOV = State.currentFOV, espEnabled = espEnabled, espSpeedEnabled = State.espSpeedEnabled, autoTpDownEnabled = State.autoTpDownEnabled, autoTpDownY = State.autoTpDownY, safeModeEnabled = State.safeModeEnabled, stretchRezEnabled = State.stretchRezEnabled, bodyLockEnabled = bodyLockEnabled, bodyLockRange = bodyLockRange, uiScale = GUI_SCALE, mbBtnPositions = State.mbBtnPositions, mbPanelPos = State.mbPanelPos, } local okWrite, enc = pcall(function() return HttpService:JSONEncode(cfg) end) if okWrite and enc and #enc > 2 then pcall(function() writefile("SpeedHubConfig.json", enc) end) pcall(function() writefile("DIVINECCConfig.json", enc) end) end end makeSectionLabel("Settings", "Config") saveBtn = makeActionBtn("Settings", "Save Config", nil) saveBtn.MouseButton1Click:Connect(function() autoSaveConfig() local ot = saveBtn.Text saveBtn.Text = "Saved!" TweenService:Create(saveBtn, TweenInfo.new(0.1), { BackgroundColor3 = C.ACCENT }):Play() task.delay(1.2, function() saveBtn.Text = ot TweenService:Create(saveBtn, TweenInfo.new(0.3), { BackgroundColor3 = C.PANEL }):Play() end) end) makeGap("Settings", 6) do local footerS = Instance.new("TextLabel", tabPages["Settings"]) footerS.Size = UDim2.new(1, 0, 0, 18) footerS.BackgroundTransparency = 1 footerS.LayoutOrder = LO("Settings") footerS.Text = "DIVINE.CC" footerS.TextColor3 = C.ACCENT footerS.Font = Enum.Font.Gotham footerS.TextSize = 10 footerS.TextXAlignment = Enum.TextXAlignment.Center end do local function makeSearchRow(tabName) local page = tabPages[tabName] if not page then return end local row = Instance.new("Frame", page) row.Size = UDim2.new(1, 0, 0, 42) row.BackgroundColor3 = C.ROW row.BackgroundTransparency = 0.15 row.BorderSizePixel = 0 row.LayoutOrder = 0 Instance.new("UICorner", row).CornerRadius = UDim.new(0, 16) local rs = Instance.new("UIStroke", row) rs.Color = C.BORDER rs.Thickness = 1 local leftBar = Instance.new("Frame", row) leftBar.Size = UDim2.new(0, 3, 0.5, 0) leftBar.Position = UDim2.new(0, 0, 0.25, 0) leftBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) leftBar.BorderSizePixel = 0 leftBar.ZIndex = 4 Instance.new("UICorner", leftBar).CornerRadius = UDim.new(0, 14) local box = Instance.new("TextBox", row) box.Size = UDim2.new(1, -16, 1, 0) box.Position = UDim2.new(0, 10, 0, 0) box.BackgroundTransparency = 1 box.BorderSizePixel = 0 box.Text = "" box.PlaceholderText = "/search.." box.PlaceholderColor3 = Color3.fromRGB(255, 255, 255) box.TextColor3 = Color3.fromRGB(255, 255, 255) box.Font = Enum.Font.Gotham box.TextSize = 12 box.ClearTextOnFocus = false box.TextXAlignment = Enum.TextXAlignment.Left box.ZIndex = 3 box.InputBegan:Connect(function(inp) inp:StopPropagation() end) box.Focused:Connect(function() TweenService:Create(rs, TweenInfo.new(0.15), { Color = C.ACCENT }):Play() end) local function buildIndex() local t = {} for _, entry in ipairs(SearchIndex) do if entry.tab == tabName then t[entry.frame] = entry end end return t end local function applyFilter() local q = box.Text:lower():gsub("^%s+", ""):gsub("%s+$", "") local searching = (q ~= "") local idx = buildIndex() for _, child in ipairs(page:GetChildren()) do if not child:IsA("GuiObject") then continue end if child == row then continue end local entry = idx[child] if entry and entry.btn then child.Visible = (not searching or entry.label:lower():find(q, 1, true) ~= nil) else child.Visible = not searching end end end box.FocusLost:Connect(function() TweenService:Create(rs, TweenInfo.new(0.15), { Color = C.BORDER }):Play() if box.Text == "" then applyFilter() end end) box:GetPropertyChangedSignal("Text"):Connect(applyFilter) box.Active = true box.Selectable = true end for _, name in ipairs(TAB_NAMES) do makeSearchRow(name) end end local function findMedusa() local char = LP.Character if not char then return nil end for _, tool in ipairs(char:GetChildren()) do if tool:IsA("Tool") then local tn = tool.Name:lower() if tn:find("medusa") or tn:find("head") or tn:find("stone") then return tool end end end local bp2 = LP:FindFirstChild("Backpack") if bp2 then for _, tool in ipairs(bp2:GetChildren()) do if tool:IsA("Tool") then local tn = tool.Name:lower() if tn:find("medusa") or tn:find("head") or tn:find("stone") then return tool end end end end return nil end local function useMedusaCounter() if State.medusaDebounce then return end if tick() - State.medusaLastUsed < 25 then return end local char = LP.Character if not char then return end State.medusaDebounce = true local med = findMedusa() if not med then State.medusaDebounce = false return end if med.Parent ~= char then local hum2 = char:FindFirstChildOfClass("Humanoid") if hum2 then hum2:EquipTool(med) end end pcall(function() med:Activate() end) State.medusaLastUsed = tick() State.medusaDebounce = false end local function onAnchorChanged(part) return part:GetPropertyChangedSignal("Anchored"):Connect(function() if part.Anchored and part.Transparency == 1 then useMedusaCounter() end end) end setupMedusaCounter = function(char) stopMedusaCounter() if not char then return end for _, part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then table.insert(Conns.anchor, onAnchorChanged(part)) end end table.insert( Conns.anchor, char.DescendantAdded:Connect(function(part) if part:IsA("BasePart") then table.insert(Conns.anchor, onAnchorChanged(part)) end end) ) end stopMedusaCounter = function() for _, c in pairs(Conns.anchor) do pcall(function() c:Disconnect() end) end Conns.anchor = {} end stopAutoMedusaReset = function() if Conns.autoMedusaReset then Conns.autoMedusaReset:Disconnect() Conns.autoMedusaReset = nil end if Conns.autoMedusaResetDesc then Conns.autoMedusaResetDesc:Disconnect() Conns.autoMedusaResetDesc = nil end State.autoMedusaResetDebounce = false end startAutoMedusaReset = function(char) stopAutoMedusaReset() if not char then return end Conns.autoMedusaReset = RunService.Heartbeat:Connect(function() if not State.autoMedusaResetEnabled or State.autoMedusaResetDebounce then return end local c = LP.Character if not c then return end for _, part in ipairs(c:GetDescendants()) do if part:IsA("BasePart") and part.Anchored and part.Transparency == 1 then State.autoMedusaResetDebounce = true task.spawn(instantReset) task.delay(1.5, function() State.autoMedusaResetDebounce = false end) break end end end) Conns.autoMedusaResetDesc = char.DescendantAdded:Connect(function(part) if not State.autoMedusaResetEnabled or State.autoMedusaResetDebounce then return end if part:IsA("BasePart") and part.Anchored and part.Transparency == 1 then State.autoMedusaResetDebounce = true task.spawn(instantReset) task.delay(1.5, function() State.autoMedusaResetDebounce = false end) end end) end startAutoLeft = function() if Conns.autoLeft then Conns.autoLeft:Disconnect() end State.autoLeftPhase = 1 Conns.autoLeft = RunService.Heartbeat:Connect(function() if not State.autoLeftEnabled then return end if State.safeModeEnabled and State.isGrabbing then return end local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum2 = char:FindFirstChildOfClass("Humanoid") if not root or not hum2 then return end local spd = getAutoMoveSpeed() if State.autoLeftPhase == 1 then local tgt = Vector3.new(POS.L1.X, root.Position.Y, POS.L1.Z) if (tgt - root.Position).Magnitude < 1 then State.autoLeftPhase = 2 return end local d = (POS.L1 - root.Position) local mv = Vector3.new(d.X, 0, d.Z).Unit hum2:Move(mv, false) root.AssemblyLinearVelocity = Vector3.new(mv.X * spd, root.AssemblyLinearVelocity.Y, mv.Z * spd) elseif State.autoLeftPhase == 2 then local tgt = Vector3.new(POS.L2.X, root.Position.Y, POS.L2.Z) if (tgt - root.Position).Magnitude < 1 then hum2:Move(Vector3.zero, false) root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0) State.autoLeftEnabled = false if Conns.autoLeft then Conns.autoLeft:Disconnect() Conns.autoLeft = nil end State.autoLeftPhase = 1 if setAutoLeft then setAutoLeft(false) end return end local d = (POS.L2 - root.Position) local mv = Vector3.new(d.X, 0, d.Z).Unit hum2:Move(mv, false) root.AssemblyLinearVelocity = Vector3.new(mv.X * spd, root.AssemblyLinearVelocity.Y, mv.Z * spd) end end) end stopAutoLeft = function() if Conns.autoLeft then Conns.autoLeft:Disconnect() Conns.autoLeft = nil end State.autoLeftPhase = 1 local char = LP.Character if char then local hum2 = char:FindFirstChildOfClass("Humanoid") if hum2 then hum2:Move(Vector3.zero, false) end local root = char:FindFirstChild("HumanoidRootPart") if root then root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0) end end end startAutoRight = function() if Conns.autoRight then Conns.autoRight:Disconnect() end State.autoRightPhase = 1 Conns.autoRight = RunService.Heartbeat:Connect(function() if not State.autoRightEnabled then return end if State.safeModeEnabled and State.isGrabbing then return end local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum2 = char:FindFirstChildOfClass("Humanoid") if not root or not hum2 then return end local spd = getAutoMoveSpeed() if State.autoRightPhase == 1 then local tgt = Vector3.new(POS.R1.X, root.Position.Y, POS.R1.Z) if (tgt - root.Position).Magnitude < 1 then State.autoRightPhase = 2 return end local d = (POS.R1 - root.Position) local mv = Vector3.new(d.X, 0, d.Z).Unit hum2:Move(mv, false) root.AssemblyLinearVelocity = Vector3.new(mv.X * spd, root.AssemblyLinearVelocity.Y, mv.Z * spd) elseif State.autoRightPhase == 2 then local tgt = Vector3.new(POS.R2.X, root.Position.Y, POS.R2.Z) if (tgt - root.Position).Magnitude < 1 then hum2:Move(Vector3.zero, false) root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0) State.autoRightEnabled = false if Conns.autoRight then Conns.autoRight:Disconnect() Conns.autoRight = nil end State.autoRightPhase = 1 if setAutoRight then setAutoRight(false) end return end local d = (POS.R2 - root.Position) local mv = Vector3.new(d.X, 0, d.Z).Unit hum2:Move(mv, false) root.AssemblyLinearVelocity = Vector3.new(mv.X * spd, root.AssemblyLinearVelocity.Y, mv.Z * spd) end end) end stopAutoRight = function() if Conns.autoRight then Conns.autoRight:Disconnect() Conns.autoRight = nil end State.autoRightPhase = 1 local char = LP.Character if char then local hum2 = char:FindFirstChildOfClass("Humanoid") if hum2 then hum2:Move(Vector3.zero, false) end local root = char:FindFirstChild("HumanoidRootPart") if root then root.AssemblyLinearVelocity = Vector3.new(0, root.AssemblyLinearVelocity.Y, 0) end end end startAntiRagdoll = function() if Conns.antiRag then return end State._antiRagNoSplatterCD = State._antiRagNoSplatterCD or 0 Conns.antiRag = RunService.Heartbeat:Connect(function() if not State.antiRagdollEnabled then return end local char = LP.Character if not char then return end local hum2 = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not (hum2 and root) then return end local st = hum2:GetState() local ragdolled = ( st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll or st == Enum.HumanoidStateType.FallingDown ) local endTime = LP:GetAttribute("RagdollEndTime") if endTime and (endTime - workspace:GetServerTimeNow()) > 0 then ragdolled = true end if not ragdolled then return end if State.antiRagdollMode == "nosplatter" then local now = tick() if now - (State._antiRagNoSplatterCD or 0) <= 0.15 then return end State._antiRagNoSplatterCD = now pcall(function() hum2:ChangeState(Enum.HumanoidStateType.GettingUp) root.AssemblyLinearVelocity = Vector3.zero root.AssemblyAngularVelocity = Vector3.zero for _, obj in ipairs(char:GetDescendants()) do if obj:IsA("Motor6D") then obj.Enabled = true end if obj:IsA("Constraint") then obj.Enabled = true end end workspace.CurrentCamera.CameraSubject = hum2 local PM = LP.PlayerScripts and LP.PlayerScripts:FindFirstChild("PlayerModule") local controlModule = PM and PM:FindFirstChild("ControlModule") if controlModule then local ok, CM = pcall(require, controlModule) if ok and CM and CM.Enable then pcall(function() CM:Enable() end) end end hum2.AutoRotate = true hum2.PlatformStand = false hum2.Sit = false end) else pcall(function() LP:SetAttribute("RagdollEndTime", workspace:GetServerTimeNow()) end) for _, d in ipairs(char:GetDescendants()) do if d:IsA("BallSocketConstraint") or (d:IsA("Attachment") and d.Name:find("RagdollAttachment")) then pcall(function() d:Destroy() end) end end for _, obj in ipairs(char:GetDescendants()) do if obj:IsA("Motor6D") and not obj.Enabled then obj.Enabled = true end end if hum2.Health > 0 then hum2:ChangeState(Enum.HumanoidStateType.Running) end workspace.CurrentCamera.CameraSubject = hum2 root.Anchored = false root.AssemblyLinearVelocity = Vector3.zero root.AssemblyAngularVelocity = Vector3.zero root.CFrame = CFrame.new(root.Position) * CFrame.Angles(0, math.rad(root.Orientation.Y), 0) end end) end stopAntiRagdoll = function() if Conns.antiRag then Conns.antiRag:Disconnect() Conns.antiRag = nil end end applyFPSBoost = function() pcall(function() setfpscap(999999999) end) local function processObj(v) pcall(function() if v:IsA("Model") then v.LevelOfDetail = Enum.ModelLevelOfDetail.Disabled elseif v:IsA("MeshPart") then v.CastShadow = false v.RenderFidelity = Enum.RenderFidelity.Performance elseif v:IsA("BasePart") then v.CastShadow = false v.Material = Enum.Material.Plastic v.Reflectance = 0 elseif v:IsA("Decal") or v:IsA("Texture") then v.Transparency = 1 elseif v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") or v:IsA("ParticleEmitter") then v.Enabled = false elseif v:IsA("SurfaceAppearance") then v:Destroy() end end) end for _, v in pairs(workspace:GetDescendants()) do processObj(v) end pcall(function() local lighting = game:GetService("Lighting") for _, v in pairs(lighting:GetDescendants()) do pcall(function() if v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("PostEffect") then v:Destroy() end end) end lighting.GlobalShadows = false lighting.FogEnd = 9e9 lighting.Brightness = 0 end) workspace.DescendantAdded:Connect(function(v) if State.fpsBoostEnabled then task.spawn(processObj, v) end end) end local function setupChar(char) task.wait(0.1) originalAnims = nil h = char:WaitForChild("Humanoid", 5) hrp = char:WaitForChild("HumanoidRootPart", 5) if not h or not hrp then return end State.originalC0 = nil local head = char:FindFirstChild("Head") if head then local oldBB = head:FindFirstChild("SpeedBillboard") if oldBB then oldBB:Destroy() end local bb = Instance.new("BillboardGui", head) bb.Name = "SpeedBillboard" bb.Size = UDim2.new(0, 200, 0, 50) bb.StudsOffset = Vector3.new(0, 3, 0) bb.AlwaysOnTop = true speedLbl = Instance.new("TextLabel", bb) speedLbl.Size = UDim2.new(1, 0, 0.5, 0) speedLbl.Position = UDim2.new(0, 0, 0, 0) speedLbl.BackgroundTransparency = 1 speedLbl.TextColor3 = Color3.fromRGB(255, 255, 255) speedLbl.Font = Enum.Font.GothamBold speedLbl.TextScaled = true speedLbl.TextStrokeTransparency = 0 local discordLbl = Instance.new("TextLabel", bb) discordLbl.Size = UDim2.new(1, 0, 0.5, 0) discordLbl.Position = UDim2.new(0, 0, 0.5, 0) discordLbl.BackgroundTransparency = 1 discordLbl.TextColor3 = Color3.fromRGB(255, 255, 255) discordLbl.Font = Enum.Font.GothamBold discordLbl.TextScaled = false discordLbl.TextSize = 18 discordLbl.TextStrokeTransparency = 0 discordLbl.Text = "discord.gg/kNWV7gYnr" local lpSpeedShimmer = addSpeedShimmer(speedLbl, Color3.fromRGB(60, 60, 60), Color3.fromRGB(255, 255, 255)) local lpDiscShimmer = addSpeedShimmer(discordLbl, Color3.fromRGB(80, 80, 80), Color3.fromRGB(255, 255, 255)) task.spawn(function() local t = 0 while bb and bb.Parent do t = t + 0.03 lpSpeedShimmer.Offset = Vector2.new(math.sin(t) * 0.4, 0) lpDiscShimmer.Offset = Vector2.new(math.sin(t) * 0.4, 0) task.wait(0.04) end end) end if State.antiRagdollEnabled and not Conns.antiRag then task.wait(0.5) startAntiRagdoll() end if State.medusaCounterEnabled then setupMedusaCounter(char) end if State.animEnabled then task.wait(0.3) saveOriginalAnims(char) applyAnimPack(char) end if State.unwalkEnabled then task.wait(0.3) State.unwalkEnabled = false startUnwalk() end if State.autoMedusaResetEnabled then startAutoMedusaReset(char) end if State.autoBatToggled then startBatAimbot() end if State.batCounterEnabled then startBatCounter() end end LP.CharacterAdded:Connect(setupChar) if LP.Character then task.spawn(function() setupChar(LP.Character) end) end StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Health, false) LP.CharacterAdded:Connect(function(character) StarterGui:SetCoreGuiEnabled(Enum.CoreGuiType.Health, false) end) RunService.Stepped:Connect(function() for _, p in ipairs(Players:GetPlayers()) do if p ~= LP and p.Character then for _, part in ipairs(p.Character:GetChildren()) do if part:IsA("BasePart") then part.CanCollide = false end end end end end) INF_JUMP_UP = 50 INF_JUMP_MAX_Y = 50 infJumpTapUsed = false UIS.JumpRequest:Connect(function() if not State.infJumpEnabled or State.infJumpMode ~= "tap" then return end if infJumpTapUsed then return end infJumpTapUsed = true local char = LP.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local vel = root.AssemblyLinearVelocity if vel.Y < INF_JUMP_MAX_Y then root.AssemblyLinearVelocity = Vector3.new(vel.X, INF_JUMP_UP, vel.Z) end end) UIS.InputEnded:Connect(function(inp) if inp.KeyCode == Enum.KeyCode.Space or inp.UserInputType == Enum.UserInputType.Touch then infJumpTapUsed = false end end) RunService.Heartbeat:Connect(function() if not State.infJumpEnabled then return end if State.infJumpMode ~= "hold" then return end local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root then return end local jumping = hum.Jump or UIS:IsKeyDown(Enum.KeyCode.Space) if jumping then local vel = root.AssemblyLinearVelocity if vel.Y < INF_JUMP_MAX_Y then root.AssemblyLinearVelocity = Vector3.new(vel.X, INF_JUMP_UP, vel.Z) end end end) RunService.Heartbeat:Connect(function() if not (h and hrp) then return end if State._tpInProgress then return end if State.autoTpDownEnabled then local char = LP.Character local root = char and char:FindFirstChild("HumanoidRootPart") if root then local curY = root.Position.Y if curY >= State.autoTpDownY then local rot = root.CFrame.Rotation root.CFrame = CFrame.new(root.Position.X, -8.80, root.Position.Z) * rot end end end if State.currentFOV and workspace.CurrentCamera then if math.abs(workspace.CurrentCamera.FieldOfView - State.currentFOV) > 1 then pcall(function() workspace.CurrentCamera.FieldOfView = State.currentFOV end) end end end) RunService.Heartbeat:Connect(function(dt) if not (h and hrp) then State._resetSpeedTracking(nil) return end if State._tpInProgress then State._resetSpeedTracking(hrp) return end local guidedMovement = State.autoBatToggled or State.autoLeftEnabled or State.autoRightEnabled if guidedMovement then State._resetSpeedTracking(hrp) else local md = h.MoveDirection local spd = getCurrentSpeed() State._speedOwnershipTimer += dt if State._speedOwnershipTimer >= 1.5 then State._claimSpeedOwnership(hrp) State._speedOwnershipTimer = 0 end local vonStealing = (VonData and VonData.IsStealing) == true if not vonStealing then if h.WalkSpeed ~= spd then h.WalkSpeed = spd end end if md.Magnitude > 0 then State.lastMoveDir = md local currentVel = hrp.AssemblyLinearVelocity local targetVel = Vector3.new(md.X * spd, currentVel.Y, md.Z * spd) local alpha = math.min(dt * 22, 1) hrp.AssemblyLinearVelocity = currentVel:Lerp(targetVel, alpha) State._speedLagbackCooldown -= dt local now = tick() local elapsed = State._speedLastTime and (now - State._speedLastTime) or 0 if elapsed > 0.1 and State._speedLastPosition then local expectedDist = spd * elapsed local actualDist = (hrp.Position - State._speedLastPosition).Magnitude if actualDist < expectedDist * 0.3 and State._speedLagbackCooldown <= 0 then hrp.AssemblyLinearVelocity = targetVel * 1.2 State._speedLagbackCooldown = 0.3 end end State._speedLastPosition = hrp.Position State._speedLastTime = now elseif State.antiRagdollEnabled and State.lastMoveDir.Magnitude > 0 then local anyHeld = false for key in pairs(MOVE_KEYS) do if UIS:IsKeyDown(key) then anyHeld = true break end end if anyHeld then local currentVel = hrp.AssemblyLinearVelocity local targetVel = Vector3.new(State.lastMoveDir.X * spd, currentVel.Y, State.lastMoveDir.Z * spd) hrp.AssemblyLinearVelocity = currentVel:Lerp(targetVel, math.min(dt * 22, 1)) end State._resetSpeedTracking(hrp) else State._resetSpeedTracking(hrp) end end pcall(function() if speedLbl then local hspd = Vector3.new(hrp.AssemblyLinearVelocity.X, 0, hrp.AssemblyLinearVelocity.Z).Magnitude if h.MoveDirection.Magnitude > 0 and not guidedMovement then hspd = getCurrentSpeed() end speedLbl.Text = string.format("%.1f", hspd) end end) end) State.vonLastReset = State.vonLastReset or 0 RunService.Heartbeat:Connect(function() if not VonData.IsStealing then if tick() - (State.vonLastReset or 0) > 2 then State.vonLastReset = tick() if progressFill then progressFill.Size = UDim2.new(0, 0, 1, 0) end end end end) task.defer(function() if State.vonEnabled and autoGrabSync then autoGrabSync() end end) safeModeHoldingBrainrot = function() local ok, val = pcall(function() return LP:GetAttribute("Stealing") end) if ok and val == true then return true end local ok2, val2 = pcall(function() return LP:GetAttribute("AntiKick") end) if ok2 and val2 == true then return true end local char = LP.Character if not char then return false end local ok3, val3 = pcall(function() return char:GetAttribute("Stealing") end) if ok3 and val3 == true then return true end for _, name in ipairs({ "Carrying", "IsCarrying", "Grabbed", "Holding", "StealHold", "HasGrab" }) do local v = char:FindFirstChild(name, true) if v then if v:IsA("BoolValue") and v.Value then return true end if v:IsA("ObjectValue") and v.Value then return true end if v:IsA("StringValue") and v.Value ~= "" then return true end end end for _, child in ipairs(char:GetChildren()) do if child:IsA("Model") and child:FindFirstChildWhichIsA("BasePart", true) then local n = child.Name:lower() if n:find("brainrot") or n:find("animal") or n:find("carry") or n:find("grab") or n:find("steal") or n:find("hold") then return true end end end return false end safeModeIsLocked = function() if not State.safeModeEnabled then return false end return safeModeHoldingBrainrot() end safeModeForceStop = function(reason) if State.autoLeftEnabled or Conns.autoLeft then State.autoLeftEnabled = false pcall(stopAutoLeft) if setAutoLeft then pcall(setAutoLeft, false) end end if State.autoRightEnabled or Conns.autoRight then State.autoRightEnabled = false pcall(stopAutoRight) if setAutoRight then pcall(setAutoRight, false) end end if State.autoBatToggled or _batAimbotConn then State.autoBatToggled = false pcall(stopBatAimbot) if setAutoBat then pcall(setAutoBat, false) end end if State.batV2Toggled or (Conns and Conns.batV2) then State.batV2Toggled = false pcall(stopBatV2Aimbot) if setBypassBatUI then pcall(setBypassBatUI, false) end end State.safeModeActive = true State.isGrabbing = true end safeModeTryStart = function() if State.safeModeEnabled and safeModeIsLocked() then safeModeForceStop("SAFE MODE LOCK") return false end return true end safeModeBlockOn = function(on, uiSetter) if on and State.safeModeEnabled and safeModeIsLocked() then if uiSetter then pcall(uiSetter, false) end return true end return false end applySafeModeRestore = function() State.safeModeActive = false State.savedStealFeatures = nil State.isGrabbing = false end RunService.Heartbeat:Connect(function() if not State.safeModeEnabled then if State.safeModeActive or State.isGrabbing then applySafeModeRestore() end return end local locked = safeModeIsLocked() State.isGrabbing = locked if locked then safeModeForceStop("SAFE MODE LOCK") elseif State.safeModeActive then State.safeModeActive = false end State.safeModeLastIsStealing = locked end) UIS.InputBegan:Connect(function(inp, gp) if gp then return end if inp.UserInputType ~= Enum.UserInputType.Keyboard and inp.UserInputType ~= Enum.UserInputType.Gamepad1 then return end local kc = inp.KeyCode if State.autoLeftEnabled or State.autoRightEnabled then if MOVE_KEYS[kc] then return end end if kc == Keys.speed then State.speedType = (State.speedType == "normal") and "carry" or "normal" if State.speedType == "carry" and State.laggerActive then State.laggerActive = false if setLaggerToggleUI then setLaggerToggleUI(false) end end if setSpeedToggleUI then setSpeedToggleUI(State.speedType == "carry") end State.applyLiveWalkSpeed() autoSaveConfig() elseif kc == Keys.lagger then State.laggerActive = not State.laggerActive if State.laggerActive and State.speedType == "carry" then State.speedType = "normal" if setSpeedToggleUI then setSpeedToggleUI(false) end end if setLaggerToggleUI then setLaggerToggleUI(State.laggerActive) end State.applyLiveWalkSpeed() autoSaveConfig() elseif kc == Keys.autoBat then local nv = not State.autoBatToggled if nv and State.safeModeEnabled and (State.safeModeActive or State.isGrabbing or (safeModeIsLocked and safeModeIsLocked())) then return end if nv then if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end end State.autoBatToggled = nv if setAutoBat then setAutoBat(nv) end if nv then startBatAimbot() else stopBatAimbot() end autoSaveConfig() elseif kc == Keys.batV2 then local nv = not State.batV2Toggled if nv and State.safeModeEnabled and (State.safeModeActive or State.isGrabbing or (safeModeIsLocked and safeModeIsLocked())) then return end if nv then if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end end State.batV2Toggled = nv if setBypassBatUI then setBypassBatUI(nv) end autoSaveConfig() elseif kc == Keys.autoLeft then local nv = not State.autoLeftEnabled if nv and State.safeModeEnabled and (State.safeModeActive or State.isGrabbing or (safeModeIsLocked and safeModeIsLocked())) then return end if nv then if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if State.autoRightEnabled then State.autoRightEnabled = false if setAutoRight then setAutoRight(false) end stopAutoRight() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end end State.autoLeftEnabled = nv if setAutoLeft then setAutoLeft(nv) end if nv then startAutoLeft() else stopAutoLeft() end autoSaveConfig() elseif kc == Keys.autoRight then local nv = not State.autoRightEnabled if nv and State.safeModeEnabled and (State.safeModeActive or State.isGrabbing or (safeModeIsLocked and safeModeIsLocked())) then return end if nv then if State.autoBatToggled then State.autoBatToggled = false if setAutoBat then setAutoBat(false) end stopBatAimbot() end if State.autoLeftEnabled then State.autoLeftEnabled = false if setAutoLeft then setAutoLeft(false) end stopAutoLeft() end if State.batV2Toggled then State.batV2Toggled = false if setBypassBatUI then setBypassBatUI(false) end stopBatV2Aimbot() end end State.autoRightEnabled = nv if setAutoRight then setAutoRight(nv) end if nv then startAutoRight() else stopAutoRight() end autoSaveConfig() elseif kc == Keys.dropBrainrot then task.spawn(runDropBrainrot) elseif kc == Keys.tpDown then tpToGround() elseif kc == Keys.autoMedusaReset then local nv = not State.autoMedusaResetEnabled State.autoMedusaResetEnabled = nv if setAutoMedusaResetUI then setAutoMedusaResetUI(nv) end if nv then startAutoMedusaReset(LP.Character) else stopAutoMedusaReset() end autoSaveConfig() elseif kc == Keys.guiHide then if State.guiVisible then hideGui() else showGui() end end end) _introAnimEnabled = State.introEnabled pcall(function() local _s = readfile("SpeedHubConfig.json") if not _s or _s == "" then _s = readfile("DIVINECCConfig.json") end if _s and _s ~= "" then local _c = game:GetService("HttpService"):JSONDecode(_s) if _c and _c.introEnabled == false then _introAnimEnabled = false end end end) if _introAnimEnabled then task.spawn(function() local introGui = Instance.new("ScreenGui") introGui.Name = "DIVINE.CC_Intro" introGui.ResetOnSpawn = false introGui.DisplayOrder = 999 introGui.IgnoreGuiInset = true introGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling introGui.Parent = LP:WaitForChild("PlayerGui") local bg = Instance.new("Frame", introGui) bg.Size = UDim2.new(1, 0, 1, 0) bg.BackgroundColor3 = Color3.fromRGB(8, 8, 10) bg.BackgroundTransparency = 0 bg.BorderSizePixel = 0 bg.ZIndex = 1 local bgGrad = Instance.new("UIGradient", bg) bgGrad.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(12, 12, 18)), ColorSequenceKeypoint.new(1, Color3.fromRGB(5, 5, 8)), }) bgGrad.Rotation = 135 local speedLbl = Instance.new("TextLabel", introGui) speedLbl.Size = UDim2.new(0, 500, 0, 90) speedLbl.AnchorPoint = Vector2.new(0.5, 0.5) speedLbl.Position = UDim2.new(0.5, 0, -0.25, 0) speedLbl.BackgroundTransparency = 1 speedLbl.Text = "DIVINE" speedLbl.TextColor3 = Color3.fromRGB(255, 255, 255) speedLbl.Font = Enum.Font.GothamBlack speedLbl.TextSize = 80 speedLbl.TextTransparency = 1 speedLbl.ZIndex = 10 local speedGrad = Instance.new("UIGradient", speedLbl) speedGrad.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(200, 200, 200)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(1, Color3.fromRGB(200, 200, 200)), }) speedGrad.Rotation = 0 local hubLbl = Instance.new("TextLabel", introGui) hubLbl.Size = UDim2.new(0, 500, 0, 90) hubLbl.AnchorPoint = Vector2.new(0.5, 0.5) hubLbl.Position = UDim2.new(0.5, 0, 1.25, 0) hubLbl.BackgroundTransparency = 1 hubLbl.Text = ".CC" hubLbl.TextColor3 = Color3.fromRGB(255, 255, 255) hubLbl.Font = Enum.Font.GothamBlack hubLbl.TextSize = 80 hubLbl.TextTransparency = 1 hubLbl.ZIndex = 10 local hubGrad = Instance.new("UIGradient", hubLbl) hubGrad.Color = ColorSequence.new({ ColorSequenceKeypoint.new(0, Color3.fromRGB(200, 200, 200)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(1, Color3.fromRGB(200, 200, 200)), }) hubGrad.Rotation = 0 local accentLine = Instance.new("Frame", introGui) accentLine.Size = UDim2.new(0, 0, 0, 2) accentLine.AnchorPoint = Vector2.new(0.5, 0.5) accentLine.Position = UDim2.new(0.5, 0, 0.5, 0) accentLine.BackgroundColor3 = Color3.fromRGB(255, 255, 255) accentLine.BorderSizePixel = 0 accentLine.BackgroundTransparency = 1 accentLine.ZIndex = 9 local glowDot = Instance.new("Frame", introGui) glowDot.Size = UDim2.new(0, 6, 0, 6) glowDot.AnchorPoint = Vector2.new(0.5, 0.5) glowDot.Position = UDim2.new(0.5, 0, 0.5, 0) glowDot.BackgroundColor3 = Color3.fromRGB(255, 255, 255) glowDot.BorderSizePixel = 0 glowDot.BackgroundTransparency = 0.3 glowDot.ZIndex = 9 Instance.new("UICorner", glowDot).CornerRadius = UDim.new(1, 0) if main then main.Visible = false end task.wait(0.3) TweenService :Create( speedLbl, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { TextTransparency = 0 } ) :Play() TweenService :Create( hubLbl, TweenInfo.new(0.5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { TextTransparency = 0 } ) :Play() TweenService:Create(speedLbl, TweenInfo.new(1.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Position = UDim2.new(0.5, 0, 0.5, -58), }):Play() TweenService:Create(hubLbl, TweenInfo.new(1.1, Enum.EasingStyle.Back, Enum.EasingDirection.Out), { Position = UDim2.new(0.5, 0, 0.5, 58), }):Play() task.spawn(function() local t = 0 while introGui and introGui.Parent do t = t + 0.04 local offset = math.sin(t * 2.5) * 0.35 speedGrad.Offset = Vector2.new(offset, 0) hubGrad.Offset = Vector2.new(-offset, 0) task.wait(0.03) end end) task.wait(1.25) TweenService:Create(accentLine, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = UDim2.new(0, 220, 0, 2), BackgroundTransparency = 0.5, }):Play() TweenService:Create(glowDot, TweenInfo.new(0.6, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = UDim2.new(0, 120, 0, 120), BackgroundTransparency = 1, }):Play() task.wait(1.0) TweenService :Create( bg, TweenInfo.new(0.8, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { BackgroundTransparency = 1 } ) :Play() TweenService :Create( speedLbl, TweenInfo.new(0.65, Enum.EasingStyle.Quad, Enum.EasingDirection.In), { TextTransparency = 1 } ) :Play() TweenService :Create( hubLbl, TweenInfo.new(0.65, Enum.EasingStyle.Quad, Enum.EasingDirection.In), { TextTransparency = 1 } ) :Play() TweenService:Create(accentLine, TweenInfo.new(0.55), { BackgroundTransparency = 1 }):Play() task.wait(0.55) if main then main.Visible = true end task.wait(0.45) introGui:Destroy() end) else if main then main.Visible = true end end loadSavedPos(main, "DIVINE.CC_GUIPos.txt") loadSavedPos(miniBtn, "SpeedHubMiniPos.txt") updateClosePos() task.spawn(function() task.wait(0.5) createMobilePanel() end) createGrabFloatingPanel() ; (function() local antiDieConfig = { healthThreshold = 25, healAmount = 100, fallDamageProtection = true, ragdollProtection = true, invincibilityFrames = 0.5, autoRevive = true, } local antiDieLoop = nil local lastHealTime = 0 local invincibleUntil = 0 local function superHeal(hum) if not hum then return end local maxHealth = hum.MaxHealth or 100 if hum.Health >= maxHealth and hum.Health > 0 then return end hum.Health = maxHealth invincibleUntil = tick() + antiDieConfig.invincibilityFrames lastHealTime = tick() pcall(function() local char = hum.Parent if char then for _, child in ipairs(char:GetChildren()) do if child:IsA("NumberValue") then local name = child.Name:lower() if name:find("health") or name:find("hp") or name:find("life") then child.Value = 100 end end end if hum.Health < maxHealth then hum.Health = maxHealth end end end) pcall(function() if hum.Parent then for _, child in ipairs(hum.Parent:GetChildren()) do if child:IsA("BoolValue") and child.Name:lower():find("dead") then child.Value = false end end end end) end local function preventDamage(root, hum) if not hum then return end if tick() < invincibleUntil then if hum.Health < hum.MaxHealth then hum.Health = hum.MaxHealth or 100 end end if antiDieConfig.fallDamageProtection and root then local yv = 0 pcall(function() yv = root.AssemblyLinearVelocity.Y end) if yv < -80 and hum.Health < hum.MaxHealth then superHeal(hum) end end if antiDieConfig.ragdollProtection then local state = hum:GetState() if state == Enum.HumanoidStateType.Physics or state == Enum.HumanoidStateType.Ragdoll then hum:ChangeState(Enum.HumanoidStateType.GettingUp) superHeal(hum) end end if hum.Health <= 0 then superHeal(hum) hum:ChangeState(Enum.HumanoidStateType.Running) if root then root.CFrame = CFrame.new(root.Position + Vector3.new(0, 2, 0)) root.Velocity = Vector3.zero end end end local function autoRevive() if not antiDieConfig.autoRevive then return end local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end if hum.Health <= 0 then superHeal(hum) hum:ChangeState(Enum.HumanoidStateType.Running) local root = char:FindFirstChild("HumanoidRootPart") if root then root.CFrame = CFrame.new(root.Position + Vector3.new(0, 3, 0)) root.Velocity = Vector3.zero end end end local function setupAntiDie() if antiDieLoop then antiDieLoop:Disconnect() antiDieLoop = nil end antiDieLoop = RunService.Heartbeat:Connect(function() local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum then return end if hum.Health <= 0 then autoRevive() return end if hum.Health <= antiDieConfig.healthThreshold then superHeal(hum) pcall(function() if hum.Health < 50 then hum.Health = 100 end end) end preventDamage(root, hum) if hum.Health < 20 and hum.Health > 0 then superHeal(hum) end if hum.Health <= 0 then superHeal(hum) hum:ChangeState(Enum.HumanoidStateType.Running) if root then root.CFrame = CFrame.new(root.Position + Vector3.new(0, 2, 0)) root.Velocity = Vector3.zero end end end) end local function monitorHealth() local char = LP.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") if not hum then return end hum:GetPropertyChangedSignal("Health"):Connect(function() if hum.Health <= 0 then superHeal(hum) hum:ChangeState(Enum.HumanoidStateType.Running) task.wait(0.05) local root = char:FindFirstChild("HumanoidRootPart") if root then root.CFrame = CFrame.new(root.Position + Vector3.new(0, 3, 0)) root.Velocity = Vector3.zero end end end) end setupAntiDie() LP.CharacterAdded:Connect(function(char) task.wait(0.1) local hum = char:FindFirstChildOfClass("Humanoid") if hum then superHeal(hum) hum.Health = hum.MaxHealth or 100 end setupAntiDie() task.wait(0.1) monitorHealth() end) task.spawn(function() if LP.Character then task.wait(0.1) monitorHealth() local hum = LP.Character:FindFirstChildOfClass("Humanoid") if hum then superHeal(hum) hum.Health = hum.MaxHealth or 100 end end end) LP.CharacterAdded:Connect(function(char) task.wait(0.05) local hum = char:FindFirstChildOfClass("Humanoid") if hum and hum.Health <= 0 then superHeal(hum) hum.Health = hum.MaxHealth or 100 hum:ChangeState(Enum.HumanoidStateType.Running) end end) end)() loadConfig()